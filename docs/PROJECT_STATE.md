> **Standing instruction:** This document is regenerated/updated at the end of each working session before switching chats. It is the authoritative current-state snapshot. If anything here conflicts with memory, a prior summary, or MASTER_CONTEXT.md, THIS document wins (it is verified against the live codebase and live DB on the date shown above).
>
> **At the end of every working session:** refresh the ENTIRE document — structure, schema, endpoints, feature status, AND the Known Issues Register — against the live codebase. Remove resolved issues, keep unresolved ones, add newly-found ones.

---

# PROJECT_STATE.md — Ziva BI Authoritative Codebase Snapshot

**Generated:** 2026-06-23  
**Git commit:** `227052d38e2f61f397927a055db112637672cdca` (pre-this-session)  
**After this session commit:** see git log  
**Branch:** `main`  
**Method:** Generated by reading actual source files (models, routers, services, migrations) and querying the live `ziva_dev` database directly — NOT from memory or prior summaries.

## Summary

Ziva BI is a multi-tenant, enterprise-grade finance automation SaaS platform. Stack: Next.js 15 (App Router) + TailwindCSS frontend, Python 3.12 + FastAPI backend, PostgreSQL via async SQLAlchemy + Alembic, Supabase Storage for files. Auth via JWT (access + refresh tokens) with session tracking, TOTP 2FA, and role-based access. All data is tenant-scoped; the test/live environment model uses shadow tenants (linked via `parent_tenant_id` on the `tenants` table). The platform supports a full implementation lifecycle: Trial → In Implementation → Live → Suspended.

---

## 1. Repository Structure

```
ziva-bi/
├── backend/
│   ├── alembic/versions/         — 36 migration files (72a96af → z6a7b8c9d0e1)
│   ├── app/
│   │   ├── main.py               — FastAPI app boot, CORS, system role seeding, router registration
│   │   ├── config.py             — pydantic-settings: DATABASE_URL, ALLOWED_ORIGINS, SMTP, Supabase keys
│   │   ├── database.py           — async SQLAlchemy engine + AsyncSession factory
│   │   ├── core/security.py      — JWT create/verify, bcrypt hash, refresh token generation
│   │   ├── middleware/auth.py    — require_auth, require_super_admin, block_if_readonly_impersonation
│   │   ├── models/
│   │   │   ├── auth.py           — tenants, users, user_tenants, roles, permissions, role_permissions,
│   │   │   │                       user_roles, sessions, refresh_tokens, audit_logs
│   │   │   ├── setup.py          — implementation_locks, tenant_modules, document_rules,
│   │   │   │                       tenant_tax_config, tenant_fx_config, tenant_org_config,
│   │   │   │                       org_structure, accounting_periods, period_grace_overrides,
│   │   │   │                       future_posting_exceptions, close_checklist_items,
│   │   │   │                       period_checklist_completions, fiscal_year_states,
│   │   │   │                       period_audit_logs, employee_onboarding_tokens
│   │   │   ├── master_data.py    — tenant_dimensions, dimension_values, chart_of_accounts,
│   │   │   │                       gl_code_remaps, gl_dimension_requirements, category_gl_mappings,
│   │   │   │                       employees, employee_code_history, employee_transfers,
│   │   │   │                       cost_center_config, finance_review_config
│   │   │   ├── expenses.py       — expense_reports, expense_lines, expense_report_snapshots,
│   │   │   │                       tenant_expense_config, expense_categories
│   │   │   ├── approvals.py      — approval_matrix, expense_approvals
│   │   │   ├── gl.py             — journal_entries, journal_lines
│   │   │   ├── documents.py      — expense_documents
│   │   │   ├── bank_account.py   — bank_accounts
│   │   │   ├── account_mapping.py — posting_roles, tenant_account_mappings, tenant_posting_role_settings
│   │   │   └── tenant_management.py — tenant_invitations
│   │   ├── routers/
│   │   │   ├── auth.py           — signup, login, refresh-token, logout
│   │   │   ├── users.py          — profile, password, sessions, 2FA
│   │   │   ├── expenses.py       — expense report CRUD + submit
│   │   │   ├── approvals.py      — matrix config, submit, queue, approve/reject/refer-back
│   │   │   ├── tenant.py         — user management, invitations, test-environment, promote
│   │   │   ├── invitations.py    — public invite validate/accept
│   │   │   ├── documents.py      — file upload/list/delete (Supabase Storage)
│   │   │   ├── expense_config.py — coding level config, categories, form-config, GL coding
│   │   │   ├── config.py         — dimensions, dimension values, CoA, categories, GL search
│   │   │   ├── hr.py             — employees, cost-center heads, finance reviewers
│   │   │   ├── setup.py          — org, periods, modules, currencies, tax, roles, documents, go-live
│   │   │   ├── platform.py       — super-admin: tenant list/detail/lifecycle/suspend/enter
│   │   │   ├── gl.py             — trial balance, account ledger
│   │   │   ├── account_mapping.py — posting role → GL mapping
│   │   │   └── bank_accounts.py  — bank account register CRUD
│   │   ├── schemas/              — Pydantic request/response models (one file per router)
│   │   └── services/
│   │       ├── tenant_clone.py   — Phase 4 live→test clone engine, 13 steps
│   │       ├── expense_posting.py — posts approved expense report to GL (synchronous, same transaction)
│   │       ├── gl_posting.py     — core GL journal validator + post_journal (flush-only; caller commits)
│   │       ├── gl_reporting.py   — trial_balance() + account_ledger() query builders
│   │       ├── periods.py        — period state machine helpers: is_date_postable, grace computation
│   │       ├── promotion_engine.py — Phase 3a: diff + apply config promotion from test → live
│   │       ├── account_determination.py — resolve_account(): posting role_key → GL account ID
│   │       └── storage.py        — Supabase Storage upload/delete/signed-URL proxy
│   └── scripts/seed_m7_categories.py — one-off seed script for M7 expense categories
├── frontend/src/
│   ├── app/                      — Next.js App Router pages (see §5)
│   ├── components/
│   │   ├── AppHeader.tsx         — top navigation bar with environment toggle
│   │   ├── LockedField.tsx       — read-only field with "contact consultant" lock UI
│   │   ├── PromotionReviewDialog.tsx — test→live promotion review modal
│   │   └── expenses/
│   │       ├── ExpenseItemPicker.tsx — GL popup picker (group → subgroup → GL)
│   │       └── SplitLinePanel.tsx — split expense line management
│   ├── contexts/AuthContext.tsx  — JWT auth state, login/logout, token refresh
│   └── lib/
│       ├── api.ts                — typed fetch wrapper with auth headers + body serialization
│       ├── currencies.ts         — ISO 4217 currency list
│       └── utils.ts              — fmtCommaInput, stripCommas, cn (classnames)
└── docs/                         — PRDs, ADRs, briefs, diagnosis docs (markdown only)
```

---

## 2. Database Schema (authoritative — derived from ORM model files + live DB)

**Multi-tenancy mechanism:** Every operational table carries `tenant_id UUID NOT NULL FK → tenants.id ON DELETE CASCADE`. The JWT embeds `tenant_id`; the `require_auth` middleware extracts and enforces it. No query in normal operation can cross tenant boundaries. Shadow test tenants have a distinct `tenant_id` from their live parent, so isolation is automatic.

### 2.1 Tenant / Auth Tables

| Table | Key columns | Notes |
|---|---|---|
| `tenants` | `id UUID PK`, `name`, `country CHAR(2)`, `slug UNIQUE`, `is_active`, `environment` ('live'/'test'), `parent_tenant_id UUID FK→tenants SET NULL`, `lifecycle_status` ('trial'/'in_implementation'/'live'/'suspended'), `dimensions_not_applicable`, `documents_setup_complete`, `module_setup_visited JSONB`, `suppress_outbound_email`, `test_data_retention_days`, `pre_suspension_status` | Shadow test tenants have `parent_tenant_id` set. |
| `users` | `id UUID PK`, `email UNIQUE`, `full_name`, `first_name`, `account_type` (individual/business), `is_active`, `is_super_admin`, `employee_code`, `department`, `job_title`, `phone`, `totp_secret`, `totp_enabled` | Global identity — one row per person across all tenants |
| `user_tenants` | `id UUID PK`, `user_id FK→users CASCADE`, `tenant_id FK→tenants CASCADE NULL`, `password_hash`, `is_active`, `role_tier` ('consultant'/'power_admin'/'functional_admin'), `failed_login_attempts`, `locked_until`, `last_login_at` | tenant_id NULL = individual. UQ: (user_id, tenant_id); partial index for individual rows |
| `roles` | `id UUID PK`, `name`, `description`, `tenant_id FK→tenants CASCADE NULL`, `is_system` | 15 system roles seeded at startup |
| `permissions` | `id UUID PK`, `code UNIQUE`, `description` | Canonical permission codes, seeded at startup |
| `role_permissions` | `id PK`, `role_id FK→roles CASCADE`, `permission_id FK→permissions CASCADE`, UQ:(role_id,permission_id) | |
| `user_roles` | `id PK`, `user_tenant_id FK→user_tenants CASCADE`, `role_id FK→roles CASCADE`, `assigned_at`, `assigned_by_id`, `revoked_at` | |
| `sessions` | `id PK`, `user_tenant_id FK→user_tenants CASCADE`, `ip_address`, `user_agent`, `expires_at`, `ended_at`, `is_active` | |
| `refresh_tokens` | `id PK`, `session_id FK→sessions CASCADE`, `token_hash UNIQUE`, `expires_at`, `used_at`, `replaced_by_id`, `is_revoked` | SHA-256 hash only; token rotation + replay detection |
| `audit_logs` | `id PK`, `event_type`, `user_id FK→users SET NULL`, `tenant_id FK→tenants SET NULL`, `ip_address`, `log_metadata JSONB`, `created_at` | Append-only |
| `tenant_invitations` | `id PK`, `tenant_id FK→tenants CASCADE`, `invited_by FK→users SET NULL`, `email`, `role`, `token UNIQUE`, `status` ('PENDING'/'ACCEPTED'/'EXPIRED'), `expires_at` | 48-hour invite token |

### 2.2 Organisation & Setup Tables

| Table | Key columns | Notes |
|---|---|---|
| `tenant_org_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `legal_name`, `rc_number`, `date_of_registration DATE`, `commencement_date DATE`, `company_type`, `industry`, `tin`, `vat_reg_number`, `country`, `registered_address TEXT`, `operating_address TEXT`, `company_phone`, `company_email`, `website`, `external_auditor`, `group_structure`, `parent_company_name`, **`functional_currency CHAR(3)`**, **`reporting_currency CHAR(3)`**, **`enabled_currencies JSONB`** (sorted ISO list), `authorised_share_capital NUMERIC(20,2)`, `fiscal_year_start_month INT`, `fiscal_year_start_day INT`, `fiscal_year_name_format`, `period_closing_frequency`, `branding JSONB`, `org_configuration JSONB`, `block_journal_into_open_prior BOOL DEFAULT TRUE`, `default_audit_grace_months INT DEFAULT 3` | **SOLE SOURCE OF TRUTH for functional_currency, reporting_currency, enabled_currencies.** functional_currency protected via PROTECTED_ORG_FIELDS. Standing date floor: no transaction date may precede `date_of_registration`. |
| `tenant_fx_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `fx_rates JSONB`, `revaluation_rules JSONB` | FX mechanics ONLY — no currency identity here. |
| `tenant_tax_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `vat_config JSONB`, `wht_config JSONB`, `paye_config JSONB`, `other_statutory JSONB` | |
| `tenant_modules` | `id PK`, `tenant_id FK→tenants CASCADE`, `module_key`, `is_active BOOL`, `is_licensed BOOL`, `activated_at`, `activated_by FK→users SET NULL`, UQ:(tenant_id,module_key) | 403 if unlicensed activate attempted |
| `implementation_locks` | `id PK`, `tenant_id FK→tenants CASCADE`, `section`, `locked_at`, `locked_by FK→users SET NULL`, `notes` | |
| `document_rules` | `id PK`, `tenant_id FK→tenants CASCADE`, `module`, `transaction_type`, `document_name`, `is_required`, `track_expiry`, `max_size_mb INT DEFAULT 10`, `allowed_formats TEXT[]`, `max_files INT DEFAULT 0`, `is_active` | |
| `org_structure` | `id PK`, `tenant_id FK→tenants CASCADE`, `parent_id FK→org_structure NULL`, `node_type` ('Legal entity'/'Division / Business unit'/'Department'/'Cost center'), `name`, `code`, `cost_center_code`, `entity_code`, `is_active BOOL`, `sort_order INT`, UQ:(tenant_id,code) | **Sole source of truth for cost centers.** employees.cost_center_id and cost_center_config.cost_center_id both FK here. |
| `employee_onboarding_tokens` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `token UNIQUE`, `expires_at`, `used_at` | |

### 2.3 Period Management Tables

| Table | Key columns | Notes |
|---|---|---|
| `accounting_periods` | `id PK`, `tenant_id FK→tenants CASCADE`, `fiscal_year VARCHAR(20)`, `period_no INT`, `period_name`, `start_date DATE`, `end_date DATE`, `status` ('FUTURE'/'OPEN'/'SOFT_CLOSED'/'OVERDUE'/'HARD_CLOSED'), `hard_closed_at`, `soft_closed_at`, `grace_expires_at`, `reopened_count INT DEFAULT 0`, UQ:(tenant_id,fiscal_year,period_no) | |
| `period_grace_overrides` | `id PK`, `tenant_id FK→tenants CASCADE`, `module`, `applies_to_type`, `applies_to_role`, `applies_to_user_id`, `period_type`, `grace_value INT`, `grace_unit`, `is_default BOOL` | |
| `future_posting_exceptions` | `id PK`, `tenant_id FK→tenants CASCADE`, `created_by UUID`, `target_date DATE`, `module`, `reason TEXT` | |
| `close_checklist_items` | `id PK`, `tenant_id FK→tenants CASCADE`, `label`, `description TEXT`, `applies_to` ('every_close'/'year_end_only'), `sort_order INT`, `is_active BOOL` | |
| `period_checklist_completions` | `id PK`, `tenant_id FK→tenants CASCADE`, `period_id FK→accounting_periods CASCADE`, `checklist_item_id FK→close_checklist_items NO ACTION`, `item_label_snapshot`, `prepared_by UUID`, `prepared_at`, `approved_by UUID`, `approved_at`, `status` ('pending'/'prepared'/'approved'), UQ:(period_id,checklist_item_id) | approved_by ≠ prepared_by enforced in router |
| `fiscal_year_states` | `id PK`, `tenant_id FK→tenants CASCADE`, `fiscal_year`, `status` ('OPEN'/'AUDIT_PENDING'/'AUDIT_OVERDUE'/'STATUTORY_CLOSED'), `management_closed_at`, `audit_grace_months INT DEFAULT 3`, `audit_grace_expires_at`, `statutory_closed_at`, `retained_earnings_rolled BOOL DEFAULT FALSE`, UQ:(tenant_id,fiscal_year) | STATUTORY_CLOSED = permanent lock |
| `period_audit_logs` | `id PK`, `tenant_id FK→tenants CASCADE`, `fiscal_year`, `period_id UUID NULL`, `action` ('REOPEN'/'MANAGEMENT_CLOSE'/'STATUTORY_CLOSE'/'HARD_CLOSE'), `actor_id UUID`, `detail TEXT` | Append-only |

### 2.4 CoA & Dimensions Tables

| Table | Key columns | Notes |
|---|---|---|
| `tenant_dimensions` | `id PK`, `tenant_id FK→tenants CASCADE`, `name`, `code`, `is_required BOOL`, `is_active BOOL`, `sort_order INT`, `accepted_value_types`, `locked_by_implementation BOOL`, `value_source`, `dimension_sources JSONB`, `display_name`, `description`, `icon` | |
| `dimension_values` | `id PK`, `tenant_id FK→tenants CASCADE`, `dimension_id FK→tenant_dimensions CASCADE`, `code`, `name`, `is_active BOOL`, `sort_order INT`, `value_type`, `cascade_dimension_id FK→tenant_dimensions SET NULL`, `cascade_value_id FK→dimension_values SET NULL`, `valid_from DATE`, `valid_to DATE` | ⚠️ May have cost_center-typed rows (legacy), but NOT read for cost center lookup (org_structure is used). |
| `chart_of_accounts` | `id PK`, `tenant_id FK→tenants CASCADE`, `gl_number VARCHAR(50)`, `gl_name`, `account_type` ('PL'/'BS'/'SOCI'/'SOFP'), `is_active BOOL`, `gl_group`, `gl_subgroup`, `gl_sub_subgroup`, `fs_head`, `fs_note`, `tb_mapping`, `group_account_number`, `group_account_name`, `account_classification`, `is_foreign_currency BOOL`, `foreign_currency_code`, `revalue_at_period_end BOOL`, `locked_by_implementation BOOL`, `is_retired BOOL DEFAULT FALSE` | is_retired distinguishes retired (via remap) from plain-deactivated |
| `gl_code_remaps` | `id PK`, `tenant_id FK→tenants CASCADE`, `old_account_id FK→chart_of_accounts NO ACTION`, `new_account_id FK→chart_of_accounts NO ACTION`, `remapped_by FK→users SET NULL`, `remapped_at`, `reason` | NO CASCADE — retired accounts never deleted |
| `gl_dimension_requirements` | `id PK`, `tenant_id FK→tenants CASCADE`, `gl_id FK→chart_of_accounts CASCADE`, `dimension_id FK→tenant_dimensions CASCADE`, `requirement` ('required'/'optional'/'na') | |
| `category_gl_mappings` | `id PK`, `tenant_id FK→tenants CASCADE`, `category_id FK→expense_categories CASCADE`, `gl_id FK→chart_of_accounts CASCADE`, `is_default BOOL` | One is_default per category, enforced in router |

### 2.5 Employee Tables

| Table | Key columns | Notes |
|---|---|---|
| `employees` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_code NULL`, `first_name`, `last_name`, `other_name`, `preferred_name`, `email`, `phone`, **`cost_center_id FK→org_structure SET NULL`**, **`line_manager_id FK→employees SET NULL`**, `resumption_date DATE`, `is_active BOOL` | cost_center_id → org_structure.id (NOT dimension_values) |
| `employee_code_history` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `old_code`, `new_code`, `change_type` ('retrospective'/'progressive'), `effective_date DATE`, `changed_by FK→users SET NULL` | |
| `employee_transfers` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `from_cost_center_id FK→org_structure SET NULL`, `to_cost_center_id FK→org_structure SET NULL`, `effective_date DATE`, `transferred_by FK→users SET NULL` | |
| `cost_center_config` | `id PK`, `tenant_id FK→tenants CASCADE`, **`cost_center_id FK→org_structure CASCADE`**, `head_employee_id FK→employees SET NULL`, `head_user_id FK→users SET NULL`, UQ:(tenant_id,cost_center_id) | cost_center_id → org_structure.id |
| `finance_review_config` | `id PK`, `tenant_id FK→tenants CASCADE`, `module` ('expense_retirement'/'accounts_payable'), `reviewer_user_id FK→users CASCADE`, `review_level INT DEFAULT 1`, `cost_center_id FK→org_structure SET NULL` | |

### 2.6 Expense Tables

| Table | Key columns | Notes |
|---|---|---|
| `expense_reports` | `id PK`, `tenant_id FK→tenants CASCADE`, `report_number` (EXP-YYYY-NNNN, indexed, NOT uniquely constrained), `employee_id FK→users CASCADE`, `employee_code`, `employee_function`, `report_date DATE`, `status` ('DRAFT'/'SUBMITTED'/'PENDING_APPROVAL'/'APPROVED'/'REJECTED'/'REFERRED_TO_REQUESTOR'), `currency CHAR(3)`, `total_amount NUMERIC(15,2)`, `submitted_at`, `current_approval_level INT`, `rejection_comment TEXT`, `rejected_at_level INT`, `referred_back_from_level INT`, `referred_back_levels JSONB` | ⚠️ report_number has no UNIQUE constraint — race-condition duplicates possible |
| `expense_lines` | `id PK`, `report_id FK→expense_reports CASCADE`, `line_number INT`, `pl_group`, `gl_account VARCHAR(255) NULL` (legacy), `io_dimension`, `cost_center`, `location`, `invoice_date DATE`, `invoice_number`, `description`, `amount NUMERIC(15,2)`, `category_id FK→expense_categories SET NULL`, `subcategory_id FK→expense_categories SET NULL`, **`gl_id FK→chart_of_accounts SET NULL`** (M9), `dimension_values JSONB`, `is_split_parent BOOL DEFAULT FALSE`, `split_parent_id FK→expense_lines SET NULL`, `flag_incorrect BOOL`, `flag_comment TEXT` | |
| `expense_report_snapshots` | `id PK`, `report_id FK→expense_reports CASCADE`, `tenant_id FK→tenants CASCADE`, `snapshot_data JSONB`, `submitted_at`, `version INT DEFAULT 1` | Immutable. ⚠️ snapshot_data does NOT include M9 fields (gl_id, dimension_values, split data) |
| `tenant_expense_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `coding_level INT DEFAULT 0` (0-4), `require_category BOOL`, `require_subcategory BOOL`, `allow_free_text_description BOOL DEFAULT TRUE`, `show_location BOOL DEFAULT TRUE`, `require_location BOOL DEFAULT FALSE`, `locked_by_implementation BOOL` | |
| `expense_categories` | `id PK`, `tenant_id FK→tenants CASCADE`, `name`, `code VARCHAR(100)`, `parent_id FK→expense_categories SET NULL`, `is_active BOOL`, `sort_order INT` | Two-level tree |

### 2.7 Approval Tables

| Table | Key columns | Notes |
|---|---|---|
| `approval_matrix` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `levels INT`, `level1_role`, `level2_role NULL`, `level3_role NULL`, `amount_threshold_l2 NUMERIC(15,2) NULL`, `amount_threshold_l3 NUMERIC(15,2) NULL` | |
| `expense_approvals` | `id PK`, `report_id FK→expense_reports CASCADE`, `tenant_id FK→tenants CASCADE`, `level INT`, `approver_id FK→users CASCADE`, `status` ('PENDING'/'APPROVED'/'REJECTED'/'REFERRED_BACK'), `comment TEXT`, `visible_to_requestor BOOL`, `response_comment TEXT`, `actioned_at` | |

### 2.8 GL / Journal Tables

| Table | Key columns | Notes |
|---|---|---|
| `journal_entries` | `id PK`, `tenant_id FK→tenants CASCADE`, `entry_date DATE`, `description`, `source`, `reference_number` (JE-YYYY-NNNNNN), `status` ('DRAFT'/'POSTED'/'REVERSED'), `reverses_entry_id FK→journal_entries SET NULL`, `reversed_by_entry_id FK→journal_entries SET NULL`, `created_by FK→users SET NULL`, `posted_at`, UQ:(tenant_id,reference_number) | POSTED = immutable |
| `journal_lines` | `id PK`, `tenant_id FK→tenants CASCADE`, `journal_entry_id FK→journal_entries CASCADE`, `gl_account_id FK→chart_of_accounts NO CASCADE`, `debit NUMERIC(18,2)`, `credit NUMERIC(18,2)`, `description`, `line_number INT`, `dimensions JSONB`, `bank_account_id FK→bank_accounts SET NULL` | Invariants: one of debit/credit > 0, Σdebit == Σcredit (app-enforced) |

### 2.9 Account Mapping & Bank Tables

| Table | Key columns | Notes |
|---|---|---|
| `posting_roles` | `role_key VARCHAR(60) PK`, `label`, `statement` ('BS'/'PL'), `group`, `subgroup NULL`, `display_order INT`, `expected_account_type`, `is_control_account BOOL`, `description TEXT` | System catalogue |
| `tenant_account_mappings` | `id PK`, `tenant_id FK→tenants CASCADE`, `role_key FK→posting_roles CASCADE`, `gl_account_id FK→chart_of_accounts NO CASCADE`, `created_by FK→users SET NULL`, UQ:(tenant_id,role_key) | |
| `tenant_posting_role_settings` | `id PK`, `tenant_id FK→tenants CASCADE`, `role_key FK→posting_roles CASCADE`, `is_control_account_override BOOL NULL`, `is_relevant BOOL NULL`, UQ:(tenant_id,role_key) | NULL = use catalogue default; is_relevant is cosmetic only |
| `bank_accounts` | `id PK`, `tenant_id FK→tenants CASCADE`, `bank_name`, `account_name`, `account_number`, `currency CHAR(3)`, `gl_account_id FK→chart_of_accounts NO CASCADE`, `is_default BOOL`, `is_active BOOL`, `created_by FK→users SET NULL` | At most one is_default per (tenant_id, currency) — app-enforced |

### 2.10 Documents Table

| Table | Key columns | Notes |
|---|---|---|
| `expense_documents` | `id PK`, `tenant_id FK→tenants CASCADE`, `report_id FK→expense_reports CASCADE`, `line_id FK→expense_lines SET NULL` (NULL = report-level), `uploaded_by FK→users CASCADE`, `file_name`, `file_size INT`, `mime_type`, `storage_path VARCHAR(1000)` | Actual bytes in Supabase bucket 'documents' |

---

## 3. Architectural Invariants

| Invariant | Detail |
|---|---|
| **Cost center source of truth** | Cost centers live in `org_structure` (node_type='Cost center'). `employees.cost_center_id` and `cost_center_config.cost_center_id` both FK to `org_structure.id`. `dimension_values` may have cost_center-typed rows (legacy) but are NOT read for primary cost center lookup. |
| **Currency source of truth** | `tenant_org_config.functional_currency`, `.reporting_currency`, `.enabled_currencies JSONB` are the single authority. `tenant_fx_config` holds ONLY `fx_rates` and `revaluation_rules`. GET /api/setup/currencies reads both. |
| **Standing date floor** | No transaction date may precede `tenant_org_config.date_of_registration`. |
| **lifecycle_status gates** | CoA Replace All only when `lifecycle_status='in_implementation'`. CoA Remap only when `lifecycle_status='live'`. |
| **Expense→GL posting** | Synchronous, same DB transaction as final approval. Uncoded leaf lines (`gl_id=NULL`) block posting. Split parent containers (`is_split_parent=True`) are skipped. |
| **GL entries immutable** | Once `status='POSTED'`, journal_entry rows may not be modified. Corrections require reversing entries. |
| **CoA retired accounts** | `is_retired=True` set on remap. These rows remain in DB for historical integrity and cannot be deleted. |
| **Segregation of duties** | Period checklist: `approved_by ≠ prepared_by` enforced in router. |

---

## 4. API Endpoint Inventory

All endpoints require JWT auth except: `/api/auth/*`, `/api/invitations/*`, `/onboard/{token}`.

### 4.1 Auth (`/api/auth`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/auth/signup | Register individual or business account |
| POST | /api/auth/login | Email + password login |
| POST | /api/auth/refresh-token | Rotate refresh token |
| POST | /api/auth/logout | Revoke refresh token, end session |

### 4.2 Users (`/api/users`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/users/me | Current user profile |
| PATCH | /api/users/me | Update own profile |
| PATCH | /api/users/me/password | Change own password |
| GET | /api/users/tenant | List tenant users (approver dropdowns) |
| GET | /api/users/me/sessions | List active sessions |
| DELETE | /api/users/me/sessions/{session_id} | Revoke specific session |
| POST | /api/users/me/sessions/revoke-others | Sign out everywhere else |
| POST | /api/users/me/2fa/enroll | Generate TOTP secret |
| POST | /api/users/me/2fa/verify | Confirm code → enable 2FA |
| POST | /api/users/me/2fa/disable | Disable 2FA |

### 4.3 Expense Reports (`/api/expenses`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/expenses/reports | Create DRAFT report |
| GET | /api/expenses/reports | List reports |
| GET | /api/expenses/reports/{report_id} | Single report with lines |
| POST | /api/expenses/reports/{report_id}/lines | Add line to DRAFT |
| DELETE | /api/expenses/reports/{report_id}/lines/{line_id} | Remove line |
| PATCH | /api/expenses/reports/{report_id}/lines/{line_id} | Update line (auto-save) |
| PATCH | /api/expenses/reports/{report_id} | Update DRAFT header |
| POST | /api/expenses/reports/{report_id}/submit | Submit (legacy path) |

### 4.4 Approvals (`/api/approvals`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/approvals/matrix | Create or upsert approval matrix (admin) |
| GET | /api/approvals/matrix | Get current tenant matrix |
| POST | /api/approvals/reports/{report_id}/submit | Submit report with approvers selected |
| GET | /api/approvals/queue | Reports pending current user's approval |
| GET | /api/approvals/rejected | Reports rejected involving current user |
| GET | /api/approvals/reports/{report_id}/audit-log | Event trail (admin only) |
| GET | /api/approvals/reports/{report_id}/snapshot/{version} | Immutable submission snapshot |
| GET | /api/approvals/reports/{report_id} | All approval records |
| POST | /api/approvals/{approval_id}/approve | Approve; final approval triggers GL posting |
| POST | /api/approvals/{approval_id}/reject | Reject with required comment |
| POST | /api/approvals/{approval_id}/refer-back | Refer to lower approver or requestor |

### 4.5 Tenant Management (`/api/tenant`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/tenant/users | List all users in tenant |
| GET | /api/tenant/users/{user_id} | Get single user detail |
| PATCH | /api/tenant/users/{user_id}/roles | Replace user roles |
| PATCH | /api/tenant/users/{user_id}/deactivate | Soft-deactivate user |
| PATCH | /api/tenant/users/{user_id}/reactivate | Reactivate user |
| POST | /api/tenant/invitations | Send invitation ⚠️ SMTP stub |
| GET | /api/tenant/invitations | List invitations |
| DELETE | /api/tenant/invitations/{invite_id} | Cancel pending invitation |
| POST | /api/tenant/create-test-environment | Create/retrieve shadow test tenant |
| POST | /api/tenant/promote | Copy config test → live (promotion_engine) |
| POST | /api/tenant/purge-test-data | **STUB — no-op** |

### 4.6 Invitations (`/api/invitations`) — Public
| Method | Path | Purpose |
|---|---|---|
| GET | /api/invitations/validate/{token} | Validate token |
| POST | /api/invitations/accept/{token} | Create account + auto-login |

### 4.7 Documents (`/api/documents`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/documents/reports/{report_id}/upload | Upload file (max 10MB) |
| GET | /api/documents/reports/{report_id} | List docs with signed URLs |
| DELETE | /api/documents/{document_id} | Delete (DRAFT/REJECTED only) |

### 4.8 Expense Config (`/api/expense-config`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/expense-config | Get coding config |
| POST | /api/expense-config | Upsert coding config (admin) |
| GET | /api/expense-config/categories | Category tree |
| POST | /api/expense-config/categories | Create category (admin) |
| PATCH | /api/expense-config/categories/{id} | Update category (admin) |
| DELETE | /api/expense-config/categories/{id} | Soft-delete + subcategories (admin) |
| GET | /api/expense-config/form-config | Combined config + dims + categories for form |
| POST | /api/expense-config/reports/{report_id}/gl-codes | Finance GL coding batch update |

### 4.9 Master Data Config (`/api/config`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/config/dimensions | List dimensions |
| GET | /api/config/dimensions/org-structure-preview | Cost center nodes |
| POST | /api/config/dimensions | Create dimension (admin) |
| GET | /api/config/dimensions/{id}/inline-values | Merged values from all sources |
| PATCH | /api/config/dimensions/{id} | Update dimension (admin) |
| DELETE | /api/config/dimensions/{id} | Soft delete (admin) |
| PATCH | /api/config/dimensions/{id}/reactivate | Reactivate (admin) |
| DELETE | /api/config/dimensions/{id}/permanent | Hard delete (admin) |
| POST | /api/config/dimensions/{id}/reorder | Update sort_order (admin) |
| GET | /api/config/dimensions/{id}/values | List values |
| POST | /api/config/dimensions/{id}/values | Add value (admin) |
| PATCH | /api/config/dimensions/{id}/values/{vid} | Update value (admin) |
| DELETE | /api/config/dimensions/{id}/values/{vid} | Soft delete value (admin) |
| POST | /api/config/dimensions/{id}/values/upload | Bulk upload (admin) |
| GET | /api/config/coa | List GL accounts (paginated, searchable) |
| POST | /api/config/coa | Create GL account (admin) |
| PATCH | /api/config/coa/{id} | Update GL account (admin) |
| DELETE | /api/config/coa/{id} | Soft delete (admin) |
| GET | /api/config/coa/template | Download xlsx template |
| POST | /api/config/coa/upload | Bulk upload (admin) |
| PATCH | /api/config/coa/{id}/dimensions | Set dimension requirements (admin) |
| POST | /api/config/coa/replace-all | Deactivate all + reimport (**in_implementation** only) |
| POST | /api/config/coa/remap | Retire old codes → new code (**live** only) |
| GET | /api/config/categories | Category tree with GL mappings |
| POST | /api/config/categories | Create category (admin) |
| PATCH | /api/config/categories/{id} | Update (admin) |
| DELETE | /api/config/categories/{id} | Soft delete (admin) |
| POST | /api/config/categories/{id}/gl-mappings | Add GL to subcategory (admin) |
| DELETE | /api/config/categories/{id}/gl-mappings/{gl_id} | Remove GL mapping (admin) |
| PATCH | /api/config/categories/{id}/gl-mappings/{gl_id} | Set/unset default GL (admin) |
| GET | /api/config/gl/search | Search GL accounts (?q=, ?limit=) |

### 4.10 HR (`/api/hr`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/hr/employees | List employees (paginated) |
| POST | /api/hr/employees | Create employee (admin) |
| PATCH | /api/hr/employees/{id} | Update employee (admin) |
| DELETE | /api/hr/employees/{id} | Soft delete (admin) |
| POST | /api/hr/employees/upload | Bulk upload (admin) |
| GET | /api/hr/employees/template | Download template |
| POST | /api/hr/employees/{id}/transfer | Transfer cost center (admin) |
| POST | /api/hr/employees/{id}/update-code | Change employee code (admin) |
| GET | /api/hr/employees/{id}/history | Code + transfer history |
| POST | /api/hr/employees/invite | Send self-onboarding invite (admin) ⚠️ prints link to stdout |
| POST | /api/hr/employees/{id}/approve-onboarding | Approve onboarding (admin) |
| POST | /api/hr/employees/{id}/reject-onboarding | Reject onboarding (admin) |
| GET | /api/hr/cost-centers | List cost centers with head assignments |
| PUT | /api/hr/cost-centers/{id}/head | Set cost center head (admin) |
| GET | /api/hr/finance-review | List finance reviewers |
| POST | /api/hr/finance-review | Add reviewer (admin) |
| PATCH | /api/hr/finance-review/{id} | Update reviewer (admin) |
| DELETE | /api/hr/finance-review/{id} | Remove reviewer (admin) |
| GET | /onboard/{token} | Validate token (public) |
| POST | /onboard/{token} | Submit self-onboarding (public) |

### 4.11 Setup / Implementation Portal (`/api/setup`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/setup/progress | Setup dashboard completion |
| GET/PATCH | /api/setup/org | Org config |
| GET/POST/PATCH/DELETE | /api/setup/org-structure[/{id}] | Org tree CRUD |
| GET | /api/setup/org-structure/template | Download xlsx template |
| POST | /api/setup/org-structure/upload | Upload from xlsx |
| POST | /api/setup/periods/generate | Generate 12 monthly periods for a FY |
| GET | /api/setup/periods | List periods |
| GET | /api/setup/periods/check | Postability check (?date=) |
| POST | /api/setup/periods/{id}/soft-close | Soft-close period |
| POST | /api/setup/periods/{id}/hard-close | Hard-close (sequential enforcement) |
| POST | /api/setup/periods/{id}/reopen | Reopen (**super_admin** only + audit log) |
| POST | /api/setup/periods/management-close | Stage 1 year-end close |
| POST | /api/setup/periods/statutory-close | Stage 2 permanent year lock |
| GET | /api/setup/periods/year-state | FiscalYearState |
| PATCH | /api/setup/periods/year-state/{fy} | Update audit grace months |
| GET | /api/setup/periods/audit-log | Period audit log |
| GET/POST/PATCH/DELETE | /api/setup/periods/checklist[/{id}] | Close checklist template CRUD |
| GET | /api/setup/periods/{id}/checklist | Per-period checklist with completion |
| POST | /api/setup/periods/{id}/checklist/{item_id}/prepare | Mark prepared |
| POST | /api/setup/periods/{id}/checklist/{item_id}/approve | Mark approved (SOD enforced) |
| GET/PATCH | /api/setup/modules | Module activation |
| POST | /api/setup/dimensions/not-applicable | Mark not using dimensions |
| GET/PATCH | /api/setup/currencies | FX config |
| GET/PATCH | /api/setup/tax | Tax config |
| GET/PATCH | /api/setup/roles/matrix | Permission matrix |
| GET/POST/PATCH | /api/setup/roles/assignments[/{id}] | Role tier assignments |
| GET/POST/PATCH/DELETE | /api/setup/documents[/{id}] | Document rules |
| POST | /api/setup/go-live | Mark tenant live (**super_admin** only) |

### 4.12 Account Mapping (`/api/setup/account-mapping`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/setup/account-mapping/roles | Full posting role catalogue + tenant mapping |
| PUT | /api/setup/account-mapping/{role_key} | Upsert GL mapping |
| DELETE | /api/setup/account-mapping/{role_key} | Remove GL mapping |
| PUT | /api/setup/account-mapping/{role_key}/control | Per-tenant control-account override (**super_admin** only) |

### 4.13 Bank Accounts (`/api/setup/bank-accounts`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/setup/bank-accounts | List bank accounts |
| POST | /api/setup/bank-accounts | Create (GL must be BS/SOFP) |
| PUT | /api/setup/bank-accounts/{id} | Update |
| DELETE | /api/setup/bank-accounts/{id} | Hard/soft delete based on journal line references |

### 4.14 GL Reporting (`/api/gl`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/gl/trial-balance | Trial balance (?date_from, ?date_to, ?include_zero) |
| GET | /api/gl/accounts/{gl_account_id}/ledger | Account ledger (?date_from, ?date_to, ?dimension filters) |

### 4.15 Platform / Owner Portal (`/api/platform`) — Super Admin Only
| Method | Path | Purpose |
|---|---|---|
| GET | /api/platform/tenants | List tenants (live, non-shadow by default) |
| GET | /api/platform/tenants/{tenant_id} | Tenant detail + users + modules |
| PATCH | /api/platform/tenants/{tenant_id}/lifecycle | Transition lifecycle state |
| POST | /api/platform/tenants/{tenant_id}/suspend | Suspend tenant |
| POST | /api/platform/tenants/{tenant_id}/reactivate | Restore prior lifecycle |
| POST | /api/platform/tenants/{tenant_id}/enter | Mint impersonation token (implementation or support mode) |
| GET | /api/platform/tenants/{tenant_id}/promotion-diff | Compute test→live diff |
| POST | /api/platform/tenants/{tenant_id}/promotion-apply | Apply promotion |

### 4.16 Health
| Method | Path | Purpose |
|---|---|---|
| GET | /api/health | DB connectivity check (public) |

---

## 5. Frontend Page Map

| Route | Purpose |
|---|---|
| `/` | Root → redirects to login |
| `/auth/login` | Login page |
| `/auth/signup` | Business signup with geolocation + currency detection |
| `/invite/accept` | Invitation accept |
| `/onboard/[token]` | New hire self-onboarding (public) |
| `/dashboard/business` | Main dashboard → redirects to setup |
| `/dashboard/business/setup` | Implementation portal dashboard |
| `/dashboard/business/setup/organisation` | Org identity + structure tree + fiscal year |
| `/dashboard/business/setup/modules` | Module activation |
| `/dashboard/business/setup/modules/[module]` | Module detail |
| `/dashboard/business/setup/periods` | Period management |
| `/dashboard/business/setup/currencies` | Currencies & FX (4 tabs; UI complete, backend JSONB-based) |
| `/dashboard/business/setup/tax` | Tax & statutory config |
| `/dashboard/business/setup/roles` | Roles & permissions |
| `/dashboard/business/setup/documents` | Document rules |
| `/dashboard/business/setup/account-mapping` | Posting role → GL mapping |
| `/dashboard/business/setup/bank-accounts` | Bank accounts register |
| `/dashboard/business/setup/go-live` | Go-live readiness |
| `/dashboard/business/expenses` | Expense report list |
| `/dashboard/business/expenses/new` | Create expense report |
| `/dashboard/business/expenses/[report_id]` | Report detail view |
| `/dashboard/business/expenses/[report_id]/edit` | Edit DRAFT (full M9 form) |
| `/dashboard/business/expenses/[report_id]/audit` | Audit trail |
| `/dashboard/business/expenses/[report_id]/snapshot/[version]` | Immutable snapshot |
| `/dashboard/business/approvals` | Approval queue |
| `/dashboard/business/admin/users` | User management |
| `/dashboard/business/settings/dimensions` | Dimension setup |
| `/dashboard/business/settings/dimensions/[id]/values` | Dimension values |
| `/dashboard/business/settings/chart-of-accounts` | CoA management |
| `/dashboard/business/settings/employees` | Employee management |
| `/dashboard/business/settings/cost-centers` | Cost center heads |
| `/dashboard/business/settings/expense-categories` | Expense categories |
| `/dashboard/business/settings/expense-config` | Expense coding config |
| `/dashboard/business/settings/finance-review` | Finance reviewer config |
| `/dashboard/business/settings/approval-matrix` | Approval matrix |
| `/dashboard/personal` | Personal dashboard (individual accounts) |
| `/dashboard/profile` | User profile |
| `/platform` | Owner portal landing |
| `/platform/tenants` | Tenant list |
| `/platform/tenants/[id]` | Tenant detail |
| `/platform/audit` | **Stub** |
| `/platform/billing` | **Stub** |
| `/platform/settings` | **Stub** |
| `/platform/support` | **Stub** |
| `/platform/team` | **Stub** |
| `/platform/trials` | **Stub** |

---

## 6. Feature / Milestone Status

| Feature | Status | Notes |
|---|---|---|
| Auth (signup/login/JWT/2FA/sessions) | ✅ Working | |
| Tenant invite & user management | ✅ Working | Email stub — only sends if SMTP vars configured |
| Expense submit → approve → GL posting | ✅ Working (non-split) | ⚠️ Split-line reports cannot be posted — see Issue #001 |
| Approval matrix (1–3 levels) | ✅ Working | |
| Refer-back + audit trail + snapshots | ✅ Working | ⚠️ Snapshots missing M9 fields — see Issue #003 |
| Supporting documents (Supabase) | ✅ Working | |
| Chart of Accounts (CRUD, bulk upload, hierarchy) | ✅ Working | |
| CoA remap (retire codes) | ✅ Working | Lifecycle-gated: live only |
| CoA replace-all | ✅ Working | Lifecycle-gated: in_implementation only |
| Dimensions (setup, values, cascade, bulk upload) | ✅ Working | |
| Expense categories + GL mappings | ✅ Working | |
| Expense coding levels 0–4 | ✅ Working | |
| AI GL suggestions | ✅ Working | |
| Employee master data | ✅ Working | cost_center_id → org_structure confirmed |
| Employee self-onboarding | ✅ Working | ⚠️ Invite link printed to stdout (not emailed) |
| Org structure (tree CRUD, upload) | ✅ Working | |
| Implementation portal (all sections) | ✅ Working | |
| Accounting periods (generate, close, grace) | ✅ Working | |
| Period close checklist + SOD | ✅ Working | |
| Fiscal year close (management + statutory) | ✅ Working | |
| Currencies & FX UI | ✅ UI built | 4 tabs complete |
| Currencies & FX backend | ⚠️ JSONB only | JSONB in org_config + fx_config; no dedicated tables yet |
| Tax & statutory | ✅ Working | JSONB storage |
| Module activation (licensed gate) | ✅ Working | |
| Posting roles + GL account mapping | ✅ Working | |
| Bank accounts register | ✅ Working | |
| GL posting engine (journal entries/lines) | ✅ Working | |
| Trial balance + account ledger | ✅ Working | |
| Shadow test environment (clone engine) | ✅ Working | 13-step clone |
| Test→live promotion engine | ✅ Working | Phase 3a diff+apply |
| Owner portal (platform UI) | ✅ Working | Tenant list/detail/lifecycle |
| Consultant impersonation (enter tenant) | ✅ Working | ⚠️ readonly guard incomplete — see Issue #002 |
| role_tier enforcement | ⚠️ Partial | Column + JWT exist; full gate enforcement incomplete |
| Manual journal entry UI | ⚠️ Not started | Backend tables exist; no router endpoint or frontend |
| Self-service trial provisioning | ⚠️ Not started | Roadmap Phase 1 |
| OCR / AI engine | ⚠️ Not started | Roadmap Layer 2 |
| Reporting (FS statements, export) | ⚠️ Not started | Trial balance API exists; no formatted output |
| Accounts Payable | ⚠️ Not started | Module licensed on test shadow |
| Accounts Receivable | ⚠️ Not started | Module licensed on test shadow |

---

## 7. Environment & Config Facts

| Item | Value |
|---|---|
| Local DB name | `ziva_dev` (PostgreSQL on localhost:5432) |
| Backend URL (local dev) | `http://localhost:8000` |
| Frontend URL (local dev) | `http://localhost:3000` |
| API docs | `http://localhost:8000/api/docs` |
| CORS | Always includes localhost:3000, localhost:3001; additional from `ALLOWED_ORIGINS` env var |
| SMTP | Stubbed — sends only if smtp_host/smtp_user/smtp_password configured; silently logs otherwise |
| Supabase Storage | Bucket: `documents` (private); config via SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_BUCKET |
| **Local DB state** | **POPULATED with real data** — 2 tenants (live Red Bull + test shadow), 595 GL accounts each, 40 employees on test shadow, 17 org structure nodes each. DB was NOT wiped. It is fully active. |
| **Alembic version (local)** | **`i5j6k7l8m9n0`** — 18 migrations BEHIND head (`z6a7b8c9d0e1`). The schema has all 52 tables (applied manually outside Alembic tracking). Running `alembic upgrade head` will fail. See Issue #004. |
| Total migrations in repo | 36 files (72a96af → z6a7b8c9d0e1) |

**Test shadow tenant:**
| Field | Value |
|---|---|
| `tenant_id` | `e8a2fd8c-5466-4618-bb37-97681a8bfb05` |
| Name | Red Bull Nigeria Limited (Test) |
| `parent_tenant_id` | `bd2c8a25-7467-494a-96fa-30f40b5b5d19` (live Red Bull) |
| `lifecycle_status` | `in_implementation` |
| Employees | 40 (on test shadow only) |
| Accounting periods | 12 (test shadow); 36 (live) |

**System roles (15):** super_admin, tenant_admin, employee, line_manager, approver, gm, finance_reviewer, finance_poster, finance_manager, internal_auditor, external_auditor, vendor_admin, customer_admin, hr_officer, payroll_officer

---

## 8. Known Issues Register

> **Maintenance rule:** This register is updated every working session. When an issue is resolved, DELETE it from this register (resolved items must not clutter the list). When a new issue is found, ADD it with severity + evidence. Issues that remain unresolved STAY. Each issue carries: ID, severity, short title, evidence (file/line), and date first identified.

---

### CRITICAL

#### #001 — Split-line expense reports cannot be posted to GL

- **Date found:** 2026-06-23
- **Severity:** Critical — data/functional correctness: approved split-line reports will never post
- **Evidence:** `backend/app/routers/expenses.py:98-105` — `_recalculate_total` sums ALL expense lines including `is_split_parent=True` containers. For a split (parent amount=100, child1=60, child2=40) this yields `total_amount=200`. At final approval, `expense_posting.py:144-150` pre-flight check computes `sum_debits=100` (leaf lines only) vs `report_total=200` → raises `ExpensePostingError("sum of line amounts does not equal report total_amount")`. The approval call returns 422 and the report is permanently stuck.
- **Fix:** In `_recalculate_total`, add `.where(ExpenseLine.is_split_parent.is_(False))` to exclude container parents from the sum.

#### #004 — Local alembic_version is 18 migrations behind head; schema applied outside Alembic tracking

- **Date found:** 2026-06-23
- **Severity:** Critical for deployment — running `alembic upgrade head` locally or on Render will fail with "column/table already exists" for all 18 unapplied migrations
- **Evidence:** `ziva_dev` alembic_version = `i5j6k7l8m9n0`; repo head = `z6a7b8c9d0e1` (file `backend/alembic/versions/z6a7b8c9d0e1_profile_sessions_2fa.py`). All 52 tables exist in the DB, meaning schema was applied outside Alembic (likely via direct ORM reflection or manual SQL). `alembic upgrade head` will attempt to re-apply columns already present.
- **Fix:** Run `alembic stamp z6a7b8c9d0e1` to advance the tracking record to head without re-running migrations. Do this on both local DB and Render before any future `alembic upgrade head`.

---

### HIGH

#### #002 — `block_if_readonly_impersonation` not called in most write routers

- **Date found:** 2026-06-23
- **Severity:** High security — support-mode impersonation on a live tenant is supposed to be read-only, but 6 of 9 write routers skip the guard
- **Evidence:** `backend/app/middleware/auth.py:58-73` defines the guard. It is only called in: `setup.py:211`, `setup.py:1356`, `account_mapping.py:71`, `account_mapping.py:79`, `bank_accounts.py:51`. NOT called in: `config.py`, `hr.py`, `approvals.py`, `expenses.py`, `expense_config.py`, `tenant.py`. A super admin in support mode on a live tenant can post journals, modify employees, mutate CoA, and approve expenses.
- **Fix:** Add `block_if_readonly_impersonation(current_user)` to the `_require_admin` helper in each of these routers, or call it directly at the top of every state-mutating endpoint in those files.

#### #003 — Expense report snapshots omit all M9 structured data

- **Date found:** 2026-06-23
- **Severity:** High data integrity — the audit trail is incomplete for every M9-coded report
- **Evidence:** `backend/app/routers/approvals.py:191-218` (`_write_snapshot`) builds `lines_data` from legacy fields only (`gl_account`, `pl_group`, `io_dimension`, `cost_center`, `location`, `invoice_date`, `invoice_number`, `description`, `amount`). Fields `gl_id`, `dimension_values`, `is_split_parent`, `split_parent_id`, `flag_incorrect`, `flag_comment` are not captured. The structured GL code and dimension selections are permanently lost in all snapshots taken since M9.
- **Fix:** Extend the `lines_data` dict in `_write_snapshot` to include `gl_id`, `dimension_values`, `is_split_parent`, `split_parent_id`, `flag_incorrect`, `flag_comment`.

---

### MEDIUM

#### #005 — Report number and GL reference number use COUNT-based sequences (race condition)

- **Date found:** 2026-06-23
- **Severity:** Medium — concurrent report creation or concurrent final approvals can produce duplicate identifiers
- **Evidence:** `backend/app/routers/expenses.py:80-95` (`_generate_report_number`) and `backend/app/services/gl_posting.py:98-112` (`_generate_reference_number`) both use `SELECT COUNT(*) + 1`. Under concurrent requests two callers read the same count and generate the same identifier. `expense_reports.report_number` has no UNIQUE constraint (only an index), so duplicates persist silently. `journal_entries` has a UNIQUE constraint on `(tenant_id, reference_number)` which will raise a 500 integrity error for the approver on collision.
- **Fix:** Replace COUNT-based generation with a PostgreSQL sequence (`CREATE SEQUENCE`) or with a `SERIAL`/`identity` column, or use `SELECT ... FOR UPDATE` on a counters table.

#### #006 — GL posting uses report_date (employee-entered) as the journal entry date

- **Date found:** 2026-06-23
- **Severity:** Medium — reports with dates in closed periods will fail at final-approval time with an unrecoverable 422
- **Evidence:** `backend/app/services/expense_posting.py:154-165` passes `entry_date=report.report_date` to `post_journal`. `gl_posting.py:302-314` runs `is_date_postable` for POSTED entries. If `report_date` falls in a HARD_CLOSED or FUTURE period, the approval call returns 422 ("DATE_NOT_POSTABLE"). The approver cannot resolve this without period reopen (super_admin action). No pre-submission validation warns the employee their date is in a closed period.
- **Fix:** Add a period postability check at submit time (`POST /api/approvals/reports/{id}/submit`) or surface a clear warning in the expense form before the employee selects a report date in a closed period.

---

### LOW

#### #007 — Debug `print()` statements left in production router code

- **Date found:** 2026-06-23
- **Severity:** Low — log noise + potential sensitive data exposure in server output
- **Evidence:**
  - `backend/app/routers/config.py:137` — `print("DEBUG parse_upload called")` (fires on every CoA/dimension upload)
  - `backend/app/routers/config.py:951` — `print("ERROR in download_dimension_values_template:")` + `traceback.print_exc()` (bypasses logger)
  - `backend/app/routers/config.py:1230` — same pattern for universal dimension values template
  - `backend/app/routers/hr.py:1166` — `print(f"[ONBOARDING] Invite for {data.email}: {onboarding_link}")` — exposes the invite URL and employee email in server stdout
  - `backend/app/routers/hr.py:1216` — `print(f"[ONBOARDING] Rejected employee {employee_id}: {comment}")` — exposes rejection comment
- **Fix:** Replace all `print()` calls in router files with `logger.debug()` / `logger.info()` / `logger.error()` using the module-level `logger = logging.getLogger(__name__)` pattern already used elsewhere in the codebase.

#### #008 — `POST /api/tenant/purge-test-data` returns 200 OK for a no-op stub

- **Date found:** 2026-06-23
- **Severity:** Low — misleading success response; callers cannot distinguish stub from real execution
- **Evidence:** `backend/app/routers/tenant.py:408-424` — endpoint returns `{"message": "purge-test-data is a no-op stub..."}` with HTTP 200. Any caller (future automation, frontend) that calls this endpoint and checks for 200 will believe data was purged.
- **Fix:** Return HTTP 501 Not Implemented until the scheduled purge is built.

#### #009 — MASTER_CONTEXT.md §8 API paths are stale (corrected in PROJECT_STATE)

- **Date found:** 2026-06-23
- **Severity:** Low — documentation drift only; no runtime impact
- **Evidence:** MASTER_CONTEXT.md previously listed `/api/config/cost-centers` and `/api/config/finance-review`. Actual routes are `/api/hr/cost-centers` and `/api/hr/finance-review` (verified in `backend/app/routers/hr.py:1-36`). MASTER_CONTEXT.md has been cleaned up (volatile API paths removed); PROJECT_STATE.md §4.10 is authoritative.
- **Fix:** ✅ Resolved in MASTER_CONTEXT.md cleanup (this session).

---

### UNCONFIRMED SUSPICIONS (not yet verified — need deeper investigation before adding as findings)

1. **Promotion engine may not clone org_structure**: `promotion_engine.py` handles 5 entity types (TenantDimension, ChartOfAccount, DimensionValue, GLDimensionRequirement, TenantAccountMapping). The `tenant_clone.py` clone engine includes org_structure in Step 13, but the promotion engine (test→live config copy) does NOT appear to include org_structure. If org_structure changes on test need to be promoted to live, the promotion diff/apply would silently miss them. Need to read full promotion_engine.py to confirm.

2. **`expenses.py GET /reports` visibility bypass**: The role-based visibility filter at `expenses.py:195-215` checks `finance_roles` for non-admin users. If a non-admin user has no roles assigned at all (which is possible — role assignment is optional), the `NOT IN (finance_roles)` path might apply the employee filter correctly. But if a user's roles query fails or returns stale data, all reports might be visible. Low-confidence; need to trace the query under zero-roles condition.

---

## 9. Totals

| Entity | Count |
|---|---|
| Application DB tables | 51 (+ alembic_version system table = 52 total in DB) |
| Alembic migrations in repo | 36 |
| Backend router files | 15 |
| Backend model files | 10 |
| Backend service files | 8 |
| API endpoints | ~160 |
| Frontend pages | ~45 (including stubs) |
