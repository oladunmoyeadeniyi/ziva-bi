> **Standing instruction:** This document is regenerated/updated at the end of each working session before switching chats. It is the authoritative current-state snapshot. If anything here conflicts with memory, a prior summary, or MASTER_CONTEXT.md, THIS document wins (it is verified against the live codebase and live DB on the date shown above).
>
> **At the end of every working session:** refresh the ENTIRE document — structure, schema, endpoints, feature status, AND the Known Issues Register — against the live codebase. Remove resolved issues, keep unresolved ones, add newly-found ones.

---

# PROJECT_STATE.md — Ziva BI Authoritative Codebase Snapshot

**Generated:** 2026-06-28 (refreshed 2026-06-29, 2026-06-30, 2026-07-11 ×5, 2026-07-13, 2026-07-15, 2026-07-20 ×3, 2026-07-21, 2026-07-25 ×4, 2026-07-27, 2026-07-28, 2026-07-29 ×2)  
**Git commit:** Last CC-committed head: `k9l0m1n2o3p4` (M20 AI Intelligence, committed `9ffd9e0`). **PENDING CC COMMIT (2026-07-29):** M17b (FIFO/Standard costing, migration `l0m1n2o3p4q5`) + ICE AI Categorisation Engine (migration `p9q0r1s2t3u4`). PENDING_COMMIT.md ready for `/review-commit`.  
**Alembic migration head (written, pending CC commit):** `p9q0r1s2t3u4` (ICE). Last CC-committed head: `k9l0m1n2o3p4` (M20). Run `alembic upgrade head` after CC commits — applies both pending migrations.  
**Branch:** `main`  
**Method:** Generated by reading actual source files (models, routers, services, migrations) — NOT from memory or prior summaries.

## Summary

Ziva BI is a multi-tenant, enterprise-grade finance automation SaaS platform. Stack: Next.js 15 (App Router) + TailwindCSS frontend, Python 3.12 + FastAPI backend, PostgreSQL via async SQLAlchemy + Alembic, Supabase Storage for files. Auth via JWT (access + refresh tokens) with session tracking, TOTP 2FA, and role-based access. All data is tenant-scoped; the test/live environment model uses shadow tenants linked via `parent_tenant_id` on the `tenants` table. Since M9.0.1 (2026-06-29), signup creates ONLY a test tenant — live is born second, via an explicit super-admin promotion, and `parent_tenant_id` runs test→live (the inverse of the original live-first design; see §3). The platform supports a full implementation lifecycle: Trial → In Implementation → Live → Suspended.

---

## 1. Repository Structure

```
ziva-bi/
├── backend/
│   ├── alembic/versions/         — 80 migration files (last CC head: `d2e3f4g5h6i7`; 7 more written pending commit, head `k9l0m1n2o3p4`)
│   ├── app/
│   │   ├── main.py               — FastAPI app boot, CORS, system role seeding, router registration
│   │   ├── config.py             — pydantic-settings: DATABASE_URL, ALLOWED_ORIGINS, RESEND_API_KEY + EMAIL_FROM, Supabase keys
│   │   ├── database.py           — async SQLAlchemy engine + AsyncSession factory
│   │   ├── core/security.py      — JWT create/verify, bcrypt hash, refresh token generation
│   │   ├── middleware/auth.py    — require_auth, require_super_admin, block_if_readonly_impersonation
│   │   ├── models/
│   │   │   ├── auth.py           — tenants (+ company_size, interested_modules), users, user_tenants
│   │   │   │                       (+ user_type, must_change_password), roles, permissions,
│   │   │   │                       role_permissions, user_roles, sessions, refresh_tokens,
│   │   │   │                       audit_logs, impersonation_sessions (nullable FKs — migration m1n2o3p4q5r6)
│   │   │   ├── setup.py          — implementation_locks, tenant_modules, document_rules,
│   │   │   │                       tenant_tax_config, tenant_fx_config, tenant_org_config,
│   │   │   │                       org_structure, accounting_periods, period_grace_overrides,
│   │   │   │                       future_posting_exceptions, close_checklist_items,
│   │   │   │                       period_checklist_completions, fiscal_year_states,
│   │   │   │                       period_audit_logs, employee_onboarding_tokens
│   │   │   ├── master_data.py    — tenant_dimensions, dimension_values, chart_of_accounts,
│   │   │   │                       gl_code_remaps, gl_dimension_requirements, category_gl_mappings,
│   │   │   │                       employees (+ user_id FK), employee_code_history, employee_transfers,
│   │   │   │                       employee_position_assignments, cost_center_config,
│   │   │   │                       finance_review_config, finance_review_steps,
│   │   │   │                       system_function_mappings, approval_roles (+ code/grade/area/sub_area)
│   │   │   ├── expenses.py       — expense_reports, expense_lines, expense_report_snapshots,
│   │   │   │                       tenant_expense_config, expense_categories
│   │   │   ├── approvals.py      — approval_matrix, expense_approvals, approval_roles
│   │   │   ├── gl.py             — journal_entries, journal_lines
│   │   │   ├── documents.py      — expense_documents
│   │   │   ├── bank_account.py   — bank_accounts
│   │   │   ├── account_mapping.py — posting_roles, tenant_account_mappings, tenant_posting_role_settings
│   │   │   ├── ar.py             — customers, ar_invoices, ar_invoice_lines, ar_approvals, ar_invoice_snapshots
│   │   │   ├── tenant_management.py — tenant_invitations
│   │   │   ├── budget.py         — BudgetPeriod, BudgetLine (M16, pending CC commit)
│   │   │   ├── billing.py        — PricingPlan, TenantSubscription, BillingEvent (SA-B, pending CC commit)
│   │   │   ├── tax_engine.py     — TaxReturn, WhtCertificate (M19, pending CC commit)
│   │   │   ├── payroll.py        — SalaryStructure, PayrollRun, PayrollLine, Payslip, LeaveType, LeaveRequest, LeaveBalance (M15, pending CC commit)
│   │   │   ├── fixed_assets.py   — AssetCategory, Asset, AssetDepreciationSchedule, AssetDisposal (M18, pending CC commit)
│   │   │   ├── inventory.py      — InventoryCategory, InventoryLocation, InventoryItem, StockMovement; WACC engine; COGS GL posting (M17, pending CC commit)
│   │   │   └── ai.py             — EXTENDED: AiInsight (M20); existing: ai_predictions, ai_learning_overrides (pending CC commit)
│   │   ├── routers/
│   │   │   ├── auth.py           — signup, login, refresh-token, logout. Since 2026-07-11: creates `lifecycle_status='trial'` (not 'in_implementation') + `suppress_outbound_email=True`. Test-first since M9.0.1 (see §3): no clone at signup.
│   │   │   ├── users.py          — profile, password, sessions, 2FA
│   │   │   ├── expenses.py       — expense report CRUD + submit
│   │   │   ├── approvals.py      — matrix config, submit, queue, approve/reject/refer-back; approval_roles CRUD (code/grade/area/sub_area/occupants)
│   │   │   ├── tenant.py         — user management, invitations, test-environment, promote
│   │   │   ├── invitations.py    — public invite validate/accept
│   │   │   ├── documents.py      — file upload/list/delete (Supabase Storage)
│   │   │   ├── expense_config.py — coding level config, categories, form-config, GL coding
│   │   │   ├── config.py         — dimensions, dimension values, CoA, categories, GL search
│   │   │   ├── hr.py             — employees (+ cascade user link), cost-center heads, finance reviewers, finance_review_steps, positions (queries approval_roles), employee_position_assignments
│   │   │   ├── setup.py          — org, periods, modules, currencies, tax, roles, documents, go-live, system_function_mappings (GET /api/setup/functions/{code}/users)
│   │   │   ├── platform.py       — super-admin: tenant list/detail/lifecycle/suspend/enter; user impersonation (start/end); user_type on TenantUserSummary
│   │   │   ├── gl.py             — trial balance, account ledger
│   │   │   ├── account_mapping.py — posting role → GL mapping
│   │   │   ├── bank_accounts.py  — bank account register CRUD
│   │   │   ├── posting_batches.py — Connected Mode export queue: list/detail/export/mark-synced
│   │   │   ├── ar.py             — AR customers CRUD + invoice lifecycle + aging report + export
│   │   │   ├── budget.py         — /api/budgets/* — period + line CRUD + variance report (M16, pending CC commit)
│   │   │   ├── billing.py        — /api/sa/billing/* — SA-only pricing plans + subscriptions (SA-B, pending CC commit)
│   │   │   ├── tax_engine.py     — /api/tax/* — VAT/WHT/PAYE compute + returns + WHT certs (M19, pending CC commit)
│   │   │   ├── payroll.py        — /api/payroll/* — salary structures + payroll runs + leave (M15, pending CC commit)
│   │   │   ├── fixed_assets.py   — /api/assets/* — asset categories + register + depreciation + disposals (M18, pending CC commit)
│   │   │   ├── inventory.py      — /api/inventory/* — categories + locations + items + movements + valuation (M17, pending CC commit)
│   │   │   └── ai.py             — /api/ai/* EXTENDED: detect-anomalies, spending-patterns, forecast, classify, insights CRUD (M20, pending CC commit)
│   │   ├── schemas/              — Pydantic request/response models (one file per router)
│   │   └── services/
│   │       ├── tenant_clone.py   — Phase 4 live→test clone engine, 13 steps. Since M9.0.1: no longer invoked at signup. Still used on demand by a super admin to create a test shadow for a live tenant that doesn't have one yet (e.g. a legacy/retrofitted tenant).
│   │       ├── expense_posting.py — posts approved expense report to GL (synchronous, same transaction)
│   │       ├── gl_posting.py     — core GL journal validator + post_journal (flush-only; caller commits)
│   │       ├── gl_reporting.py   — trial_balance() + account_ledger() query builders
│   │       ├── periods.py        — period state machine helpers: is_date_postable, grace computation
│   │       ├── promotion_engine.py — Phase 3a: diff + apply config promotion from test → live
│   │       ├── account_determination.py — resolve_account(): posting role_key → GL account ID
│   │       ├── email.py          — async Resend email service (httpx → REST API); helpers: send_invitation/password_reset/live_promotion/onboarding_invite/approval_notification; suppress flag for test tenants
│   │       ├── ar_posting.py     — post_ar_approval() (Full ERP DR AR/CR revenue), post_ar_receipt() (DR bank/CR AR), create_ar_posting_batch() (Connected)
│   │       ├── storage.py        — Supabase Storage upload/delete/signed-URL proxy
│   │       ├── budget_service.py — variance engine: actuals from journal_lines (Full ERP) / posting_batches (Connected) / expense_lines (Lite) (M16, pending CC commit)
│   │       ├── tax_compute_service.py — VAT/WHT/PAYE compute helpers + summary aggregation per return period (M19, pending CC commit)
│   │       └── ai_intelligence.py — detect_anomalies (3σ outlier + AP dup scan), generate_spending_patterns (LLM narrative), forecast_cash_flow (AR/AP net), suggest_category (GL auto-classify); AiIntelligenceError → HTTP 503 (internal names hidden from tenants) (M20, pending CC commit)
│   ├── scripts/
│   │   ├── seed_m7_categories.py — one-off seed script for M7 expense categories
│   │   ├── seed_demo_tenant.py   — idempotent demo seeder for trial tenants (--list-trials, --tenant-slug, --apply)
│   │   ├── cleanup_orphan_employee_usertenant.py — one-time: deactivate UserTenants for employees already deactivated before cascade was wired
│   │   └── purge_test_tenant_users.py — hard-delete deactivated test-tenant user accounts
├── apps/ziva-bi/src/
│   ├── app/                      — Next.js App Router pages (see §5)
│   ├── components/
│   │   ├── AppHeader.tsx         — top navigation bar with environment toggle
│   │   ├── LockedField.tsx       — read-only field with "contact consultant" lock UI
│   │   ├── PromotionReviewDialog.tsx — test→live promotion review modal
│   │   └── expenses/
│   │       ├── ExpenseItemPicker.tsx — GL popup picker (group → subgroup → GL)
│   │       └── SplitLinePanel.tsx — split expense line management
│   ├── contexts/AuthContext.tsx  — JWT auth state, login/logout, token refresh
│   └── lib/
│       ├── api.ts                — typed fetch wrapper with auth headers + body serialization
│       ├── currencies.ts         — ISO 4217 currency list
│       └── utils.ts              — fmtCommaInput, stripCommas, formatMoney, formatNumber, cn (classnames)
└── docs/                         — PRDs, ADRs, briefs, diagnosis docs (markdown only)
```

---

## 2. Database Schema (authoritative — derived from ORM model files + live DB)

**Multi-tenancy mechanism:** Every operational table carries `tenant_id UUID NOT NULL FK → tenants.id ON DELETE CASCADE`. The JWT embeds `tenant_id`; the `require_auth` middleware extracts and enforces it. No query in normal operation can cross tenant boundaries. Test and live are always two distinct `tenants` rows with their own `tenant_id`, so isolation is automatic — no table outside `tenants` itself knows or cares which environment it belongs to (M9.0.1 deliberately did not add an `environment` column anywhere else).

### 2.1 Tenant / Auth Tables

| Table | Key columns | Notes |
|---|---|---|
| `tenants` | `id UUID PK`, `name`, `country CHAR(2)`, `slug UNIQUE`, `is_active`, `environment` ('live'/'test'), `parent_tenant_id UUID FK→tenants SET NULL`, `lifecycle_status` ('trial'/'in_implementation'/'live'/'suspended'), `dimensions_not_applicable`, `documents_setup_complete`, `module_setup_visited JSONB`, `suppress_outbound_email`, `test_data_retention_days`, `pre_suspension_status`, `lead_status VARCHAR(30) DEFAULT 'new'`, `implementation_notes TEXT`, `company_size VARCHAR(50)`, `interested_modules JSONB`, **`is_internal BOOL DEFAULT FALSE`**, **`plan VARCHAR(30) NULL`** (free\|starter\|growth\|enterprise; NULL = free), **`paid_since DATE NULL`** (date of first payment; NULL = not yet paying) — migration `x6y7z8a9b0c1` | `lead_status` + `implementation_notes`: migration `j1k2l3m4n5o6`. `company_size` + `interested_modules`: migration `n2o3p4q5r6s7` — captured at signup for SA lead qualification. | **M9.0.1 direction flip:** the LIVE tenant now has `parent_tenant_id` set, pointing back at the test tenant it was promoted from; the test tenant's `parent_tenant_id` is NULL. (Old live-first model had it the other way — test pointing at live. The existing Red Bull pair predates the flip and still has the old direction pending retrofit; see §7, §8.) |
| `users` | `id UUID PK`, `email UNIQUE`, `full_name`, `first_name`, `account_type` (individual/business), `is_active`, `is_super_admin`, `employee_code`, `department`, `job_title`, `phone`, `totp_secret`, `totp_enabled` | Global identity — one row per person across all tenants |
| `user_tenants` | `id UUID PK`, `user_id FK→users CASCADE`, `tenant_id FK→tenants CASCADE NULL`, `password_hash`, `is_active`, `role_tier` ('consultant'/'power_admin'/'functional_admin'), `user_type VARCHAR(20) DEFAULT 'employee'` ('employee'/'external'), `failed_login_attempts`, `locked_until`, `last_login_at`, `must_change_password BOOL DEFAULT false` | tenant_id NULL = individual. UQ: (user_id, tenant_id); partial index for individual rows. `must_change_password`: set True by SA's Create Company; cleared by `POST /api/auth/change-password`. Migration `o3p4q5r6s7t8`. |
| `roles` | `id UUID PK`, `name`, `description`, `tenant_id FK→tenants CASCADE NULL`, `is_system` | 15 system roles seeded at startup |
| `permissions` | `id UUID PK`, `code UNIQUE`, `description` | Canonical permission codes, seeded at startup |
| `role_permissions` | `id PK`, `role_id FK→roles CASCADE`, `permission_id FK→permissions CASCADE`, UQ:(role_id,permission_id) | |
| `user_roles` | `id PK`, `user_tenant_id FK→user_tenants CASCADE`, `role_id FK→roles CASCADE`, `assigned_at`, `assigned_by_id`, `revoked_at` | |
| `sessions` | `id PK`, `user_tenant_id FK→user_tenants CASCADE`, `ip_address`, `user_agent`, `expires_at`, `ended_at`, `is_active` | |
| `refresh_tokens` | `id PK`, `session_id FK→sessions CASCADE`, `token_hash UNIQUE`, `expires_at`, `used_at`, `replaced_by_id`, `is_revoked` | SHA-256 hash only; token rotation + replay detection |
| `audit_logs` | `id PK`, `event_type`, `user_id FK→users SET NULL`, `tenant_id FK→tenants SET NULL`, `ip_address`, `log_metadata JSONB`, `created_at` | Append-only |
| `tenant_invitations` | `id PK`, `tenant_id FK→tenants CASCADE`, `invited_by FK→users SET NULL`, `email`, `role`, `token UNIQUE`, `status` ('PENDING'/'ACCEPTED'/'EXPIRED'), `expires_at` | 48-hour invite token |
| `password_reset_tokens` | `id UUID PK`, `user_id FK→users CASCADE`, `token_hash VARCHAR(64) UNIQUE` (SHA-256 of raw token), `expires_at TIMESTAMPTZ`, `used_at TIMESTAMPTZ NULL`, `created_at TIMESTAMPTZ` | One-use, 1-hour TTL. Old tokens invalidated before a new one is created. Token is stored hashed; raw token sent in email URL. Migration `w5x6y7z8a9b0`. |

### 2.2 Organisation & Setup Tables

| Table | Key columns | Notes |
|---|---|---|
| `tenant_org_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `legal_name`, `rc_number`, `date_of_registration DATE`, `commencement_date DATE`, **`first_fiscal_year_end DATE NULL`**, `company_type`, `industry`, `tin`, `vat_reg_number`, `country`, `registered_address TEXT`, `operating_address TEXT`, `company_phone`, `company_email`, `website`, `external_auditor`, `group_structure`, `parent_company_name`, **`functional_currency CHAR(3)`**, **`reporting_currency CHAR(3)`**, **`enabled_currencies JSONB`** (sorted ISO list), `authorised_share_capital NUMERIC(20,2)`, `fiscal_year_start_month INT`, `fiscal_year_start_day INT`, `fiscal_year_name_format`, `period_closing_frequency`, `branding JSONB`, `org_configuration JSONB`, `block_journal_into_open_prior BOOL DEFAULT TRUE`, `default_audit_grace_months INT DEFAULT 3` | **SOLE SOURCE OF TRUTH for functional_currency, reporting_currency, enabled_currencies.** functional_currency protected via PROTECTED_ORG_FIELDS. `first_fiscal_year_end`: last day of the company's very first accounting year — when set, backend derives `fiscal_year_start_month`/`day` automatically. Standing date floor: no transaction date may precede `date_of_registration`. `posting_mode VARCHAR(20) DEFAULT 'full_erp'` ✅ — Added by migration `h1i2j3k4l5m6` (committed `f24c2fe`). Values: `'lite' | 'connected' | 'full_erp'`. Set by consultant in SA portal only — tenants never see this. `document_retention_years INT DEFAULT 15` ✅ — SA-settable per-tenant retention floor (platform minimum 15 years; drives `retain_until` on all new document uploads). Added migration `k2l3m4n5o6p7`. |
| `tenant_fx_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `fx_rates JSONB`, `revaluation_rules JSONB` | FX mechanics ONLY — no currency identity here. |
| `tenant_tax_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `vat_config JSONB`, `wht_config JSONB`, `paye_config JSONB`, `other_statutory JSONB` | |
| `tenant_modules` | `id PK`, `tenant_id FK→tenants CASCADE`, `module_key`, `is_active BOOL`, `is_licensed BOOL`, `activated_at`, `activated_by FK→users SET NULL`, UQ:(tenant_id,module_key) | 403 if unlicensed activate attempted |
| `implementation_locks` | `id PK`, `tenant_id FK→tenants CASCADE`, `section`, `locked_at`, `locked_by FK→users SET NULL`, `notes` | |
| `document_rules` | `id PK`, `tenant_id FK→tenants CASCADE`, `module`, `transaction_type`, `document_name`, `is_required`, `track_expiry`, `max_size_mb INT DEFAULT 10`, `allowed_formats TEXT[]`, `max_files INT DEFAULT 0`, `is_active` | |
| `org_structure` | `id PK`, `tenant_id FK→tenants CASCADE`, `parent_id FK→org_structure NULL`, `node_type` ('Legal entity'/'Division / Business unit'/'Department'/'Cost center'), `name`, `code`, `cost_center_code`, `entity_code`, `is_active BOOL`, `sort_order INT`, UQ:(tenant_id,code) | **Sole source of truth for cost centers.** employees.cost_center_id and cost_center_config.cost_center_id both FK here. |
| `employee_onboarding_tokens` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `token UNIQUE`, `expires_at`, `used_at` | |

### 2.3 Period Management Tables

| Table | Key columns | Notes |
|---|---|---|
| `accounting_periods` | `id PK`, `tenant_id FK→tenants CASCADE`, `fiscal_year VARCHAR(20)`, `period_no INT`, `period_name`, `start_date DATE`, `end_date DATE`, `status` ('FUTURE'/'OPEN'/'SOFT_CLOSED'/'OVERDUE'/'HARD_CLOSED'), `hard_closed_at`, `soft_closed_at`, `grace_expires_at`, `reopened_count INT DEFAULT 0`, **UQ:(tenant_id,start_date)** | **Uniqueness changed 2026-06-28** from (tenant_id,fiscal_year,period_no) via migration `k7l8m9n0o1p2` (committed `b3e70e3`, confirmed pushed 2026-06-29). Reason: `fiscal_year` is a *formatted display label* — changing `fiscal_year_name_format`/`fiscal_year_start_month` after periods exist produces a new label that didn't collide under the old constraint, letting auto-generation triggers silently create duplicate period sets for the same months. `start_date` never changes regardless of label/format. Pre-migration cleanup via `backend/scripts/cleanup_duplicate_periods.py` (run once, successful). |
| `period_grace_overrides` | `id PK`, `tenant_id FK→tenants CASCADE`, `module`, `applies_to_type`, `applies_to_role`, `applies_to_user_id`, `period_type`, `grace_value INT`, `grace_unit`, `is_default BOOL` | |
| `future_posting_exceptions` | `id PK`, `tenant_id FK→tenants CASCADE`, `created_by UUID`, `target_date DATE`, `module`, `reason TEXT` | |
| `close_checklist_items` | `id PK`, `tenant_id FK→tenants CASCADE`, `label`, `description TEXT`, `applies_to` ('every_close'/'year_end_only'), `sort_order INT`, `is_active BOOL` | |
| `period_checklist_completions` | `id PK`, `tenant_id FK→tenants CASCADE`, `period_id FK→accounting_periods CASCADE`, `checklist_item_id FK→close_checklist_items NO ACTION`, `item_label_snapshot`, `prepared_by UUID`, `prepared_at`, `approved_by UUID`, `approved_at`, `status` ('pending'/'prepared'/'approved'), UQ:(period_id,checklist_item_id) | approved_by ≠ prepared_by enforced in router |
| `fiscal_year_states` | `id PK`, `tenant_id FK→tenants CASCADE`, `fiscal_year`, `status` ('OPEN'/'AUDIT_PENDING'/'AUDIT_OVERDUE'/'STATUTORY_CLOSED'), `management_closed_at`, `audit_grace_months INT DEFAULT 3`, `audit_grace_expires_at`, `statutory_closed_at`, `retained_earnings_rolled BOOL DEFAULT FALSE`, UQ:(tenant_id,fiscal_year) | STATUTORY_CLOSED = permanent lock |
| `period_audit_logs` | `id PK`, `tenant_id FK→tenants CASCADE`, `fiscal_year`, `period_id UUID NULL`, `action` ('REOPEN'/'MANAGEMENT_CLOSE'/'STATUTORY_CLOSE'/'HARD_CLOSE'), `actor_id UUID`, `detail TEXT` | Append-only |

### 2.4 CoA & Dimensions Tables

| Table | Key columns | Notes |
|---|---|---|
| `tenant_dimensions` | `id PK`, `tenant_id FK→tenants CASCADE`, `name`, `code`, `is_required BOOL`, `is_active BOOL`, `sort_order INT`, `accepted_value_types`, `locked_by_implementation BOOL`, `value_source`, `dimension_sources JSONB`, `display_name`, `description`, `icon` | |
| `dimension_values` | `id PK`, `tenant_id FK→tenants CASCADE`, `dimension_id FK→tenant_dimensions CASCADE`, `code`, `name`, `is_active BOOL`, `sort_order INT`, `value_type`, `cascade_dimension_id FK→tenant_dimensions SET NULL`, `cascade_value_id FK→dimension_values SET NULL`, `valid_from DATE`, `valid_to DATE` | ⚠️ May have cost_center-typed rows (legacy), but NOT read for cost center lookup (org_structure is used). |
| `chart_of_accounts` | `id PK`, `tenant_id FK→tenants CASCADE`, `gl_number VARCHAR(50)`, `gl_name`, `account_type` ('PL'/'BS'/'SOCI'/'SOFP'), `is_active BOOL`, `gl_group`, `gl_subgroup`, `gl_sub_subgroup`, `fs_head`, `fs_note`, `tb_mapping`, `group_account_number`, `group_account_name`, `account_classification`, `is_foreign_currency BOOL`, `foreign_currency_code`, `revalue_at_period_end BOOL`, `locked_by_implementation BOOL`, `is_retired BOOL DEFAULT FALSE` | is_retired distinguishes retired (via remap) from plain-deactivated |
| `gl_code_remaps` | `id PK`, `tenant_id FK→tenants CASCADE`, `old_account_id FK→chart_of_accounts NO ACTION`, `new_account_id FK→chart_of_accounts NO ACTION`, `remapped_by FK→users SET NULL`, `remapped_at`, `reason` | NO CASCADE — retired accounts never deleted |
| `gl_dimension_requirements` | `id PK`, `tenant_id FK→tenants CASCADE`, `gl_id FK→chart_of_accounts CASCADE`, `dimension_id FK→tenant_dimensions CASCADE`, `requirement` ('required'/'optional'/'na') | |
| `category_gl_mappings` | `id PK`, `tenant_id FK→tenants CASCADE`, `category_id FK→expense_categories CASCADE`, `gl_id FK→chart_of_accounts CASCADE`, `is_default BOOL` | One is_default per category, enforced in router |

### 2.5 Employee Tables

| Table | Key columns | Notes |
|---|---|---|
| `employees` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_code NULL`, `first_name`, `last_name`, `other_name`, `preferred_name`, `email`, `phone`, **`cost_center_id FK→org_structure SET NULL`**, **`line_manager_id FK→employees SET NULL`**, `resumption_date DATE`, `is_active BOOL` | cost_center_id → org_structure.id (NOT dimension_values) |
| `employee_code_history` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `old_code`, `new_code`, `change_type` ('retrospective'/'progressive'), `effective_date DATE`, `changed_by FK→users SET NULL` | |
| `employee_transfers` | `id PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `from_cost_center_id FK→org_structure SET NULL`, `to_cost_center_id FK→org_structure SET NULL`, `effective_date DATE`, `transferred_by FK→users SET NULL` | |
| `cost_center_config` | `id PK`, `tenant_id FK→tenants CASCADE`, **`cost_center_id FK→org_structure CASCADE`**, `head_employee_id FK→employees SET NULL`, `head_user_id FK→users SET NULL`, UQ:(tenant_id,cost_center_id) | cost_center_id → org_structure.id |
| `finance_review_config` | `id PK`, `tenant_id FK→tenants CASCADE`, `module` ('expense_retirement'/'accounts_payable'), `reviewer_user_id FK→users CASCADE`, `review_level INT DEFAULT 1`, `cost_center_id FK→org_structure SET NULL` | |

### 2.6 Expense Tables

| Table | Key columns | Notes |
|---|---|---|
| `expense_reports` | `id PK`, `tenant_id FK→tenants CASCADE`, `report_number` (EXP-YYYY-NNNN, indexed, NOT uniquely constrained), `employee_id FK→users CASCADE`, `employee_code`, `employee_function`, `report_date DATE`, `status` ('DRAFT'/'SUBMITTED'/'PENDING_APPROVAL'/'APPROVED'/'REJECTED'/'REFERRED_TO_REQUESTOR'), `currency CHAR(3)`, `total_amount NUMERIC(15,2)`, `submitted_at`, `current_approval_level INT`, `rejection_comment TEXT`, `rejected_at_level INT`, `referred_back_from_level INT`, `referred_back_levels JSONB` | ⚠️ report_number has no UNIQUE constraint — race-condition duplicates possible |
| `expense_lines` | `id PK`, `report_id FK→expense_reports CASCADE`, `line_number INT`, `pl_group`, `gl_account VARCHAR(255) NULL` (legacy), `io_dimension`, `cost_center`, `location`, `invoice_date DATE`, `invoice_number`, `description`, `amount NUMERIC(15,2)`, `category_id FK→expense_categories SET NULL`, `subcategory_id FK→expense_categories SET NULL`, **`gl_id FK→chart_of_accounts SET NULL`** (M9), `dimension_values JSONB`, `is_split_parent BOOL DEFAULT FALSE`, `split_parent_id FK→expense_lines SET NULL`, `flag_incorrect BOOL`, `flag_comment TEXT` | |
| `expense_report_snapshots` | `id PK`, `report_id FK→expense_reports CASCADE`, `tenant_id FK→tenants CASCADE`, `snapshot_data JSONB`, `submitted_at`, `version INT DEFAULT 1` | Immutable. ⚠️ snapshot_data does NOT include M9 fields (gl_id, dimension_values, split data) |
| `tenant_expense_config` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `coding_level INT DEFAULT 0` (0-4), `require_category BOOL`, `require_subcategory BOOL`, `allow_free_text_description BOOL DEFAULT TRUE`, `show_location BOOL DEFAULT TRUE`, `require_location BOOL DEFAULT FALSE`, `locked_by_implementation BOOL`, `ocr_enabled BOOL DEFAULT TRUE` (M10) | |
| `expense_categories` | `id PK`, `tenant_id FK→tenants CASCADE`, `name`, `code VARCHAR(100)`, `parent_id FK→expense_categories SET NULL`, `is_active BOOL`, `sort_order INT` | Two-level tree |

### 2.7 Approval Tables

| Table | Key columns | Notes |
|---|---|---|
| `approval_matrix` | `id PK`, `tenant_id UNIQUE FK→tenants CASCADE`, `levels INT`, `level1_role`, `level2_role NULL`, `level3_role NULL`, `amount_threshold_l2 NUMERIC(15,2) NULL`, `amount_threshold_l3 NUMERIC(15,2) NULL` | Legacy matrix config — superseded by `approval_policies` for M112+ policy-based routing. |
| `approval_policies` | `id PK`, `tenant_id FK→tenants CASCADE`, `module VARCHAR(50)`, `routing_mode VARCHAR(30)` (org_tree/selective_tree), `selected_designations JSONB NULL`, `requires_finance_review BOOL DEFAULT FALSE`, `ceiling_designation VARCHAR(50) NULL`, `finance_l1_designation VARCHAR(50) NULL`, `finance_l2_designation VARCHAR(50) NULL`, `finance_l3_designation VARCHAR(50) NULL` | **Designation-based** (migration `s1t2u3v4w5x6`). Old `ceiling_role_id`/`finance_l[1-3]_role_id` FK columns retained on ORM model only (soft-deprecated, not queried). |
| `approval_role_thresholds` | `id PK`, `policy_id FK→approval_policies CASCADE`, `designation VARCHAR(50) NOT NULL`, `max_amount NUMERIC(15,2) NULL`, UQ(policy_id, designation) | **Designation-keyed** (migration `s1t2u3v4w5x6`). Previously had `approval_role_id UUID FK→approval_roles` — dropped and replaced with `designation` string. |
| `finance_review_steps` | `id PK`, `tenant_id FK→tenants CASCADE`, `policy_id FK→approval_policies CASCADE`, `level INT`, `label VARCHAR(100)`, `step_type VARCHAR(50)` (free-form; built-in: capture/validate/review/approve/internal_audit), `function_code VARCHAR(50) NULL`, `assigned_designation VARCHAR(50) NULL`, `assigned_employee_id FK→employees SET NULL NULL`, `min_amount NUMERIC NULL`, `is_required BOOL`, `vacant_seat_behavior VARCHAR(30)` | Step builder output. **Now actually read by `approval_routing.py` `compute_chain()`** (previously ignored — routing used dead `finance_l[1-3]_role_id` fields instead). `function_code`: filters assignee picker to employees mapped to that function. |
| `expense_approvals` | `id PK`, `report_id FK→expense_reports CASCADE`, `tenant_id FK→tenants CASCADE`, `level INT`, `approver_id FK→users CASCADE`, `status` ('PENDING'/'APPROVED'/'REJECTED'/'REFERRED_BACK'), `comment TEXT`, `visible_to_requestor BOOL`, `response_comment TEXT`, `actioned_at`, `is_advisory BOOL NOT NULL DEFAULT FALSE` | `is_advisory`: migration `r6s7t8u9v0w1`. Advisory steps don't block chain advance; notified but skipped in `current_approval_level` progression. |

### 2.8 GL / Journal Tables

| Table | Key columns | Notes |
|---|---|---|
| `journal_entries` | `id PK`, `tenant_id FK→tenants CASCADE`, `entry_date DATE`, `description`, `source`, `reference_number` (JE-YYYY-NNNNNN), `status` ('DRAFT'/'POSTED'/'REVERSED'), `reverses_entry_id FK→journal_entries SET NULL`, `reversed_by_entry_id FK→journal_entries SET NULL`, `created_by FK→users SET NULL`, `posted_at`, UQ:(tenant_id,reference_number) | POSTED = immutable |
| `journal_lines` | `id PK`, `tenant_id FK→tenants CASCADE`, `journal_entry_id FK→journal_entries CASCADE`, `gl_account_id FK→chart_of_accounts NO CASCADE`, `debit NUMERIC(18,2)`, `credit NUMERIC(18,2)`, `description`, `line_number INT`, `dimensions JSONB`, `bank_account_id FK→bank_accounts SET NULL` | Invariants: one of debit/credit > 0, Σdebit == Σcredit (app-enforced) |

### 2.9 Account Mapping & Bank Tables

| Table | Key columns | Notes |
|---|---|---|
| `posting_roles` | `role_key VARCHAR(60) PK`, `label`, `statement` ('BS'/'PL'), `group`, `subgroup NULL`, `display_order INT`, `expected_account_type`, `is_control_account BOOL`, `description TEXT` | System catalogue |
| `tenant_account_mappings` | `id PK`, `tenant_id FK→tenants CASCADE`, `role_key FK→posting_roles CASCADE`, `gl_account_id FK→chart_of_accounts NO CASCADE`, `created_by FK→users SET NULL`, UQ:(tenant_id,role_key) | |
| `tenant_posting_role_settings` | `id PK`, `tenant_id FK→tenants CASCADE`, `role_key FK→posting_roles CASCADE`, `is_control_account_override BOOL NULL`, `is_relevant BOOL NULL`, UQ:(tenant_id,role_key) | NULL = use catalogue default; is_relevant is cosmetic only |
| `bank_accounts` | `id PK`, `tenant_id FK→tenants CASCADE`, `bank_name`, `account_name`, `account_number`, `currency CHAR(3)`, `gl_account_id FK→chart_of_accounts NO CASCADE`, `is_default BOOL`, `is_active BOOL`, `created_by FK→users SET NULL` | At most one is_default per (tenant_id, currency) — app-enforced |

### 2.10 Documents Tables (tasks #53–#55, migration `k2l3m4n5o6p7`)

| Table | Key columns | Notes |
|---|---|---|
| `expense_documents` | `id PK`, `tenant_id FK→tenants CASCADE`, `report_id FK→expense_reports CASCADE`, `line_id FK→expense_lines SET NULL` (NULL = report-level), `uploaded_by FK→users CASCADE`, `file_name`, `file_size INT`, `size_stored INT NULL` (post-compression bytes), `mime_type`, `storage_path VARCHAR(1000)`, `file_hash VARCHAR(64) NULL` (SHA-256 of original bytes), `file_hash_algorithm VARCHAR(10) NULL` ('sha256'), `retain_until DATE NULL` (15yr minimum; SA-configurable), `dedup_ref UUID FK→expense_documents SET NULL NULL` | Actual bytes in Supabase bucket 'documents'. Signed URLs expire after 15 minutes. Accepted upload types: PDF, JPEG, PNG, GIF, WEBP, DOCX, XLSX. DOCX/XLSX validated via ZIP structure inspection; macro-enabled variants rejected. Images compressed to WebP (Pillow); PDFs compressed via pikepdf. Dedup: same hash within tenant reuses Supabase blob. Deletion blocked if `retain_until > today()`. |
| `document_access_log` | `id PK`, `document_id FK→expense_documents CASCADE`, `accessed_by FK→users SET NULL`, `tenant_id FK→tenants CASCADE`, `accessed_at TIMESTAMPTZ DEFAULT now()`, `access_type VARCHAR(20)` ('upload'/'view'/'delete'), `ip_address VARCHAR(45)` | Append-only audit log. Cascade on document_id (access log removed with document by design — financial audit in audit_logs). SET NULL on accessed_by (user deletion must not destroy compliance records). Indexes: document_id, tenant_id, accessed_at. |

### 2.12 Posting Batches Table (migration committed `f24c2fe`, not yet applied to local DB)

| Table | Key columns | Notes |
|---|---|---|
| `posting_batches` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `batch_ref VARCHAR(50) UNIQUE`, `module VARCHAR(30)`, `status VARCHAR(20)` (pending/exported/synced), `transactions JSONB`, `created_at`, `exported_at NULLABLE`, `synced_at NULLABLE` | Connected Mode export queue. Ref format `BATCH-{YYYY}-{MM}-{NNN}`. Populated by `expense_posting.py` when `posting_mode='connected'`. Migration `i2j3k4l5m6n7` (applied locally). |

### 2.13 Accounts Payable Tables (migration `y7z8a9b0c1d2`, 2026-07-25)

| Table | Key columns | Notes |
|---|---|---|
| `vendors` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `code VARCHAR(20)` (auto `V-{NNNN}`), `name`, `vendor_type`, `tax_id`, `email`, `phone`, `address`, `bank_name`, `bank_account_number`, `bank_sort_code`, `is_active BOOL DEFAULT TRUE`, `notes`, `created_at`, `updated_at`. UQ: `(tenant_id, code)`. | Supplier master per tenant. |
| `ap_invoices` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `vendor_id FK→vendors`, `reference VARCHAR(50)` (`AP-{YYYY}-{NNNN}`), `invoice_number VARCHAR(100)`, `invoice_date DATE`, `received_date DATE`, `due_date DATE`, `currency CHAR(3)`, `exchange_rate NUMERIC(18,6)`, `total_amount_foreign`, `total_amount_base`, `total_vat`, `total_wht`, `net_payable NUMERIC(18,2)`, `description`, `status VARCHAR(20)` (`DRAFT\|SUBMITTED\|APPROVED\|REJECTED\|CANCELLED\|PAID`), `posting_mode`, `duplicate_flag BOOL`, `is_advance_settlement BOOL`, `submitted_at/by`, `approved_at/by`, `rejected_at/by/reason`, `cancelled_at/by`, `paid_at/by`, `payment_reference`, `payment_bank_account_id FK→bank_accounts`, `journal_entry_id FK→journal_entries`, `payment_journal_entry_id FK→journal_entries`, `posting_batch_id FK→posting_batches`, `created_at/by`. UQ: `(tenant_id, vendor_id, invoice_number)`. | AP invoice header. Duplicate flag set if same `(tenant_id, vendor_id, invoice_number)` exists with non-cancelled/rejected status. |
| `ap_invoice_lines` | `id UUID PK`, `invoice_id FK→ap_invoices CASCADE`, `line_number INT`, `description`, `quantity NUMERIC(18,4)`, `unit_price NUMERIC(18,2)`, `amount_foreign`, `amount_base`, `gl_account_id FK→chart_of_accounts SET NULL`, `dimension_values JSONB`, `vat_applicable BOOL`, `vat_rate NUMERIC(6,4)`, `vat_amount NUMERIC(18,2)`, `wht_applicable BOOL`, `wht_rate NUMERIC(6,4)`, `wht_amount NUMERIC(18,2)`, `net_payable_line NUMERIC(18,2)`, `category_hint VARCHAR(100)`. | Line items. `amount_base = amount_foreign × exchange_rate`. `net_payable_line = amount_base − wht_amount`. |
| `ap_approvals` | `id UUID PK`, `invoice_id FK→ap_invoices CASCADE`, `tenant_id FK→tenants CASCADE`, `step_order INT`, `approver_id FK→users SET NULL`, `role_id FK→approval_roles SET NULL`, `status VARCHAR(20)` (`PENDING\|APPROVED\|REJECTED\|REFERRED_BACK\|SKIPPED`), `is_advisory BOOL DEFAULT FALSE`, `action_at`, `comment`. | Step-ordered approval trail. Mirrors `expense_approvals`. |
| `ap_invoice_snapshots` | `id UUID PK`, `invoice_id FK→ap_invoices CASCADE`, `snapshot_data JSONB`, `created_at`. | Immutable JSONB snapshot written at submission. |

### 2.14 Purchase Order & 3-Way Match Tables (migration `z8a9b0c1d2e3`, 2026-07-25)

| Table | Key columns | Notes |
|---|---|---|
| `purchase_orders` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `vendor_id FK→vendors`, `po_number VARCHAR(50)` (`PO-{YYYY}-{NNNN}`), `requester_id FK→users SET NULL`, `department_id FK→org_structure SET NULL`, `title VARCHAR(255)`, `delivery_date DATE`, `delivery_address TEXT`, `currency CHAR(3)`, `exchange_rate NUMERIC(18,6)`, `total_amount_foreign`, `total_amount_base`, `amount_received NUMERIC(18,2)`, `amount_invoiced NUMERIC(18,2)`, `status VARCHAR(20)` (`DRAFT\|SUBMITTED\|APPROVED\|REJECTED\|SENT\|PARTIALLY_RECEIVED\|FULLY_RECEIVED\|CLOSED\|CANCELLED`), `posting_mode VARCHAR(20)` (snapshotted at approval), `notes`, `submitted_at/by`, `approved_at/by`, `rejected_at/by/reason`, `sent_at/by`, `closed_at/by`, `cancelled_at/by`, `journal_entry_id FK→journal_entries SET NULL`, `posting_batch_id FK→posting_batches SET NULL`, `created_at/by`, `updated_at`. UQ: `(tenant_id, po_number)`. | PO header. `amount_received` incremented by GRN confirm; `amount_invoiced` incremented by match creation. |
| `purchase_order_lines` | `id UUID PK`, `po_id FK→purchase_orders CASCADE`, `line_number INT`, `description TEXT`, `unit_of_measure VARCHAR(30)`, `quantity_ordered NUMERIC(18,4)`, `unit_price NUMERIC(18,2)`, `amount_foreign`, `amount_base`, `quantity_received NUMERIC(18,4)`, `quantity_invoiced NUMERIC(18,4)`, `gl_account_id FK→chart_of_accounts SET NULL`, `dimension_values JSONB`, `vat_applicable BOOL`, `vat_rate NUMERIC(6,4)`, `wht_applicable BOOL`, `wht_rate NUMERIC(6,4)`, `category_hint VARCHAR(100)`. | `amount_foreign = quantity_ordered × unit_price`; `amount_base = amount_foreign × exchange_rate`. `quantity_received` incremented by GRN confirmation. `quantity_invoiced` incremented by match creation. |
| `po_approvals` | `id UUID PK`, `po_id FK→purchase_orders CASCADE`, `tenant_id FK→tenants CASCADE`, `step_order INT`, `approver_id FK→users SET NULL`, `role_id FK→approval_roles SET NULL`, `status VARCHAR(20)` (`PENDING\|APPROVED\|REJECTED\|REFERRED_BACK\|SKIPPED`), `is_advisory BOOL DEFAULT FALSE`, `action_at`, `comment`. | Per-step approval audit trail. Mirrors `ap_approvals`. Approval module key: `"po"`. |
| `po_snapshots` | `id UUID PK`, `po_id FK→purchase_orders CASCADE`, `snapshot_data JSONB`, `created_at`. | Immutable JSONB snapshot written at submission. Same pattern as `ap_invoice_snapshots`. |
| `goods_receipt_notes` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `po_id FK→purchase_orders`, `grn_number VARCHAR(50)` (`GRN-{YYYY}-{NNNN}`), `received_by FK→users SET NULL`, `receipt_date DATE`, `delivery_note_number VARCHAR(100)`, `notes TEXT`, `status VARCHAR(20)` (`DRAFT\|CONFIRMED`), `confirmed_at`, `confirmed_by FK→users SET NULL`, `grni_journal_entry_id FK→journal_entries SET NULL`, `grni_posting_batch_id FK→posting_batches SET NULL`, `created_at/by`. UQ: `(tenant_id, grn_number)`. | GRN is immutable once CONFIRMED. Confirmation triggers GRNI accrual. |
| `grn_lines` | `id UUID PK`, `grn_id FK→goods_receipt_notes CASCADE`, `po_line_id FK→purchase_order_lines`, `line_number INT`, `description TEXT`, `quantity_received NUMERIC(18,4)`, `unit_price_on_po NUMERIC(18,2)` (locked at GRN creation), `amount_base NUMERIC(18,2)`, `condition_notes TEXT`. | Over-receipt guard enforced in router: `quantity_received ≤ po_line.quantity_ordered − po_line.quantity_received`. `amount_base = quantity_received × unit_price_on_po × exchange_rate`. |
| `ap_invoice_po_matches` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `invoice_id FK→ap_invoices CASCADE`, `invoice_line_id FK→ap_invoice_lines CASCADE`, `grn_id FK→goods_receipt_notes`, `grn_line_id FK→grn_lines`, `po_id FK→purchase_orders`, `po_line_id FK→purchase_order_lines`, `matched_quantity NUMERIC(18,4)`, `matched_amount_base NUMERIC(18,2)`, `price_variance NUMERIC(18,2)`, `price_variance_pct NUMERIC(6,4)`, `qty_variance NUMERIC(18,4)`, `match_status VARCHAR(30)` (`MATCHED\|PRICE_VARIANCE\|QTY_VARIANCE\|OVER_INVOICED\|UNDER_INVOICED\|MANUAL_OVERRIDE`), `override_comment TEXT`, `created_at/by`. | N:M junction: one invoice line ↔ many GRN lines (split deliveries). Variance frozen at creation. Status computed by `po_match_engine.compute_match_status()`. |
| `po_tolerance_config` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `price_tolerance_pct NUMERIC(6,4) DEFAULT 0.02` (2%), `qty_tolerance_pct NUMERIC(6,4) DEFAULT 0.05` (5%), `auto_approve_within_tolerance BOOL DEFAULT FALSE`, `block_payment_on_variance BOOL DEFAULT TRUE`, `updated_at`, `updated_by FK→users SET NULL`. UQ: `(tenant_id)`. | Auto-created with defaults on first match operation if not present. |

**Posting roles added by this migration:**
- `po_commitment` (`role_key='po_commitment'`, statement='BS', group='memo') — off-balance-sheet memo role. Only used when commitment accounting is enabled (not yet implemented). Seeded with `ON CONFLICT DO NOTHING`.
- `grni` (`role_key='grni'`) — already seeded in `c9d0e1f2g3h4_catalogue_redesign`. NOT re-seeded.

**GL journal flows (Full ERP):**
- GRN confirmed: DR `<po_line.gl_account_id>` per line / CR `grni` (total) — posted by `po_posting.post_grni_accrual()`
- Invoice match + approval: DR `grni` / CR `accounts_payable` — posted by `po_posting.post_grni_clearance()` (called from `ap_posting.post_ap_approval()` when matched)

### 2.15 Bank Reconciliation Tables (migration `a9b0c1d2e3f4`, 2026-07-25)

| Table | Key columns | Notes |
|---|---|---|
| `bank_statements` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `bank_account_id FK→bank_accounts RESTRICT`, `statement_ref VARCHAR(30)` (`STMT-{YYYY}-{NNN}`), `statement_date DATE`, `period_start DATE nullable`, `opening_balance NUMERIC(18,2)`, `closing_balance NUMERIC(18,2)`, `currency CHAR(3)`, `status VARCHAR(20)` (`DRAFT\|IN_PROGRESS\|RECONCILED`), `notes TEXT nullable`, `uploaded_by FK→users SET NULL`, `created_at/updated_at`. UQ: `(tenant_id, statement_ref)`. | Statement header. Status advances: DRAFT (no lines) → IN_PROGRESS (lines uploaded) → RECONCILED (all lines MATCHED or EXCLUDED). `bank_account_id` uses RESTRICT — prevents accidental account deletion when statements exist. |
| `bank_statement_lines` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `statement_id FK→bank_statements CASCADE`, `line_number INT`, `transaction_date DATE`, `value_date DATE nullable`, `description VARCHAR(500)`, `reference VARCHAR(255) nullable`, `debit NUMERIC(18,2) DEFAULT 0`, `credit NUMERIC(18,2) DEFAULT 0`, `running_balance NUMERIC(18,2) nullable`, `match_status VARCHAR(20)` (`UNMATCHED\|MATCHED\|PARTIAL\|EXCLUDED`). | Bank-perspective: credit = inflow, debit = outflow. `match_status` is a denormalised cache updated by `recompute_line_status()` after every match create/delete. EXCLUDED is set explicitly by user and not overwritten by the engine. Indexed on `(statement_id, match_status)`. |
| `bank_recon_matches` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `statement_line_id FK→bank_statement_lines CASCADE`, `match_type VARCHAR(20)` (`journal_line\|posting_batch\|manual`), `matched_journal_line_id FK→journal_lines SET NULL nullable`, `matched_posting_batch_id FK→posting_batches SET NULL nullable`, `matched_amount NUMERIC(18,2)`, `notes VARCHAR(500) nullable`, `matched_by FK→users SET NULL`, `matched_at DATETIME`. | One statement line may have multiple match records (partial matching — sum of `matched_amount` = line amount when fully matched). `match_type` discriminator: exactly one of the two FKs is non-null, or both null for manual/notes-only. `posting_batches` FK is SET NULL so batches may be deleted without breaking recon history. |

**Three-mode summary:**
- Lite: `match_type='manual'`. No FK to GL or batches. `auto_match_statement()` returns 0 matches immediately.
- Connected: `match_type='posting_batch'`. Auto-match searches `posting_batches` (status: pending/exported) by amount ± tolerance and date ± window.
- Full ERP: `match_type='journal_line'`. Auto-match searches `journal_lines` with `bank_account_id = statement.bank_account_id`, POSTED entries only, within date window, correct side (bank credit ↔ GL debit; bank debit ↔ GL credit).

**Reconciliation equation (Full ERP):**
`adjusted_gl_balance = gl_book_balance + Σ outstanding_deposits − Σ outstanding_payments`
`is_balanced = abs(adjusted_gl_balance − statement.closing_balance) ≤ 0.01`

### 2.16 Accounts Receivable Tables (migration `d2e3f4g5h6i7`, 2026-07-28)

| Table | Key columns | Notes |
|---|---|---|
| `customers` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `code VARCHAR(20)` (auto `C-{NNNN}` if omitted), `name`, `customer_type VARCHAR(30)` (`standard/government/ngo/corporate/individual/non_resident`), `tax_id`, `email`, `phone`, `address`, `credit_limit NUMERIC(18,2) NULL`, `credit_terms VARCHAR(20) NULL` (`immediate/net_30/net_60/net_90/custom`), `credit_terms_days INT NULL`, `is_active BOOL DEFAULT TRUE`, `notes TEXT`, `created_at/updated_at`. UQ: `(tenant_id, code)`. | Customer master per tenant. Outstanding balance computed at query time by summing APPROVED (unreceived) `ar_invoices.net_receivable`. |
| `ar_invoices` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `customer_id FK→customers`, `ar_reference VARCHAR(50)` (`AR-{YYYY}-{NNNN}`), `invoice_number VARCHAR(100)`, `invoice_date DATE`, `due_date DATE NULL` (auto-computed from credit_terms if blank), `service_period_start/end DATE NULL`, `currency CHAR(3)`, `exchange_rate NUMERIC(18,6)`, `total_amount_foreign NUMERIC(18,2)`, `total_amount_base NUMERIC(18,2)`, `total_vat NUMERIC(18,2)`, `total_wht NUMERIC(18,2)`, `net_receivable NUMERIC(18,2)` (= total_amount_base + total_vat − total_wht), `description`, `status VARCHAR(20)` (`DRAFT/SUBMITTED/APPROVED/RECEIVED/REJECTED/CANCELLED`), `posting_mode VARCHAR(20)` (snapshotted at approval), `duplicate_flag BOOL DEFAULT FALSE`, `snapshot_json JSONB NULL`, `submitted_at/by`, `approved_at/by`, `rejected_at/by/reason`, `cancelled_at/by`, `received_at`, `received_by FK→users SET NULL`, `receipt_reference VARCHAR(100) NULL`, `receipt_bank_account_id FK→bank_accounts SET NULL`, `journal_entry_id FK→journal_entries SET NULL`, `receipt_journal_entry_id FK→journal_entries SET NULL`, `posting_batch_id FK→posting_batches SET NULL`, `created_at/by`, `updated_at`. UQ: `(tenant_id, ar_reference)`. | Duplicate flag: same `(tenant_id, customer_id, invoice_number)` with non-cancelled/rejected status. |
| `ar_invoice_lines` | `id UUID PK`, `invoice_id FK→ar_invoices CASCADE`, `line_number INT`, `description TEXT`, `amount_foreign NUMERIC(18,2)`, `amount_base NUMERIC(18,2)`, `gl_account_id FK→chart_of_accounts SET NULL`, `dimension_values JSONB`, `vat_applicable BOOL DEFAULT FALSE`, `vat_rate NUMERIC(6,4)`, `vat_amount NUMERIC(18,2)`, `wht_applicable BOOL DEFAULT FALSE`, `wht_rate NUMERIC(6,4)`, `wht_amount NUMERIC(18,2)`, `net_receivable_line NUMERIC(18,2)` (= amount_base + vat_amount − wht_amount). | GL is the revenue GL account (not an expense/COGS account). |
| `ar_approvals` | `id UUID PK`, `invoice_id FK→ar_invoices CASCADE`, `tenant_id FK→tenants CASCADE`, `step_order INT`, `approver_id FK→users SET NULL`, `role_id FK→approval_roles SET NULL`, `status VARCHAR(20)` (`PENDING/APPROVED/REJECTED/REFERRED_BACK/SKIPPED`), `is_advisory BOOL DEFAULT FALSE`, `action_at`, `comment`. | Step-ordered approval trail. Mirrors `ap_approvals`. |
| `ar_invoice_snapshots` | `id UUID PK`, `invoice_id FK→ar_invoices CASCADE`, `snapshot_data JSONB`, `created_at`. | Immutable JSONB snapshot written at submission. |

**Net receivable formula (opposite sign from AP net_payable):**
- AR: `net_receivable = amount_base + vat_amount − wht_amount` — VAT adds to what the customer owes; WHT is deducted by customer before paying.
- AP: `net_payable = amount_base − wht_amount` — no VAT added to payable.

**Approval module key:** `"receivable"` (AP uses `"payable"`).

**GL journals (Full ERP):**
- On final approval (`post_ar_approval()`): DR `accounts_receivable` / CR `revenue GL per line` — synchronous, same transaction.
- On receipt (`post_ar_receipt()`): DR `bank GL (receipt_bank_account_id)` / CR `accounts_receivable`.

**Connected mode:** `create_ar_posting_batch()` creates a `posting_batches` row with `module="ar"`.

**AR aging:** APPROVED (unreceived) invoices only, bucketed by days past `due_date`: Current (not yet due), 1–30, 31–60, 61–90, 90+.

### 2.17 AI Engine Tables (migration `b0c1d2e3f4g5`, 2026-07-25)

| Table | Key columns | Notes |
|---|---|---|
| `ai_predictions` | `id PK`, `tenant_id FK→tenants CASCADE`, `user_id FK→users SET NULL`, `prediction_type TEXT` ('ocr'/'classify'/'fraud'/etc.), `source_document_id FK→expense_documents SET NULL`, `input_hash TEXT`, `ocr_model TEXT`, `prediction_json JSONB`, `confidence_overall NUMERIC(5,4)`, `accepted BOOL nullable`, `processing_ms INT`, `created_at` | Audit trail for every AI action (AI Engine PRD §12). One row per API call. |
| `ai_learning_overrides` | `id PK`, `prediction_id FK→ai_predictions CASCADE`, `tenant_id FK→tenants CASCADE`, `user_id FK→users SET NULL`, `field_name TEXT`, `original_value JSONB`, `override_value JSONB`, `created_at` | Records Finance reviewer corrections. Populated via `POST /api/ai/override`. M20 learning loop trains on these. |

### 2.18 Budget & Planning Tables — M16 (migration `e3f4g5h6i7j8`, pending CC commit)

| Table | Key columns | Notes |
|---|---|---|
| `budget_periods` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `period_name VARCHAR(100)`, `fiscal_year VARCHAR(20)`, `start_date DATE`, `end_date DATE`, `status VARCHAR(20)` (`DRAFT\|ACTIVE\|CLOSED`), `version INT DEFAULT 1` (BP=1, FRE=2, SRE=3), `notes TEXT`, `created_by FK→users SET NULL`, `created_at`, `updated_at`. UQ: `(tenant_id, fiscal_year, version)`. | Budget version per fiscal year. Latest version (highest version number) used for variance comparisons. |
| `budget_lines` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `budget_period_id FK→budget_periods CASCADE`, `gl_account_id FK→chart_of_accounts SET NULL`, `dimension_values JSONB`, `description VARCHAR(255)`, `amount NUMERIC(18,2)`, `currency CHAR(3)`, `notes TEXT`, `created_at`, `updated_at`. | One row per GL account + dimension combination per budget period. `amount` is the full-period budget. Variance computed at query time. |

**Three-mode variance engine (`budget_service.py`):**
- Full ERP: actuals from `journal_lines` JOINed to `journal_entries` (POSTED, date within period range) by `gl_account_id`
- Connected: actuals from `posting_batches` JSONB transactions for the period
- Lite: actuals from `expense_lines` (APPROVED reports) by `gl_id` for the period

**GL journals:** No direct GL posting for budget creation (budgets are plan data, not transactions).

---

### 2.19 SA Billing & Subscriptions — SA-B (migration `f4g5h6i7j8k9`, pending CC commit)

| Table | Key columns | Notes |
|---|---|---|
| `pricing_plans` | `id UUID PK`, `plan_code VARCHAR(30) UNIQUE`, `plan_name VARCHAR(100)`, `monthly_price NUMERIC(10,2)`, `annual_price NUMERIC(10,2)`, `max_users INT NULL`, `max_modules INT NULL`, `features JSONB`, `is_active BOOL DEFAULT TRUE`, `created_at`. | Platform-wide plan catalogue. Managed by SA only. |
| `tenant_subscriptions` | `id UUID PK`, `tenant_id FK→tenants CASCADE UNIQUE`, `plan_id FK→pricing_plans SET NULL`, `status VARCHAR(20)` (`TRIAL\|ACTIVE\|PAST_DUE\|CANCELLED`), `trial_ends_at TIMESTAMPTZ NULL`, `current_period_start DATE`, `current_period_end DATE`, `cancelled_at TIMESTAMPTZ NULL`, `cancel_reason TEXT NULL`, `created_at`, `updated_at`. | One subscription per tenant. `plan_id NULL` = free/trial tier. |
| `billing_events` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `event_type VARCHAR(50)` (`PLAN_CHANGE\|PAYMENT\|REFUND\|TRIAL_START\|TRIAL_END\|SUSPENSION`), `amount NUMERIC(10,2) NULL`, `currency CHAR(3) DEFAULT 'USD'`, `description TEXT`, `metadata JSONB`, `recorded_by FK→users SET NULL`, `created_at`. | Append-only billing event log per tenant. |

**Access control:** All `/api/sa/billing/*` endpoints require `require_super_admin()`. Tenants never see this data.

---

### 2.20 Tax Engine Tables — M19 (migration `g5h6i7j8k9l0`, pending CC commit)

| Table | Key columns | Notes |
|---|---|---|
| `tax_returns` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `tax_type VARCHAR(20)` (`VAT\|WHT\|PAYE`), `return_period VARCHAR(20)` (e.g. `2026-06`), `period_start DATE`, `period_end DATE`, `status VARCHAR(20)` (`DRAFT\|FILED\|PAID`), `gross_tax NUMERIC(18,2)`, `credits_applied NUMERIC(18,2)`, `net_payable NUMERIC(18,2)`, `filing_date DATE NULL`, `payment_date DATE NULL`, `reference VARCHAR(100) NULL`, `notes TEXT`, `prepared_by FK→users SET NULL`, `filed_by FK→users SET NULL`, `created_at`, `updated_at`. UQ: `(tenant_id, tax_type, return_period)`. | One return row per tax type per period. `gross_tax` summed from applicable transactions by `tax_compute_service.py`. |
| `wht_certificates` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `vendor_id FK→vendors SET NULL`, `certificate_number VARCHAR(100)`, `tax_return_id FK→tax_returns SET NULL`, `period VARCHAR(20)`, `gross_payment NUMERIC(18,2)`, `wht_rate NUMERIC(6,4)`, `wht_amount NUMERIC(18,2)`, `payment_date DATE`, `issued_at TIMESTAMPTZ NULL`, `notes TEXT`, `created_at`. | One certificate per vendor per WHT deduction event. Auto-generated from approved AP invoices with `wht_applicable=True`. |

**Compute service (`tax_compute_service.py`):**
- `compute_vat_summary(tenant_id, period_start, period_end)` — sums `vat_amount` from `ap_invoice_lines` (input) and `ar_invoice_lines` (output) for APPROVED records within the period.
- `compute_wht_summary(tenant_id, period_start, period_end)` — sums `wht_amount` from approved `ap_invoice_lines`.
- `compute_paye_summary(tenant_id, period_start, period_end)` — sums PAYE deductions from posted `payroll_runs` within the period.

---

### 2.21 Payroll & HR Tables — M15 (migration `h6i7j8k9l0m1`, pending CC commit)

| Table | Key columns | Notes |
|---|---|---|
| `salary_structures` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `name VARCHAR(100)`, `currency CHAR(3)`, `earnings JSONB` (list of `{component, amount, is_taxable}`), `deductions JSONB` (list of `{component, amount, statutory, rate}`), `is_active BOOL DEFAULT TRUE`, `created_at`. UQ: `(tenant_id, name)`. | Template for computing payroll. Earnings components: basic, housing, transport, etc. Deductions: pension, NHF, PAYE. |
| `payroll_runs` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `run_name VARCHAR(100)`, `pay_period VARCHAR(20)` (e.g. `2026-06`), `pay_date DATE`, `currency CHAR(3)`, `status VARCHAR(20)` (`DRAFT\|APPROVED\|PAID`), `total_gross NUMERIC(18,2)`, `total_deductions NUMERIC(18,2)`, `total_net NUMERIC(18,2)`, `total_paye NUMERIC(18,2)`, `approved_by FK→users SET NULL`, `approved_at`, `journal_entry_id FK→journal_entries SET NULL` (Full ERP), `posting_batch_id FK→posting_batches SET NULL` (Connected), `created_by FK→users SET NULL`, `created_at`. UQ: `(tenant_id, pay_period)`. | Payroll run header. APPROVED triggers GL posting in Full ERP mode. |
| `payroll_lines` | `id UUID PK`, `payroll_run_id FK→payroll_runs CASCADE`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `salary_structure_id FK→salary_structures SET NULL`, `gross_pay NUMERIC(18,2)`, `total_deductions NUMERIC(18,2)`, `net_pay NUMERIC(18,2)`, `paye NUMERIC(18,2)`, `pension_employee NUMERIC(18,2)`, `pension_employer NUMERIC(18,2)`, `nhf NUMERIC(18,2)`, `earnings_breakdown JSONB`, `deductions_breakdown JSONB`. | One row per employee per payroll run. PAYE computed by `tax_compute_service`. |
| `payslips` | `id UUID PK`, `payroll_line_id FK→payroll_lines CASCADE UNIQUE`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `payslip_ref VARCHAR(50)` (`PS-{YYYY}-{MM}-{NNNN}`), `issued_at TIMESTAMPTZ`, `pdf_storage_path VARCHAR(500) NULL`. | One payslip per payroll line. Generated on run approval. |
| `leave_types` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `name VARCHAR(100)`, `days_allowed INT`, `carry_over_allowed BOOL DEFAULT FALSE`, `carry_over_max_days INT DEFAULT 0`, `requires_approval BOOL DEFAULT TRUE`, `is_active BOOL DEFAULT TRUE`. UQ: `(tenant_id, name)`. | Leave type catalogue per tenant (Annual, Sick, Maternity, etc.). |
| `leave_requests` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `leave_type_id FK→leave_types CASCADE`, `start_date DATE`, `end_date DATE`, `days_requested INT`, `reason TEXT NULL`, `status VARCHAR(20)` (`PENDING\|APPROVED\|REJECTED\|CANCELLED`), `approved_by FK→users SET NULL`, `approved_at`, `rejection_reason TEXT NULL`, `created_at`. | Leave application. `days_requested` computed from start/end excluding weekends. |
| `leave_balances` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `employee_id FK→employees CASCADE`, `leave_type_id FK→leave_types CASCADE`, `year INT`, `days_allocated INT`, `days_taken INT DEFAULT 0`, `days_carried_over INT DEFAULT 0`. UQ: `(tenant_id, employee_id, leave_type_id, year)`. | Maintained by leave approval events. |

**Three-mode GL journals (Full ERP on payroll run approval):**
- DR Salary Expense (by cost center GL) / CR Payroll Payable (control)
- DR Payroll Payable / CR PAYE Payable (statutory)
- DR Payroll Payable / CR Pension Payable (statutory)

---

### 2.22 Fixed Assets Tables — M18 (migration `i7j8k9l0m1n2`, pending CC commit)

| Table | Key columns | Notes |
|---|---|---|
| `asset_categories` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `name VARCHAR(100)`, `depreciation_method VARCHAR(20)` (`STRAIGHT_LINE\|REDUCING_BALANCE`), `useful_life_years INT NULL` (SL only), `depreciation_rate NUMERIC(6,4) NULL` (RB only — e.g. 0.25 = 25%), `gl_asset_id FK→chart_of_accounts SET NULL`, `gl_accumulated_dep_id FK→chart_of_accounts SET NULL`, `gl_dep_expense_id FK→chart_of_accounts SET NULL`, `is_active BOOL DEFAULT TRUE`. UQ: `(tenant_id, name)`. | Category drives default depreciation method and GL accounts for assets. |
| `assets` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `asset_number VARCHAR(50)` (auto `FA-{NNNN}`), `name VARCHAR(255)`, `category_id FK→asset_categories CASCADE`, `acquisition_date DATE`, `cost NUMERIC(18,2)`, `residual_value NUMERIC(18,2) DEFAULT 0`, `currency CHAR(3)`, `location TEXT NULL`, `serial_number VARCHAR(100) NULL`, `status VARCHAR(20)` (`ACTIVE\|DISPOSED\|IMPAIRED`), `notes TEXT NULL`, `created_at`. UQ: `(tenant_id, asset_number)`. | Asset master record. NBV computed from cost minus accumulated depreciation schedules. |
| `asset_depreciation_schedules` | `id UUID PK`, `asset_id FK→assets CASCADE`, `tenant_id FK→tenants CASCADE`, `period VARCHAR(20)` (e.g. `2026-06`), `depreciation_amount NUMERIC(18,2)`, `accumulated_depreciation NUMERIC(18,2)`, `net_book_value NUMERIC(18,2)`, `journal_entry_id FK→journal_entries SET NULL` (Full ERP), `posting_batch_id FK→posting_batches SET NULL` (Connected), `posted_at TIMESTAMPTZ NULL`. UQ: `(asset_id, period)`. | One row per asset per period. Auto-generated on period close trigger. |
| `asset_disposals` | `id UUID PK`, `asset_id FK→assets CASCADE UNIQUE`, `tenant_id FK→tenants CASCADE`, `disposal_date DATE`, `proceeds NUMERIC(18,2) DEFAULT 0`, `disposal_type VARCHAR(30)` (`SALE\|WRITE_OFF\|DONATION`), `gain_loss NUMERIC(18,2)`, `notes TEXT NULL`, `journal_entry_id FK→journal_entries SET NULL`, `posted_by FK→users SET NULL`, `created_at`. | One disposal per asset. Marks asset DISPOSED; posts gain/loss GL entry (Full ERP). |

**Depreciation formulas:**
- Straight-line: `dep_amount = (cost − residual_value) / useful_life_years / 12` per month
- Reducing balance: `dep_amount = NBV × depreciation_rate / 12` per month; edge: if NBV ≤ residual_value, dep = 0

**Three-mode GL on depreciation (Full ERP):**
DR Depreciation Expense (`gl_dep_expense_id`) / CR Accumulated Depreciation (`gl_accumulated_dep_id`)

---

### 2.23 Inventory & Warehouse Tables — M17 (migration `j8k9l0m1n2o3`, pending CC commit)

| Table | Key columns | Notes |
|---|---|---|
| `inventory_categories` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `code VARCHAR(30)`, `name VARCHAR(100)`, `description TEXT NULL`, `is_active BOOL DEFAULT TRUE`. UQ: `(tenant_id, code)`. | Item classification catalogue. |
| `inventory_locations` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `code VARCHAR(30)`, `name VARCHAR(100)`, `parent_id FK→inventory_locations SET NULL` (self-ref — nested warehouses), `is_active BOOL DEFAULT TRUE`. UQ: `(tenant_id, code)`. | Multi-level warehouse/location hierarchy. |
| `inventory_items` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `category_id FK→inventory_categories SET NULL`, `item_code VARCHAR(30)`, `name VARCHAR(200)`, `description TEXT NULL`, `unit_of_measure VARCHAR(20) DEFAULT 'PCS'`, `current_quantity NUMERIC(18,4) DEFAULT 0`, `reorder_point NUMERIC(18,4) NULL`, `reorder_quantity NUMERIC(18,4) NULL`, `standard_cost NUMERIC(18,4) DEFAULT 0`, `moving_average_cost NUMERIC(18,4) DEFAULT 0`, `valuation_method VARCHAR(8) CHECK IN ('FIFO','WACC','STANDARD') DEFAULT 'WACC'`, `gl_inventory_id FK→chart_of_accounts SET NULL`, `gl_cogs_id FK→chart_of_accounts SET NULL`, `gl_revenue_id FK→chart_of_accounts SET NULL`, `gl_ppv_id FK→chart_of_accounts SET NULL` (Purchase Price Variance, Standard costing only), `is_active BOOL DEFAULT TRUE`, `created_at`, `updated_at`. UQ: `(tenant_id, item_code)`. | Item master. `current_quantity` and `moving_average_cost` maintained by service layer. STANDARD items: `moving_average_cost` kept equal to `standard_cost`. Migration `l0m1n2o3p4q5` added `STANDARD` method, `gl_ppv_id`, and `inventory_cost_layers`. |
| `inventory_cost_layers` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `item_id FK→inventory_items CASCADE`, `receipt_movement_id FK→stock_movements SET NULL`, `received_date DATE`, `unit_cost NUMERIC(18,4)`, `quantity_received NUMERIC(18,4)`, `quantity_remaining NUMERIC(18,4)`, `created_at`. Indexes: `(tenant_id)`, `(item_id)`, `(item_id, received_date)`. | FIFO cost layers — one row per RECEIPT for FIFO-method items. ISSUE consumes layers oldest-first (`SELECT ... FOR UPDATE`). Exhausted layers (`quantity_remaining=0`) are retained for audit. WACC and STANDARD items never create rows here. |
| `stock_movements` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `item_id FK→inventory_items RESTRICT`, `location_id FK→inventory_locations SET NULL`, `movement_type VARCHAR(20) CHECK IN ('RECEIPT','ISSUE','ADJUSTMENT','TRANSFER')`, `movement_date DATE`, `reference VARCHAR(100) NULL`, `quantity NUMERIC(18,4)` (always positive), `unit_cost NUMERIC(18,4) DEFAULT 0`, `total_cost NUMERIC(18,4) DEFAULT 0`, `quantity_after NUMERIC(18,4)`, `moving_average_cost_after NUMERIC(18,4)`, `journal_entry_id FK→journal_entries SET NULL`, `ap_invoice_id FK→ap_invoices SET NULL`, `ar_invoice_id FK→ar_invoices SET NULL`, `notes TEXT NULL`, `created_by_id FK→users SET NULL`, `created_at`. | Append-only stock ledger. `quantity_after` and `moving_average_cost_after` stored at write time for fast reporting. |

**Three costing methods:**
- **WACC:** `new_mac = (old_qty × old_mac + receipt_qty × receipt_cost) / (old_qty + receipt_qty)`. Edge: `old_qty=0 → new_mac = receipt_cost`.
- **FIFO:** RECEIPT creates cost layer; ISSUE consumes layers oldest-first; unit_cost on movement = weighted average of consumed lots.
- **STANDARD:** RECEIPT values inventory at `standard_cost`; actual purchase price (body `unit_cost`) used only for PPV. ISSUE always at `standard_cost`. PPV (Full ERP + `gl_ppv_id`): unfavorable → DR PPV / CR Inventory; favorable → DR Inventory / CR PPV. 400 if `unit_cost ≤ 0` on STANDARD RECEIPT.

**Three-mode COGS on ISSUE:**
- Full ERP: DR COGS / CR Inventory via `post_journal()`. STANDARD also posts PPV journal on RECEIPT if `gl_ppv_id` set.
- Connected: `posting_batches` row with `module="inventory"`.
- Lite: quantity updates only; no GL or batch.

---

### 2.24 AI Intelligence Tables — M20 (migration `k9l0m1n2o3p4`, committed `9ffd9e0`)

| Table | Key columns | Notes |
|---|---|---|
| `ai_insights` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `insight_type VARCHAR(30) CHECK IN ('ANOMALY','CATEGORY_SUGGESTION','CASH_FLOW_FORECAST','SPENDING_PATTERN')`, `severity VARCHAR(20) CHECK IN ('INFO','WARNING','CRITICAL')`, `status VARCHAR(20) CHECK IN ('PENDING','REVIEWED','DISMISSED','ACTIONED')`, `title VARCHAR(255)`, `description TEXT`, `metadata JSONB` (type-specific detail: amount, gl_code, period, etc.), `source_reference VARCHAR(255) NULL` (e.g. AP invoice ID for AP dup anomaly), `created_at`, `reviewed_at TIMESTAMPTZ NULL`, `reviewed_by FK→users SET NULL`. | AI-generated insight audit trail. All insights are tenant-scoped. Full ERP tenants only (enforced in router). |

**AI security (`ai_intelligence.py`):**
- `AiIntelligenceError` base exception wraps all `anthropic` SDK errors, key errors, and import failures
- `_get_client()` — attempts `import anthropic`; raises `AiIntelligenceError("configuration error")` if SDK missing or `ANTHROPIC_API_KEY` blank
- Router catches `AiIntelligenceError` → returns **HTTP 503** `{"detail": "AI analysis is temporarily unavailable. Please try again later."}`
- Tenant-facing error messages NEVER reference "Anthropic", model names, API key names, or SDK internals

**Key service functions:**
- `detect_anomalies(tenant_id, db)` — 3σ outlier per GL account over 90-day window; also scans `ap_invoices` for same vendor+amount within 7 days → CRITICAL severity
- `generate_spending_patterns(tenant_id, db)` — UNION ALL expense_lines + ap_invoice_lines by GL; sends category totals to LLM; returns narrative + `ai_insights` rows with `insight_type='SPENDING_PATTERN'`
- `forecast_cash_flow(tenant_id, db)` — sums outstanding AR net_receivable by due_date vs AP net_payable by due_date; `severity='WARNING'` if net < 0 for any forward bucket
- `suggest_category(tenant_id, description, db)` — tenant CoA (columns `gl_number`/`gl_name`) passed to LLM; returns `{GL_CODE, GL_NAME, REASON}` best match. **Bug fix 2026-07-29:** was using wrong column names `code`/`name`.

---

### 2.25 ICE — AI Categorisation Engine (migration `p9q0r1s2t3u4`, pending CC commit)

Chains after M17b (`l0m1n2o3p4q5`). All tables are tenant-scoped — `tenant_id` on every row.

| Table | Key columns | Notes |
|---|---|---|
| `ice_tenant_config` | `id UUID PK`, `tenant_id FK→tenants CASCADE UNIQUE`, `ai_enabled BOOL DEFAULT false`, `enabled_fields JSONB DEFAULT {"gl":true,"cost_center":true,"category":true}`, `confidence_threshold_high SMALLINT DEFAULT 80`, `confidence_threshold_low SMALLINT DEFAULT 50`, `sensitive_gl_accounts JSONB DEFAULT []`, `allow_user_disable BOOL DEFAULT true`, `created_at`, `updated_at` | One row per tenant. Auto-created on first GET /api/ai/ice/config call. Tenant Admin configures via PATCH. |
| `vendor_behavior_profiles` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `vendor_name TEXT`, `gl_frequency JSONB DEFAULT {}`, `category_frequency JSONB DEFAULT {}`, `sample_count INT DEFAULT 0`, `last_updated`. UQ: `(tenant_id, vendor_name)` | Frequency maps updated on every ICE acceptance/correction. Used as context hint for next prediction involving same vendor. |
| `employee_behavior_profiles` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `user_id FK→users CASCADE`, `gl_frequency JSONB DEFAULT {}`, `category_frequency JSONB DEFAULT {}`, `sample_count INT DEFAULT 0`, `last_updated`. UQ: `(tenant_id, user_id)` | Same pattern as vendor profile but keyed on user. Secondary hint when vendor pattern is absent. |
| `ice_predictions` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `expense_line_id UUID NULL` (soft ref — no hard FK), `requested_by_id FK→users SET NULL`, `input_description TEXT`, `input_amount NUMERIC(18,2)`, `input_vendor_name TEXT`, `predicted_gl_id UUID NULL`, `predicted_gl_number VARCHAR(20)`, `predicted_gl_name TEXT`, `predicted_category TEXT`, `predicted_dimensions JSONB`, `confidence SMALLINT` (0–100), `confidence_band VARCHAR(6) CHECK IN ('HIGH','MEDIUM','LOW')`, `accepted BOOL NULL`, `engine_version VARCHAR(50) DEFAULT 'ice-v1'`, `created_at` | One row per prediction call. `accepted` is NULL until feedback arrives, then True/False. |
| `ice_feedback` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `prediction_id FK→ice_predictions SET NULL`, `accepted_prediction BOOL DEFAULT false`, `corrected_gl_id UUID NULL`, `corrected_gl_number VARCHAR(20)`, `corrected_gl_name TEXT`, `corrected_category TEXT`, `corrected_dimensions JSONB`, `corrected_by_id FK→users SET NULL`, `corrected_by_role VARCHAR(30)`, `correction_reason TEXT`, `vendor_name TEXT`, `created_at` | Correction or acceptance. Writing a feedback row triggers vendor/employee profile increment in service layer. |
| `ice_audit_log` | `id UUID PK`, `tenant_id FK→tenants CASCADE`, `event_type VARCHAR(30) CHECK IN ('PREDICTED','ACCEPTED','CORRECTED','ENABLED','DISABLED','CONFIG_CHANGED')`, `prediction_id UUID NULL`, `feedback_id UUID NULL`, `user_id FK→users SET NULL`, `user_role VARCHAR(30)`, `old_value JSONB`, `new_value JSONB`, `engine_version VARCHAR(50)`, `created_at` | **Append-only, never updated or deleted.** 7-year retention. Full AI influence traceability for audit. |

**ICE API endpoints (`/api/ai/ice/`):**
- `POST /api/ai/ice/predict` — returns `IcePredictResponse` (gl_number, gl_name, category, confidence 0-100, confidence_band, reason)
- `POST /api/ai/ice/feedback` — records acceptance/correction; updates profiles; returns `IceFeedbackResponse`
- `GET  /api/ai/ice/config` — returns `IceConfigResponse`; auto-creates default config if absent
- `PATCH /api/ai/ice/config` — Tenant Admin only; updates thresholds, enabled_fields, toggle
- `GET  /api/ai/ice/analytics?period_days=30` — acceptance rate, confidence distribution, top corrected GLs
- `GET  /api/ai/ice/audit-log` — Finance/Admin; newest-first; limit 200

**ICE security:** `IceServiceError` → HTTP 503 "AI analysis is temporarily unavailable. Please try again later." — Anthropic brand name, model names, API key names NEVER reach client.

---

## 3. Architectural Invariants

| Invariant | Detail |
|---|---|
| **Cost center source of truth** | Cost centers live in `org_structure` (node_type='Cost center'). `employees.cost_center_id` and `cost_center_config.cost_center_id` both FK to `org_structure.id`. `dimension_values` may have cost_center-typed rows (legacy) but are NOT read for primary cost center lookup. |
| **Currency source of truth** | `tenant_org_config.functional_currency`, `.reporting_currency`, `.enabled_currencies JSONB` are the single authority. `tenant_fx_config` holds ONLY `fx_rates` and `revaluation_rules`. GET /api/setup/currencies reads both. |
| **Standing date floor** | No transaction date may precede `tenant_org_config.date_of_registration`. |
| **lifecycle_status gates** | CoA Replace All only when `lifecycle_status='in_implementation'`. CoA Remap only when `lifecycle_status='live'`. |
| **Expense→GL posting** | Synchronous, same DB transaction as final approval. Uncoded leaf lines (`gl_id=NULL`) block posting. Split parent containers (`is_split_parent=True`) are excluded from `leaf_lines` — their split children carry the GL codes and dimension values that post to GL. Frontend validation (Q4 fix, 2026-07-24) ensures split children are fully coded before submission. |
| **GL entries immutable** | Once `status='POSTED'`, journal_entry rows may not be modified. Corrections require reversing entries. |
| **CoA retired accounts** | `is_retired=True` set on remap. These rows remain in DB for historical integrity and cannot be deleted. |
| **Segregation of duties** | Period checklist: `approved_by ≠ prepared_by` enforced in router. |
| **Test-first tenant lifecycle (M9.0.1)** | Signup creates ONLY a test tenant (`environment="test"`, `parent_tenant_id=NULL`). Live is born second, only via an explicit super-admin promotion (`parent_tenant_id` then points test→live, the inverse of the original M9.0 live-first/clone design — see `docs/BRIEF_M9_0_1_test_first_environment_flow.md`). Test stays active permanently after go-live; it is never archived. New tenants created locally before this change (e.g. the existing Red Bull pair, §7) still have the OLD direction (`test.parent_tenant_id` → live) until the retrofit script (pending, not yet run) re-points them. |

---

## 4. API Endpoint Inventory

All endpoints require JWT auth except: `/api/auth/*`, `/api/invitations/*`, `/onboard/{token}`.

### 4.1 Auth (`/api/auth`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/auth/signup | Register individual or business account (2-step: captures company_size + interested_modules) |
| POST | /api/auth/login | Email + password login. Response now includes `must_change_password: bool`. |
| POST | /api/auth/refresh-token | Rotate refresh token |
| POST | /api/auth/logout | Revoke refresh token, end session |
| POST | /api/auth/change-password | Force-change temp password. Verifies current password; rejects same-as-current; sets `must_change_password=False`. Requires auth. |
| POST | /api/auth/forgot-password | Request password reset link. Always returns 200 (prevents enumeration). Creates `password_reset_tokens` row; sends reset email via Resend. No auth required. |
| POST | /api/auth/reset-password | Reset password using token from email URL. Validates token hash + expiry + one-use guard. Updates password_hash on all UserTenant rows for the user; clears `must_change_password`. No auth required. |

### 4.2 Users (`/api/users`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/users/me | Current user profile |
| PATCH | /api/users/me | Update own profile |
| PATCH | /api/users/me/password | Change own password |
| GET | /api/users/tenant | List tenant users (approver dropdowns) |
| GET | /api/users/me/sessions | List active sessions |
| DELETE | /api/users/me/sessions/{session_id} | Revoke specific session |
| POST | /api/users/me/sessions/revoke-others | Sign out everywhere else |
| POST | /api/users/me/2fa/enroll | Generate TOTP secret |
| POST | /api/users/me/2fa/verify | Confirm code → enable 2FA |
| POST | /api/users/me/2fa/disable | Disable 2FA |

### 4.3 Expense Reports (`/api/expenses`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/expenses/reports | Create DRAFT report |
| GET | /api/expenses/reports | List reports |
| GET | /api/expenses/reports/{report_id} | Single report with lines |
| POST | /api/expenses/reports/{report_id}/lines | Add line to DRAFT |
| DELETE | /api/expenses/reports/{report_id}/lines/{line_id} | Remove line |
| PATCH | /api/expenses/reports/{report_id}/lines/{line_id} | Update line (auto-save) |
| PATCH | /api/expenses/reports/{report_id} | Update DRAFT header |
| POST | /api/expenses/reports/{report_id}/submit | Submit (legacy path) |
| GET | /api/expenses/export.csv | **Lite mode only** — download approved reports + lines as CSV (tenant_admin). Query params: `from_date`, `to_date` (default: current month). Formula-injection safe (_csv_safe). |
| GET | /api/expenses/export.xlsx | **Lite mode only** — download approved reports + lines as Excel workbook (tenant_admin). Bold headers, date/money formats, freeze pane. Same query params as CSV. |

### 4.4 Approvals (`/api/approvals`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/approvals/matrix | Create or upsert approval matrix (admin) |
| GET | /api/approvals/matrix | Get current tenant matrix |
| POST | /api/approvals/reports/{report_id}/submit | Submit report with approvers selected |
| GET | /api/approvals/queue | Reports pending current user's approval |
| GET | /api/approvals/rejected | Reports rejected involving current user |
| GET | /api/approvals/reports/{report_id}/audit-log | Event trail (admin only) |
| GET | /api/approvals/reports/{report_id}/snapshot/{version} | Immutable submission snapshot |
| GET | /api/approvals/reports/{report_id} | All approval records |
| POST | /api/approvals/{approval_id}/approve | Approve; final approval triggers GL posting |
| POST | /api/approvals/{approval_id}/reject | Reject with required comment |
| POST | /api/approvals/{approval_id}/refer-back | Refer to lower approver or requestor |
| GET | /api/approvals/roles | List approval roles for tenant (auto-seeds defaults) |
| POST | /api/approvals/roles | Create an approval role |
| PATCH | /api/approvals/roles/{role_id} | Update an approval role |
| DELETE | /api/approvals/roles | Bulk-delete ALL approval roles (admin-gated; atomic single transaction) |
| DELETE | /api/approvals/roles/{role_id} | Delete a single approval role |
| GET | /api/approvals/policies | List all approval policies for tenant |
| POST | /api/approvals/policies | Create or replace a module policy (upsert) |
| PATCH | /api/approvals/policies/{policy_id} | Partial update a policy |
| DELETE | /api/approvals/policies/{policy_id} | Delete a policy |
| GET | /api/approvals/policies/{policy_id}/finance-steps | List finance review steps for a policy |
| PUT | /api/approvals/policies/{policy_id}/finance-steps | Bulk-replace finance review steps |
| GET | /api/approvals/policies/{module}/chain-preview | Preview computed approval chain for module + amount |

### 4.4b Accounts Payable (`/api/ap`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/ap/vendors | List vendors (filter: search, include_inactive) |
| POST | /api/ap/vendors | Create vendor. Code auto-generated `V-{NNNN}` if omitted |
| GET | /api/ap/vendors/{id} | Get vendor detail |
| PATCH | /api/ap/vendors/{id} | Update vendor (PATCH semantics) |
| GET | /api/ap/invoices | List invoices (filter: status, vendor_id; paginated) |
| POST | /api/ap/invoices | Create DRAFT invoice. Reference `AP-{YYYY}-{NNNN}`. Duplicate detection by `(tenant_id, vendor_id, invoice_number)`. |
| GET | /api/ap/invoices/{id} | Invoice detail (lines + approvals) |
| PUT | /api/ap/invoices/{id} | Update DRAFT invoice (replace lines) |
| DELETE | /api/ap/invoices/{id} | Delete DRAFT invoice |
| POST | /api/ap/invoices/{id}/submit | Submit for approval. Routing: `get_policy("payable")` → `compute_chain(module="payable")`; fallback to manual `selected_approver_id`. Writes immutable snapshot. |
| POST | /api/ap/invoices/{id}/approve | Approver action. Final approve triggers GL posting (Full ERP) or posting_batch (Connected). |
| POST | /api/ap/invoices/{id}/reject | Reject with reason |
| POST | /api/ap/invoices/{id}/cancel | Cancel DRAFT or SUBMITTED invoice |
| POST | /api/ap/invoices/{id}/pay | Record payment (payment_date, payment_reference, bank_account_id). Posts DR AP / CR bank (Full ERP). |
| GET | /api/ap/aging | AP Aging report. Params: `as_at_date`. Returns 5-bucket (Current, 1–30, 31–60, 61–90, 90+) by vendor. |
| GET | /api/ap/invoices/export | CSV or XLSX export of invoices. Param: `format=csv\|xlsx` |

**Approval routing note:** AP module key in the `approval_policies` table is `"payable"` (set by the existing approval-matrix UI "Accounts Payable" entry). The router calls `get_policy("payable", …)` and `compute_chain(module="payable")`.

**GL journals (Full ERP):**
- On final approval: `DR expense GL accounts (per line) / CR accounts_payable` (via `ap_posting.post_ap_approval()`)
- On payment: `DR accounts_payable / CR bank GL` (via `ap_posting.post_ap_payment()`)

**Connected mode:** `create_ap_posting_batch()` creates a `PostingBatch` with balanced `transactions` JSONB list (DR expense / CR AP control).

### 4.4c Purchase Orders & 3-Way Match (`/api/po`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/po/ | List POs (filter: status, vendor_id; paginated limit/offset) |
| POST | /api/po/ | Create DRAFT PO. Number auto-generated `PO-{YYYY}-{NNNN}`. |
| GET | /api/po/{id} | PO detail (lines + approvals) |
| PUT | /api/po/{id} | Update DRAFT PO (replace lines) |
| DELETE | /api/po/{id} | Delete DRAFT PO |
| POST | /api/po/{id}/submit | Submit for approval. `get_policy("po")` → `compute_chain(module="po")`. Writes snapshot. Raises 422 if no PO policy configured. |
| POST | /api/po/{id}/approve | Approve (finds pending step for current user). Sets PO to APPROVED when all mandatory steps done. |
| POST | /api/po/{id}/reject | Reject with `rejection_reason`. Returns to REJECTED. |
| POST | /api/po/{id}/send | Mark APPROVED PO as SENT to vendor. |
| POST | /api/po/{id}/close | Close (SENT / PARTIALLY_RECEIVED / FULLY_RECEIVED → CLOSED). |
| POST | /api/po/{id}/cancel | Cancel (DRAFT → CANCELLED; blocked once PARTIALLY_RECEIVED or later). |
| GET | /api/po/{po_id}/grns | List GRNs for a PO |
| POST | /api/po/{po_id}/grns | Create DRAFT GRN. Over-receipt guard enforced per line. Number `GRN-{YYYY}-{NNNN}`. |
| GET | /api/po/grns/{grn_id} | GRN detail (with lines) |
| POST | /api/po/grns/{grn_id}/confirm | Confirm GRN (immutable). Increments `po_line.quantity_received`. Posts GRNI accrual (Full ERP) or posting_batch (Connected). |
| POST | /api/po/matches | Record 3-way match records (invoice_id + list of `{invoice_line_id, grn_line_id, matched_quantity}`). Match engine computes variance + status. |
| GET | /api/po/matches/{invoice_id} | Get all match records for an AP invoice |
| PATCH | /api/po/matches/{match_id}/override | Set match_status = MANUAL_OVERRIDE with comment |
| GET | /api/po/match-report | Match status summary across all invoices with match records |
| GET | /api/po/tolerance | Get this tenant's tolerance config (auto-created with defaults on first access) |
| PUT | /api/po/tolerance | Update tolerance settings (PATCH semantics, all fields optional) |

**Approval routing note:** PO module key is `"po"`. Router calls `get_policy("po", …)` and `compute_chain(module="po", submitter_user_id=..., total_amount=..., db=...)`. `ChainStep` fields used: `.level` (→ `step_order`), `.approver_user_id` (→ `approver_id`), `.is_advisory`.

**GL journals (Full ERP):**
- GRN confirmed: `DR <po_line.gl_account_id> per line / CR grni (control)` via `po_posting.post_grni_accrual()`
- Invoice match approved: `DR grni / CR accounts_payable` via `po_posting.post_grni_clearance()`

**Connected mode:** `po_posting.create_grni_posting_batch()` creates a `PostingBatch` with `module="po"` on GRN confirmation.

### 4.4e Accounts Receivable (`/api/ar`) — M14, 2026-07-28

| Method | Path | Purpose |
|---|---|---|
| GET | /api/ar/customers | List customers. Params: `search`, `include_inactive`. Outstanding balance computed at query time from APPROVED unreceived invoices. |
| POST | /api/ar/customers | Create customer. Code auto-generated `C-{NNNN}` if omitted. |
| GET | /api/ar/customers/{id} | Customer detail |
| PATCH | /api/ar/customers/{id} | Update customer (PATCH semantics, including is_active toggle) |
| GET | /api/ar/invoices | List invoices. Params: `status`, `customer_id`. |
| GET | /api/ar/invoices/export | CSV or XLSX export. Param: `format=csv\|xlsx`. **Must be registered before `/invoices/{id}` in router.** |
| POST | /api/ar/invoices | Create DRAFT invoice. Reference `AR-{YYYY}-{NNNN}`. Due date auto-computed from customer credit_terms if blank. Duplicate detection by `(tenant_id, customer_id, invoice_number)` — sets `duplicate_flag=True` (non-blocking). |
| GET | /api/ar/invoices/{id} | Invoice detail (customer + lines + approvals) |
| PUT | /api/ar/invoices/{id} | Update DRAFT invoice (replace lines) |
| DELETE | /api/ar/invoices/{id} | Delete DRAFT invoice |
| POST | /api/ar/invoices/{id}/submit | Submit for approval. Routing: `get_policy("receivable")` → `compute_chain(module="receivable")`; fallback to manual `selected_approver_id`. Writes immutable `snapshot_json`. Snaps `posting_mode` from tenant config. |
| POST | /api/ar/invoices/{id}/approve | Approver action. Final approve → status=APPROVED → `post_ar_approval()` (Full ERP DR AR/CR revenue GL per line) or `create_ar_posting_batch()` (Connected). |
| POST | /api/ar/invoices/{id}/reject | Reject with reason |
| POST | /api/ar/invoices/{id}/cancel | Cancel DRAFT or SUBMITTED invoice |
| POST | /api/ar/invoices/{id}/receive | Record receipt (receipt_date, receipt_reference, bank_account_id). APPROVED → RECEIVED. Full ERP: `post_ar_receipt()` DR bank / CR AR. |
| GET | /api/ar/aging | AR Aging report. Params: `as_at_date`. Returns 5-bucket (Current, 1–30, 31–60, 61–90, 90+) by customer. APPROVED unreceived invoices only. |

**Approval routing note:** AR module key in `approval_policies` is `"receivable"`. Router calls `get_policy("receivable", …)` and `compute_chain(module="receivable")`.

**GL journals (Full ERP):**
- On final approval: `DR accounts_receivable / CR revenue GL per line` (via `ar_posting.post_ar_approval()`)
- On receipt: `DR bank GL / CR accounts_receivable` (via `ar_posting.post_ar_receipt()`)

**Connected mode:** `create_ar_posting_batch()` creates a `PostingBatch` with `module="ar"`.

**Route ordering note:** `GET /api/ar/invoices/export` is registered BEFORE `GET /api/ar/invoices/{id}` in `routers/ar.py` to prevent FastAPI treating "export" as a UUID.

### 4.4f Bank Reconciliation (`/api/bank-recon`)

**Migration:** `a9b0c1d2e3f4` (3 tables)

| Method | Path | Purpose |
|---|---|---|
| GET | /api/bank-recon/statements | List statements (filter: bank_account_id, status) |
| POST | /api/bank-recon/statements | Create statement header |
| GET | /api/bank-recon/statements/{id} | Detail with lines + matches |
| DELETE | /api/bank-recon/statements/{id} | Delete DRAFT only |
| POST | /api/bank-recon/statements/{id}/upload | Parse CSV/XLSX and bulk-insert lines (replaces existing) |
| POST | /api/bank-recon/statements/{id}/close | Mark RECONCILED (guard: no UNMATCHED/PARTIAL lines) |
| GET | /api/bank-recon/statements/{id}/report | Reconciliation report (Full ERP: GL balance + outstanding items) |
| GET | /api/bank-recon/statements/{id}/candidates/gl | Unmatched GL journal lines for this bank account (Full ERP) |
| GET | /api/bank-recon/statements/{id}/candidates/batches | Unmatched posting batches (Connected) |
| POST | /api/bank-recon/matches | Create match (match_type: journal_line / posting_batch / manual) |
| DELETE | /api/bank-recon/matches/{match_id} | Remove match (recomputes line status) |
| POST | /api/bank-recon/statements/{id}/auto-match | Auto-match all UNMATCHED lines (query param: date_tolerance_days=5) |
| PUT | /api/bank-recon/lines/{line_id}/exclude | Mark line EXCLUDED |
| PUT | /api/bank-recon/lines/{line_id}/unexclude | Revert EXCLUDED → recomputed status |

**Three-mode behaviour:**
- Lite: CSV import + manual matches only. Auto-match returns 0 matches. No GL/batch candidates.
- Connected: Auto-match against `posting_batches` (amount ± tolerance, date ± window). Candidates/batches endpoint active.
- Full ERP: Auto-match against `journal_lines` tagged with `bank_account_id`. GL candidates endpoint active. Report includes `gl_book_balance`, `outstanding_deposits`, `outstanding_payments`, `adjusted_gl_balance`, `is_balanced`.

**Reconciliation equation (Full ERP):**
`GL book balance + Σ outstanding deposits − Σ outstanding payments = bank closing balance`
`is_balanced = abs(adjusted_gl_balance − closing_balance) ≤ 0.01`

### 4.5 Tenant Management (`/api/tenant`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/tenant/users | List all users in tenant |
| GET | /api/tenant/users/{user_id} | Get single user detail |
| PATCH | /api/tenant/users/{user_id}/roles | Replace user roles |
| PATCH | /api/tenant/users/{user_id}/deactivate | Soft-deactivate user |
| PATCH | /api/tenant/users/{user_id}/reactivate | Reactivate user |
| POST | /api/tenant/invitations | Send invitation (Resend — wired via `services/email.py`; `suppress_outbound_email` flag respected) |
| GET | /api/tenant/invitations | List invitations |
| DELETE | /api/tenant/invitations/{invite_id} | Cancel pending invitation |
| POST | /api/tenant/create-test-environment | Create/retrieve shadow test tenant (live tenants only — under test-first, a fresh tenant already starts as test) |
| POST | /api/tenant/promote | **DEPRECATED M9.0.1 — returns 410 Gone.** Superseded by `/api/platform/tenants/{id}/promotion/diff`+`/apply` (§4.15), which now also handles a tenant's first promotion (live creation). |
| POST | /api/tenant/purge-test-data | **STUB — no-op** |

### 4.6 Invitations (`/api/invitations`) — Public
| Method | Path | Purpose |
|---|---|---|
| GET | /api/invitations/validate/{token} | Validate token |
| POST | /api/invitations/accept/{token} | Create account + auto-login |

### 4.7 Documents (`/api/documents`)
| Method | Path | Purpose |
|---|---|---|
| POST | /api/documents/reports/{report_id}/upload | Upload file. Magic bytes + ZIP structure validation. Accepted: PDF, JPEG, PNG, GIF, WEBP, DOCX, XLSX (macro-free). Images → WebP compression (Pillow). PDFs → pikepdf compression. Hash dedup within tenant. Sets retain_until from tenant_org_config.document_retention_years. Logs 'upload' to document_access_log. Max 10 MB images / 20 MB PDF+Office. |
| GET | /api/documents/reports/{report_id} | List docs with 15-min signed URLs. Logs 'view' per document to document_access_log. |
| DELETE | /api/documents/{document_id} | Delete. Permission: uploader or tenant admin. Status guard: DRAFT/REJECTED/REFERRED only. Retention guard: 403 if retain_until > today() or IS NULL. Dedup-safe: skips Supabase blob delete if other rows share storage_path. Logs 'delete'. |

### 4.8 Expense Config (`/api/expense-config`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/expense-config | Get coding config |
| POST | /api/expense-config | Upsert coding config (admin) |
| GET | /api/expense-config/categories | Category tree |
| POST | /api/expense-config/categories | Create category (admin) |
| PATCH | /api/expense-config/categories/{id} | Update category (admin) |
| DELETE | /api/expense-config/categories/{id} | Soft-delete + subcategories (admin) |
| GET | /api/expense-config/form-config | Combined config + dims + categories for form |
| POST | /api/expense-config/reports/{report_id}/gl-codes | Finance GL coding batch update |

### 4.9 Master Data Config (`/api/config`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/config/dimensions | List dimensions |
| GET | /api/config/dimensions/org-structure-preview | Cost center nodes |
| POST | /api/config/dimensions | Create dimension (admin) |
| GET | /api/config/dimensions/{id}/inline-values | Merged values from all sources |
| PATCH | /api/config/dimensions/{id} | Update dimension (admin) |
| DELETE | /api/config/dimensions/{id} | Soft delete (admin) |
| PATCH | /api/config/dimensions/{id}/reactivate | Reactivate (admin) |
| DELETE | /api/config/dimensions/{id}/permanent | Hard delete (admin) |
| POST | /api/config/dimensions/{id}/reorder | Update sort_order (admin) |
| GET | /api/config/dimensions/{id}/values | List values |
| POST | /api/config/dimensions/{id}/values | Add value (admin) |
| PATCH | /api/config/dimensions/{id}/values/{vid} | Update value (admin) |
| DELETE | /api/config/dimensions/{id}/values/{vid} | Soft delete value (admin) |
| POST | /api/config/dimensions/{id}/values/upload | Bulk upload (admin) |
| GET | /api/config/coa | List GL accounts (paginated, searchable) |
| POST | /api/config/coa | Create GL account (admin) |
| PATCH | /api/config/coa/{id} | Update GL account (admin) |
| DELETE | /api/config/coa/{id} | Soft delete (admin) |
| GET | /api/config/coa/template | Download xlsx template |
| POST | /api/config/coa/upload | Bulk upload (admin) |
| PATCH | /api/config/coa/{id}/dimensions | Set dimension requirements (admin) |
| POST | /api/config/coa/replace-all | Deactivate all + reimport (**in_implementation** only) |
| POST | /api/config/coa/remap | Retire old codes → new code (**live** only) |
| GET | /api/config/categories | Category tree with GL mappings |
| POST | /api/config/categories | Create category (admin) |
| PATCH | /api/config/categories/{id} | Update (admin) |
| DELETE | /api/config/categories/{id} | Soft delete (admin) |
| POST | /api/config/categories/{id}/gl-mappings | Add GL to subcategory (admin) |
| DELETE | /api/config/categories/{id}/gl-mappings/{gl_id} | Remove GL mapping (admin) |
| PATCH | /api/config/categories/{id}/gl-mappings/{gl_id} | Set/unset default GL (admin) |
| GET | /api/config/gl/search | Search GL accounts (?q=, ?gl_group=, ?gl_subgroup=, ?gl_sub_subgroup=, ?limit= default 50 max 200) — `gl_group/subgroup/sub_subgroup` are exact-match filters added in task #52 |
| GET | /api/config/gl/groups | Return full GL group → subgroup → sub_subgroup tree for the tenant (tenant-scoped; only accounts where `gl_group IS NOT NULL`) |

### 4.10 HR (`/api/hr`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/hr/employees | List employees (paginated) |
| POST | /api/hr/employees | Create employee (admin) |
| PATCH | /api/hr/employees/{id} | Update employee (admin) |
| DELETE | /api/hr/employees/{id} | Soft delete (admin) |
| POST | /api/hr/employees/upload | Bulk upload (admin) |
| GET | /api/hr/employees/template | Download template |
| POST | /api/hr/employees/{id}/transfer | Transfer cost center (admin) |
| POST | /api/hr/employees/{id}/update-code | Change employee code (admin) |
| GET | /api/hr/employees/{id}/history | Code + transfer history |
| POST | /api/hr/employees/invite | Send self-onboarding invite (admin) ⚠️ prints link to stdout |
| POST | /api/hr/employees/{id}/approve-onboarding | Approve onboarding (admin) |
| POST | /api/hr/employees/{id}/reject-onboarding | Reject onboarding (admin) |
| GET | /api/hr/cost-centers | List cost centers with head assignments |
| PUT | /api/hr/cost-centers/{id}/head | Set cost center head (admin) |
| GET | /api/hr/finance-review | List finance reviewers |
| POST | /api/hr/finance-review | Add reviewer (admin) |
| PATCH | /api/hr/finance-review/{id} | Update reviewer (admin) |
| DELETE | /api/hr/finance-review/{id} | Remove reviewer (admin) |
| GET | /onboard/{token} | Validate token (public) |
| POST | /onboard/{token} | Submit self-onboarding (public) |

### 4.11 Setup / Implementation Portal (`/api/setup`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/setup/progress | Setup dashboard completion |
| GET/PATCH | /api/setup/org | Org config |
| GET/POST/PATCH/DELETE | /api/setup/org-structure[/{id}] | Org tree CRUD |
| GET | /api/setup/org-structure/template | Download xlsx template |
| POST | /api/setup/org-structure/upload | Upload from xlsx |
| POST | /api/setup/periods/generate | Generate monthly periods for a FY (deprecated — auto-generation preferred; still works as manual override) |
| DELETE | /api/setup/periods/fiscal-year/{fiscal_year} | Delete all periods for a FY label + its FiscalYearState (blocked if any period is SOFT_CLOSED, OVERDUE, or HARD_CLOSED) |
| GET | /api/setup/periods | List periods |
| GET | /api/setup/periods/check | Postability check (?date=) |
| POST | /api/setup/periods/{id}/soft-close | Soft-close period |
| POST | /api/setup/periods/{id}/hard-close | Hard-close (sequential enforcement) |
| POST | /api/setup/periods/{id}/reopen | Reopen (**super_admin** only + audit log) |
| POST | /api/setup/periods/management-close | Stage 1 year-end close |
| POST | /api/setup/periods/statutory-close | Stage 2 permanent year lock |
| GET | /api/setup/periods/year-state | FiscalYearState |
| PATCH | /api/setup/periods/year-state/{fy} | Update audit grace months |
| GET | /api/setup/periods/audit-log | Period audit log |
| GET/POST/PATCH/DELETE | /api/setup/periods/checklist[/{id}] | Close checklist template CRUD |
| GET | /api/setup/periods/{id}/checklist | Per-period checklist with completion |
| POST | /api/setup/periods/{id}/checklist/{item_id}/prepare | Mark prepared |
| POST | /api/setup/periods/{id}/checklist/{item_id}/approve | Mark approved (SOD enforced; SA bypass → CONSULTANT_OVERRIDE audit log) |
| GET | /api/setup/periods/future-exception | List all future posting exceptions for tenant, newest first (added 2026-07-15) |
| GET/PATCH | /api/setup/modules | Module activation. GET: all modules with `is_licensed` + `is_active`. PATCH: toggles `is_active` on licensed modules — guard `_require_admin` (power_admin+). Cannot activate unlicensed (403). Licensing is SA-only via `PATCH /api/platform/tenants/{id}/system-config`. |
| POST | /api/setup/dimensions/not-applicable | Mark not using dimensions |
| GET/PATCH | /api/setup/currencies | FX config |
| GET/PATCH | /api/setup/tax | Tax config |
| GET/PATCH | /api/setup/roles/matrix | Permission matrix |
| GET/POST/PATCH | /api/setup/roles/assignments[/{id}] | Role tier assignments |
| GET/POST/PATCH/DELETE | /api/setup/documents[/{id}] | Document rules |
| POST | /api/setup/go-live | Mark tenant live (**super_admin** only). M9.0.1: guarded — 400 if `tenant.environment != "live"` (a test tenant must go through promotion first; see `/api/platform/tenants/{id}/promotion/apply`). |

### 4.12 Account Mapping (`/api/setup/account-mapping`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/setup/account-mapping/roles | Full posting role catalogue + tenant mapping |
| PUT | /api/setup/account-mapping/{role_key} | Upsert GL mapping |
| DELETE | /api/setup/account-mapping/{role_key} | Remove GL mapping |
| PUT | /api/setup/account-mapping/{role_key}/control | Per-tenant control-account override (**super_admin** only) |

### 4.13 Bank Accounts (`/api/setup/bank-accounts`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/setup/bank-accounts | List bank accounts |
| POST | /api/setup/bank-accounts | Create (GL must be BS/SOFP) |
| PUT | /api/setup/bank-accounts/{id} | Update |
| DELETE | /api/setup/bank-accounts/{id} | Hard/soft delete based on journal line references |

### 4.14 GL Reporting (`/api/gl`)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/gl/trial-balance | Trial balance (?date_from, ?date_to, ?include_zero) |
| GET | /api/gl/accounts/{gl_account_id}/ledger | Account ledger (?date_from, ?date_to, ?dimension filters) |
| GET | /api/gl/financial-statements/pl | P&L grouped by fs_head → fs_note. ?date_from, ?date_to. Returns PLResponse: sections (FSSection list), net_income, has_unmapped. amount = credit − debit. Full ERP primary use case. |
| GET | /api/gl/financial-statements/bs | Balance Sheet (cumulative). ?as_at_date. Returns BSResponse: sections, has_unmapped. Same sign convention as P&L. |
| GET | /api/gl/journal-entries | List journal entries — up to 200, most recent first. Any tenant user. Filters: ?date_from, ?date_to, ?status |
| GET | /api/gl/journal-entries/{entry_id} | Single entry with full line detail (gl_number, gl_name enriched). Returns 404 if not found or different tenant. |
| POST | /api/gl/journal-entries | Create manual journal entry. Wraps `post_journal()`. Admin-only. Lite mode: 400. Body: `ManualJournalCreate`. Returns `JournalEntryOut`. |

### 4.15 Platform / Owner Portal (`/api/platform`) — Super Admin Only
| Method | Path | Purpose |
|---|---|---|
| GET | /api/platform/tenants | List tenants (?environment=, ?lifecycle_status=, ?search=). Response includes `is_internal` flag per row. |
| GET | /api/platform/tenants/{tenant_id} | Tenant detail + users + modules + `test_environment`/`live_environment` summaries (M9.0.1: bidirectional — populated whichever side you're viewing from) |
| PATCH | /api/platform/tenants/{tenant_id}/lifecycle | Transition lifecycle state |
| POST | /api/platform/tenants/{tenant_id}/suspend | Suspend tenant |
| POST | /api/platform/tenants/{tenant_id}/reactivate | Restore prior lifecycle |
| POST | /api/platform/tenants/{tenant_id}/enter | Mint impersonation token (implementation or support mode) |
| POST | /api/platform/tenants/{tenant_id}/promote | **DEPRECATED M9.0.1 — returns 410 Gone.** Found mid-implementation as a third, undocumented org/tax/fx-copy implementation (super-admin proxy) duplicating both `/api/tenant/promote` and the unified engine below; deprecated rather than removed. |
| POST | /api/platform/tenants/{tenant_id}/promotion/diff | Compute test↔live diff. M9.0.1: unified — works for both first promotion (no live yet, every item is a CREATE) and repeat promotion (existing live). Accepts either the test or live tenant_id; resolves the pair internally (`_resolve_promotion_pair`). |
| POST | /api/platform/tenants/{tenant_id}/promotion/apply | Apply accepted diff items. M9.0.1: on first promotion, creates the live tenant (`_create_live_from_test`), mirrors every UserTenant row test→live (auto-grants access), and copies org_config/tax/fx (`_copy_flat_config`) unconditionally on every call — repeat or first. |
| GET | /api/platform/tenants/{tenant_id}/system-config | Returns `SystemConfigResponse`: `posting_mode` + all 13 module license rows. SA-only. Committed `803618e`. |
| PATCH | /api/platform/tenants/{tenant_id}/system-config | Upserts `posting_mode` and/or module `is_licensed` flags. Revoking license auto-sets `is_active=False`. Writes audit log. SA-only. **Posting mode guard (pending commit):** returns 409 if tenant has any posted `journal_entries` and mode is being changed. |
| POST | /api/platform/tenants | SA-only: create a new tenant directly. Body: `company_name`, `company_country`, `admin_email`, `posting_mode`, `is_internal` (bool, default false), `initial_modules[]`. **No `admin_password` in request** — backend auto-generates a secure 14-char temporary password via `secrets.choice()` and returns it in `CreateTenantResponse.temp_password` (shown once in modal, then gone). Auto-creates test tenant + `TenantOrgConfig` + `power_admin` UserTenant with `must_change_password=True`. Audit-logged. `is_internal` flag marks Ziva BI internal sandboxes vs real clients. |
| DELETE | /api/platform/tenants/{tenant_id} | SA-only: hard-delete tenant and ALL child data. Resolves paired environment and deletes both test+live if paired. Requires `?confirm=true`. Guards: cannot delete a `'live'` lifecycle tenant. Committed `946aa16` (single), extended `c6d05ee` (paired). |
| GET | /api/platform/trials | List all `lifecycle_status='trial'` tenants (?search=, ?lead_status=). SA-only. Committed `8dc89be`. |
| PATCH | /api/platform/trials/{tenant_id} | Update `lead_status` and/or `implementation_notes` on a trial tenant. SA-only. Committed `8dc89be`. |
| PATCH | /api/platform/tenants/{tenant_id}/billing | SA-B-lite: manually set `plan` (free\|starter\|growth\|enterprise; null = free) and/or `paid_since` (date; null = not paying). SA-only. Audit-logged. Uses `model_fields_set` so `plan=null` correctly clears back to free. Migration `x6y7z8a9b0c1`. |
| GET | /api/platform/config | List all `platform_config` rows (`[{key, value, description}]`). SA-only. |
| PATCH | /api/platform/config/{key} | Update a platform config value. 404 for unknown keys (must be added via migration). 422 for blank value. Writes audit log `platform.config.updated`. SA-only. |


### 4.17a App Config (Public)
| Method | Path | Purpose |
|---|---|---|
| GET | /api/app-config | Returns `{ app_name: str }` — no auth required. Read by root layout server-side with 5-min revalidation. Backend serves from 5-min in-process cache (`app/services/platform_config.py`). |

### 4.16 Posting Batches (`/api/posting-batches`) — Connected Mode Export Queue
| Method | Path | Purpose |
|---|---|---|
| GET | /api/posting-batches | List batches for current tenant (?status=, ?module=) |
| GET | /api/posting-batches/{id} | Single batch detail |
| POST | /api/posting-batches/{id}/export | Mark batch as exported (download payload) |
| POST | /api/posting-batches/{id}/mark-synced | Mark batch synced after ERP confirmation |

### 4.17 Health
| Method | Path | Purpose |
|---|---|---|
| GET | /api/health | DB connectivity check (public) |

### 4.18 Budget & Planning (`/api/budgets`) — M16, pending CC commit

| Method | Path | Purpose |
|---|---|---|
| GET | /api/budgets/periods | List budget periods (filter: fiscal_year, status) |
| POST | /api/budgets/periods | Create budget period (admin). Version 1=BP, 2=FRE, 3=SRE |
| GET | /api/budgets/periods/{id} | Budget period detail with lines |
| PATCH | /api/budgets/periods/{id} | Update period status (DRAFT→ACTIVE→CLOSED) |
| DELETE | /api/budgets/periods/{id} | Delete DRAFT period only |
| GET | /api/budgets/periods/{id}/lines | List budget lines for period |
| POST | /api/budgets/periods/{id}/lines | Create budget line (GL account + dimension + amount) |
| PUT | /api/budgets/periods/{id}/lines | Bulk-replace all lines for period |
| PATCH | /api/budgets/lines/{line_id} | Update single budget line |
| DELETE | /api/budgets/lines/{line_id} | Delete budget line |
| GET | /api/budgets/periods/{id}/variance | Variance report: budget vs actuals. Params: `date_from`, `date_to`. Actuals sourced per three-mode routing in `budget_service.py`. |

### 4.19 SA Billing & Subscriptions (`/api/sa/billing`) — SA-B, pending CC commit

| Method | Path | Purpose |
|---|---|---|
| GET | /api/sa/billing/plans | List all pricing plans |
| POST | /api/sa/billing/plans | Create pricing plan (SA only) |
| PATCH | /api/sa/billing/plans/{id} | Update plan details |
| GET | /api/sa/billing/subscriptions | List all tenant subscriptions (?status=, ?search=) |
| GET | /api/sa/billing/subscriptions/{tenant_id} | Single tenant subscription detail |
| POST | /api/sa/billing/subscriptions/{tenant_id} | Create or update subscription for tenant (SA sets plan + status + trial_ends_at) |
| POST | /api/sa/billing/events | Record a billing event (payment, refund, plan change) for a tenant |
| GET | /api/sa/billing/events/{tenant_id} | Billing event history for a tenant |

**All endpoints:** `require_super_admin()` — tenants can never access billing data.

### 4.20 Tax Engine (`/api/tax`) — M19, pending CC commit

| Method | Path | Purpose |
|---|---|---|
| GET | /api/tax/returns | List tax returns (filter: tax_type, status, period) |
| POST | /api/tax/returns | Create DRAFT tax return |
| GET | /api/tax/returns/{id} | Return detail |
| PATCH | /api/tax/returns/{id} | Update return (status, payment_date, reference) |
| POST | /api/tax/returns/{id}/file | Mark return as FILED |
| POST | /api/tax/returns/{id}/pay | Record payment against return |
| GET | /api/tax/summary/vat | VAT input/output summary for a period. Params: `period_start`, `period_end`. Uses `tax_compute_service.compute_vat_summary()`. |
| GET | /api/tax/summary/wht | WHT summary by vendor for a period. Uses `compute_wht_summary()`. |
| GET | /api/tax/summary/paye | PAYE summary for a period. Uses `compute_paye_summary()`. |
| GET | /api/tax/wht-certificates | List WHT certificates (filter: vendor_id, period) |
| POST | /api/tax/wht-certificates | Create WHT certificate manually |
| GET | /api/tax/wht-certificates/{id} | Certificate detail |

### 4.21 Payroll & HR (`/api/payroll`) — M15, pending CC commit

| Method | Path | Purpose |
|---|---|---|
| GET | /api/payroll/salary-structures | List salary structures |
| POST | /api/payroll/salary-structures | Create salary structure (admin) |
| GET | /api/payroll/salary-structures/{id} | Structure detail |
| PATCH | /api/payroll/salary-structures/{id} | Update structure |
| DELETE | /api/payroll/salary-structures/{id} | Soft-delete (deactivate) |
| GET | /api/payroll/runs | List payroll runs (filter: status, pay_period) |
| POST | /api/payroll/runs | Create DRAFT payroll run; compute lines for all active employees |
| GET | /api/payroll/runs/{id} | Run detail with payroll lines |
| POST | /api/payroll/runs/{id}/approve | Approve run. Full ERP: posts DR salary / CR payroll payable + statutory deduction JE. |
| POST | /api/payroll/runs/{id}/pay | Mark run PAID; records payment_date |
| GET | /api/payroll/runs/{id}/payslips | List payslips for run |
| GET | /api/payroll/payslips/{id} | Single payslip detail |
| GET | /api/payroll/leave-types | List leave types |
| POST | /api/payroll/leave-types | Create leave type (admin) |
| PATCH | /api/payroll/leave-types/{id} | Update leave type |
| GET | /api/payroll/leave-requests | List leave requests (filter: employee_id, status) |
| POST | /api/payroll/leave-requests | Apply for leave (employee self-service) |
| POST | /api/payroll/leave-requests/{id}/approve | Approve leave (manager/admin); updates `leave_balances.days_taken` |
| POST | /api/payroll/leave-requests/{id}/reject | Reject leave |
| GET | /api/payroll/leave-balances | Leave balance summary for all employees (admin) or self |

### 4.22 Fixed Assets (`/api/assets`) — M18, pending CC commit

| Method | Path | Purpose |
|---|---|---|
| GET | /api/assets/categories | List asset categories |
| POST | /api/assets/categories | Create category (admin) |
| PATCH | /api/assets/categories/{id} | Update category |
| DELETE | /api/assets/categories/{id} | Soft-delete category |
| GET | /api/assets | List assets (filter: category_id, status) |
| POST | /api/assets | Create asset. Number auto-generated `FA-{NNNN}`. |
| GET | /api/assets/{id} | Asset detail with depreciation schedule |
| PATCH | /api/assets/{id} | Update asset metadata |
| GET | /api/assets/{id}/schedule | Depreciation schedule (all periods) |
| POST | /api/assets/{id}/depreciate | Run depreciation for a period (posts JE in Full ERP) |
| POST | /api/assets/{id}/dispose | Record disposal; computes gain/loss; posts JE (Full ERP) |
| GET | /api/assets/report/register | Full asset register with NBV, accumulated dep, age |

### 4.23 Inventory & Warehouse (`/api/inventory`) — M17, pending CC commit

| Method | Path | Purpose |
|---|---|---|
| GET | /api/inventory/categories | List inventory categories |
| POST | /api/inventory/categories | Create category |
| PATCH | /api/inventory/categories/{id} | Update category |
| GET | /api/inventory/locations | List locations (hierarchical) |
| POST | /api/inventory/locations | Create location |
| PATCH | /api/inventory/locations/{id} | Update location |
| GET | /api/inventory/items | List items (filter: category_id, low_stock=true) |
| POST | /api/inventory/items | Create item |
| GET | /api/inventory/items/{id} | Item detail with current qty + MAC |
| PATCH | /api/inventory/items/{id} | Update item metadata |
| GET | /api/inventory/items/{id}/movements | Movement history for an item |
| POST | /api/inventory/movements | Record stock movement (RECEIPT/ISSUE/ADJUSTMENT/TRANSFER). RECEIPT: recalculates WACC. ISSUE: posts COGS JE (Full ERP) or batch (Connected). |
| GET | /api/inventory/movements | List movements (filter: item_id, movement_type, date_from, date_to) |
| GET | /api/inventory/report/valuation | Stock valuation report: all items × current_qty × MAC = total value |

### 4.24 AI Intelligence (`/api/ai`) — M20 extensions (committed `9ffd9e0`)

The existing `/api/ai/ocr` and `/api/ai/override` endpoints (M10) remain. M20 extends the router:

| Method | Path | Purpose |
|---|---|---|
| POST | /api/ai/detect-anomalies | Run anomaly scan for current tenant. 3σ outlier detection per GL + AP dup scan. Returns list of `AiInsight` objects. Full ERP only. |
| POST | /api/ai/spending-patterns | Generate spending pattern analysis via LLM. Saves `SPENDING_PATTERN` insight rows. Full ERP only. |
| POST | /api/ai/forecast | Generate AR/AP cash flow forecast. Saves `CASH_FLOW_FORECAST` insight rows. Full ERP only. |
| POST | /api/ai/classify | Auto-classify a text description → best GL account. Body: `{description: str}`. Returns `{gl_code, gl_name, reason}`. Full ERP only. |
| GET | /api/ai/insights | List AI insights for tenant. Filter: `insight_type`, `severity`, `status`. |
| POST | /api/ai/insights/{id}/review | Mark insight as REVIEWED |
| POST | /api/ai/insights/{id}/dismiss | Mark insight as DISMISSED |

**All AI endpoints:** `AiIntelligenceError` → HTTP 503 "AI analysis is temporarily unavailable. Please try again later." — no internal names exposed to tenants.

### 4.25 ICE AI Categorisation (`/api/ai/ice`) — pending CC commit

| Method | Path | Purpose |
|---|---|---|
| POST | /api/ai/ice/predict | GL + category prediction. Body: `{description, amount, vendor_name, expense_line_id?}`. Returns `{prediction_id, predicted_gl_number, predicted_gl_name, predicted_category, confidence 0-100, confidence_band HIGH/MEDIUM/LOW, reason}`. |
| POST | /api/ai/ice/feedback | Accept or correct a prediction. Body: `{prediction_id, accepted, corrected_gl_id?, corrected_category?, corrected_by_role, vendor_name?}`. Updates vendor + employee profiles. |
| GET | /api/ai/ice/config | Tenant ICE config (auto-created on first call). All authenticated users. |
| PATCH | /api/ai/ice/config | Update ICE config. Tenant Admin only. |
| GET | /api/ai/ice/analytics | Acceptance rate, confidence distribution, top corrected GLs. Finance/Admin only. `?period_days=30`. |
| GET | /api/ai/ice/audit-log | Immutable event log (PREDICTED/ACCEPTED/CORRECTED/ENABLED/DISABLED/CONFIG_CHANGED). Finance/Admin only. |

**Security:** `IceServiceError` → HTTP 503 "AI analysis is temporarily unavailable. Please try again later." — Anthropic brand, model names, API key names NEVER exposed.

---

## 5. Frontend Page Map

| Route | Purpose |
|---|---|
| `/` | Root → redirects to login |
| `/auth/login` | Login page. On login: if `must_change_password=true`, redirects to `/auth/change-password` before dashboard. |
| `/auth/signup` | Business signup with geolocation + currency detection. 2-step: Step 1 = account creation; Step 2 = intent fields (company_size, interested_modules). |
| `/auth/change-password` | Force-change page (pending commit). Shown when `must_change_password=true`. Amber "Action required" banner. Cannot be skipped. Calls `POST /api/auth/change-password`. |
| `/invite/accept` | Invitation accept |
| `/onboard/[token]` | New hire self-onboarding (public) |
| `/dashboard/business` | Main dashboard → redirects to setup |
| `/dashboard/business/setup` | Implementation portal dashboard |
| `/dashboard/business/setup/organisation` | Org identity + structure tree + fiscal year |
| `/dashboard/business/setup/modules` | Module activation |
| `/dashboard/business/setup/modules/[module]` | Module detail |
| `/dashboard/business/setup/periods` | Period management |
| `/dashboard/business/setup/currencies` | Currencies & FX (4 tabs; UI complete, backend JSONB-based) |
| `/dashboard/business/setup/tax` | Tax & statutory config |
| `/dashboard/business/setup/roles` | Roles & permissions |
| `/dashboard/business/setup/documents` | Document rules |
| `/dashboard/business/setup/account-mapping` | Posting role → GL mapping |
| `/dashboard/business/setup/bank-accounts` | Bank accounts register |
| `/dashboard/business/setup/go-live` | Go-live readiness |
| `/dashboard/business/expenses` | Expense report list |
| `/dashboard/business/expenses/new` | Create expense report |
| `/dashboard/business/expenses/[report_id]` | Report detail view |
| `/dashboard/business/expenses/[report_id]/edit` | Edit DRAFT (full M9 form) |
| `/dashboard/business/expenses/[report_id]/audit` | Audit trail |
| `/dashboard/business/expenses/[report_id]/snapshot/[version]` | Immutable snapshot |
| `/dashboard/business/approvals` | Approval queue |
| `/dashboard/business/admin/users` | User management |
| `/dashboard/business/settings/dimensions` | Dimension setup |
| `/dashboard/business/settings/dimensions/[id]/values` | Dimension values |
| `/dashboard/business/settings/chart-of-accounts` | CoA management |
| `/dashboard/business/settings/employees` | Employee management |
| `/dashboard/business/settings/cost-centers` | Cost center heads |
| `/dashboard/business/settings/expense-categories` | Expense categories |
| `/dashboard/business/settings/expense-config` | Expense coding config |
| `/dashboard/business/settings/finance-review` | Finance reviewer config |
| `/dashboard/business/settings/approval-matrix` | Approval matrix |
| `/dashboard/business/ar/invoices` | AR invoice list: status filter tabs (All/DRAFT/SUBMITTED/APPROVED/RECEIVED/REJECTED/CANCELLED), DUP badge, overdue days in red, Export (CSV/XLSX) |
| `/dashboard/business/ar/invoices/new` | New AR invoice form: customer selector, service period fields, line items with Revenue GL picker, net_receivable = amount_base + vat_amount − wht_amount |
| `/dashboard/business/ar/invoices/[id]` | AR invoice detail: Submit/Approve/Reject/Record Receipt/Cancel actions; receipt modal (date + reference + bank account); shows journal_entry_id + receipt_journal_entry_id |
| `/dashboard/business/ar/customers` | Customer list: search, include_inactive toggle, outstanding balance, inline active/deactivate toggle, Create Customer modal |
| `/dashboard/business/ar/aging` | AR Aging report: as-at-date input, 5-bucket table (Current → 90+), AgingCell dims zero values |
| `/dashboard/business/payroll` | Payroll runs list: status filter, create run modal, approve/pay actions (M15, pending CC commit) |
| `/dashboard/business/payroll/salary-structures` | Salary structure CRUD: list + create/edit modal (M15, pending CC commit) |
| `/dashboard/business/payroll/leave` | Leave requests list + leave balance summary; apply/approve/reject actions (M15, pending CC commit) |
| `/dashboard/business/tax` | Tax returns list by type (VAT/WHT/PAYE); file + record payment actions (M19, pending CC commit) |
| `/dashboard/business/tax/vat-summary` | VAT input/output summary for selected period (M19, pending CC commit) |
| `/dashboard/business/tax/wht-certificates` | WHT certificates list + create certificate form (M19, pending CC commit) |
| `/dashboard/business/inventory` | Inventory items list: summary cards (Total SKUs, Total Value, Low Stock Alerts), search, create item (M17, pending CC commit) |
| `/dashboard/business/inventory/movements` | Stock movements log with filter by item/type/date (M17, pending CC commit) |
| `/dashboard/business/inventory/valuation` | Stock valuation report: qty × MAC × total by item (M17, pending CC commit) |
| `/dashboard/business/inventory/locations` | Location CRUD: hierarchical list (M17, pending CC commit) |
| `/dashboard/business/assets` | Fixed asset register: list with status/category filter, NBV display (M18, pending CC commit) |
| `/dashboard/business/assets/categories` | Asset category CRUD (M18, pending CC commit) |
| `/dashboard/business/ai-insights` | AI Insights browser: list insights by type/severity/status; review + dismiss actions (M20, committed `9ffd9e0`) |
| `/dashboard/business/ai-insights/anomalies` | Anomaly scan trigger page: run scan, view detected anomalies with severity badges (M20, committed `9ffd9e0`) |
| `/dashboard/business/settings/ai-config` | ICE AI Categorisation config: master toggle, field controls, confidence threshold sliders, 30-day analytics panel, top corrected GLs (ICE, pending CC commit) |
| `/dashboard/personal` | Personal dashboard (individual accounts) |
| `/dashboard/profile` | User profile |
| `/platform` | Owner portal landing |
| `/platform/tenants` | Tenant list |
| `/platform/tenants/[id]` | Tenant detail |
| `/platform/audit` | **Stub** |
| `/platform/billing` | **Stub** — dedicated billing page is a future SA-B (TIER 2) deliverable. The SA-B-lite billing fields (plan + paid_since) live on the tenant detail page (`/platform/tenants/[id]`), not here. |
| `/platform/settings` | **Stub** |
| `/platform/support` | **Stub** |
| `/platform/team` | **Stub** |
| `/platform/trials` | SA lead management queue — lists trial tenants, lead-status tabs, inline notes, one-click activation |

---

## 6. Feature / Milestone Status

| Feature | Status | Notes |
|---|---|---|
| Auth (signup/login/JWT/2FA/sessions) | ✅ Working | |
| Tenant invite & user management | ✅ Working | Invitation email sent via Resend (`services/email.py`); suppressed for test tenants |
| Expense submit → approve → GL posting | ✅ Working (all modes, including split lines) | Split-line fix (Q4, 2026-07-24): `isComplete()` + `validate()` in both expense forms now skip the parent `gl_id` check and instead validate each split child for GL + required dimensions. Backend `expense_posting.py` was already correct (leaf_lines excludes split parents). |
| Approval matrix (1–3 levels) | ✅ Working | |
| Refer-back + audit trail + snapshots | ✅ Working | ⚠️ Snapshots missing M9 fields — see Issue #003 |
| Supporting documents (Supabase) | ✅ Working | |
| Chart of Accounts (CRUD, bulk upload, hierarchy) | ✅ Working | |
| CoA remap (retire codes) | ✅ Working | Lifecycle-gated: live only |
| CoA replace-all | ✅ Working | Lifecycle-gated: in_implementation only |
| Dimensions (setup, values, cascade, bulk upload) | ✅ Working | |
| Expense categories + GL mappings | ✅ Working | |
| Expense coding levels 0–4 | ✅ Working | |
| AI GL suggestions | ✅ Working | |
| Employee master data | ✅ Working | cost_center_id → org_structure confirmed |
| Employee self-onboarding | ✅ Working | ⚠️ Invite link printed to stdout (not emailed) |
| Org structure (tree CRUD, upload) | ✅ Working | |
| Implementation portal (all sections) | ✅ Working | |
| Accounting periods (generate, close, grace) | ✅ Working | Auto-generation on org save + last hard-close; manual generation deprecated but intact. Duplicate-period bug fixed 2026-06-28 — DB-level UQ now on start_date, not the formatted fiscal_year label (see §2.3). Committed `b3e70e3`. |
| Period — stub first-year close gate | ✅ Working | Year-end close section and "final period" gate now derive from MAX(period_no) per fiscal year, not a hardcoded literal 12 — fixes registration-truncated first years (e.g. Aug–Dec) which previously had no Year-end close UI and showed an incorrect "Jan–Dec" dropdown label. Fixed 2026-06-28, committed `b3e70e3`. |
| Period — auto-generation triggers | ✅ Working | Trigger 1: PATCH /api/setup/org generates current FY on save. Trigger 2: last period HARD_CLOSE generates next FY |
| Period — delete fiscal year | ✅ Working | DELETE /api/setup/periods/fiscal-year/{fy} — blocked if any closed period exists |
| Period — fiscal year name format | ✅ Working | 5 structured format codes with live preview in UI (was free-text input) |
| Period — first fiscal year end | ✅ Working | first_fiscal_year_end field on org config; backend validates range and derives start_month/day |
| Period close checklist + SOD | ✅ Working | SA can self-approve via CONSULTANT_OVERRIDE bypass (2026-07-15) |
| Period — grace default row inline edit | ✅ Working | Added 2026-07-15 (Fix 1). Default row now has inline edit for grace_value + grace_unit — PATCHes existing endpoint. |
| Period — SA SOD bypass badge | ✅ Working | Added 2026-07-15 (Fix 2). Purple "SA override" badge when prepared_by === approved_by. Backend writes CONSULTANT_OVERRIDE to period_audit_logs. |
| Period — soft-close button | ✅ Working | Added 2026-07-15 (Fix 3a). Per-OPEN-period amber button calls POST /api/setup/periods/{id}/soft-close. |
| Period — audit grace months inline edit | ✅ Working | Added 2026-07-15 (Fix 3b). Inline number input in year-end strip for AUDIT_PENDING/AUDIT_OVERDUE; PATCHes year-state endpoint. |
| Period — audit log viewer | ✅ Working | Added 2026-07-15 (Fix 3c). Auto-loads on Periods tab; table of When/Action/FY/Detail; CONSULTANT_OVERRIDE rows highlighted purple. |
| Period — future posting exceptions UI | ✅ Working | Added 2026-07-15 (Fix 3d). Create form + list in Grace tab. New GET /api/setup/periods/future-exception backend endpoint. |
| Period — FY auto-backfill on page load | ✅ Working | Added 2026-07-15 (Fix 4). Fires once after isLoading=false. No longer gates on date_of_registration or fiscal_year_start_month. allFYLabels merges by year number to avoid format-mismatch duplicates. Warning banners guide incomplete config. |
| Fiscal year close (management + statutory) | ✅ Working | |
| Currencies & FX UI | ✅ UI built | 4 tabs complete |
| Currencies & FX backend | ⚠️ JSONB only | JSONB in org_config + fx_config; no dedicated tables yet |
| Tax & statutory | ✅ Working | JSONB storage |
| Module activation (licensed gate) | ✅ Working | |
| Posting roles + GL account mapping | ✅ Working | |
| Bank accounts register | ✅ Working | |
| GL posting engine (journal entries/lines) | ✅ Working | |
| Trial balance + account ledger | ✅ Working | |
| Shadow test environment (clone engine) | ✅ Working | 13-step clone. M9.0.1: no longer used at signup (signup creates a test tenant directly); kept for on-demand test-shadow creation on existing live tenants. |
| Test-first signup + unified promotion engine | ✅ Working | M9.0.1 (2026-06-29, committed `b3e70e3`). Signup creates only a test tenant. Live is born via `POST /promotion/apply` on first promotion (same engine that handles repeat promotion, Phase 3a). See new Architectural Invariants row (§3) and Known Issues (§8) for full detail and outstanding retrofit. |
| Owner portal (platform UI) | ✅ Working | Tenant list/detail/lifecycle |
| Consultant impersonation (enter tenant) | ✅ Working | ⚠️ readonly guard incomplete — see Issue #002 |
| User-level impersonation (SA as specific user) | ✅ Working | M9.3b (`1a60a1c`). Sub = target user_id. ImpersonationUserBanner. 2 entry points. Audit log. |
| Role Hierarchy page | ✅ Working | 3-col PA/FA/UA, chip design, area/sub-area disambiguation, org chart zoom/fullscreen, collapse/expand, localStorage persistence. Drag-and-drop fixed 2026-07-15: added `e.dataTransfer.setData()` to `onDragStart` (required by HTML5 drag API). |
| Branding CSS variable injection | ✅ Working | Added 2026-07-15. `layout.tsx` fetches active theme on load; injects `--ziva-primary`, `--ziva-sidebar-*` etc. onto `:root`. `Button` primary variant uses `var(--ziva-primary)`. |
| Role-based permission assignments | ✅ Working | Scope per org role; permission_tier on approval_roles |
| Finance Review Workflow | ✅ Working | Step builder UI, drag-and-drop ordering, function-scoped chains |
| System Function Mapping | ✅ Working | Maps business functions (finance/hr/procurement etc.) to org nodes; drives finance review scoping |
| People Module — Positions (as approval_roles) | ✅ Working | Positions = approval_roles with code/grade. No separate positions table. `employee_position_assignments` FKs to approval_roles. |
| Employee-User Link | ✅ Working | `employees.user_id FK → users.id`. Cascade deactivate/reactivate/delete. Rehire reactivates UserTenant. `user_type` (employee/external) on UserTenant. |
| Employee template (latest) | ✅ Working | Org Role first + mandatory, Resumption Date mandatory, capacity enforcement, role/CC validation |
| Cost center display format | ✅ Working | `CODE - Name` (hyphen) everywhere — dropdowns, templates, all pages |
| Role dropdown display | ✅ Working | `Role Name - CC Name [Area > Sub-area]` in all dropdowns and templates |
| role_tier enforcement | ✅ Audited (2026-07-24) | `_sa()` in `platform.py` → `require_super_admin()` correctly blocks `power_admin` from all SA-only endpoints. `_require_tenant_admin` intentionally passes for power_admin (tenant-scoped op). No code changes needed. |
| Lite-mode expense export (CSV + Excel) | ✅ Done (2026-07-24) | `GET /api/expenses/export.csv` and `GET /api/expenses/export.xlsx`. Tenant admin only, Lite mode only (400 for Connected/Full ERP). Date range params; default: current month. Joins reports→users→lines→categories; excludes split-parent rows. CSV: OWASP formula-injection safe (`_csv_safe()`). Excel: `openpyxl`, bold blue header, date/money formats, freeze pane. Frontend: blue export bar on Expenses page with date inputs + Download CSV + Download Excel buttons. |
| Approval matrix — selective_tree routing mode | ✅ Working | New `routing_mode` value. DB: `approval_policies.selected_designations` JSONB. Engine (`approval_routing.py`) walks org tree and filters to designation levels in `selected_designations`; raises error if list is empty. Phase 2: designations with `role="review"` produce advisory (non-blocking) steps; `role="approve"` produce blocking steps. All-advisory guard raises `ApprovalRoutingError` at submit time if no blocking approver exists in the chain. |
| Approval matrix — open/configurable step types | ✅ Working | `finance_review_steps.step_type` is now a free-form string (max 50 chars). `VALID_STEP_TYPES` validator removed. Five built-in behaviors (capture, validate, internal_audit, review, approve) offered as datalist suggestions. `label` is fully decoupled from `step_type` — changing tag no longer overwrites name. |
| Approval matrix — function_code per step | ✅ Working | `finance_review_steps.function_code VARCHAR(50)` nullable. When set, assignee picker in approval-matrix UI filters to employees mapped to that function via `GET /api/setup/functions`. |
| Employee bulk upload — warnings | ✅ Working | Capacity exceeded and cost-centre mismatch are now non-blocking warnings (employee imports anyway). `EmployeeUploadResult.warnings: list[dict]` returned; amber warning section shown in upload result UI. |
| Positions page — occupant display | ✅ Fixed | `_resolve_position()` now queries `Employee.approval_role_id` directly (LEFT JOIN to EPA). Bulk upload sets `approval_role_id` only — never creates EPA records — so the old EPA-only query returned 0 occupants. |
| Approval matrix — advisory step behavioral distinction | ✅ Working | `expense_approvals.is_advisory` (migration `r6s7t8u9v0w1`). `selective_tree` steps with `role="review"` set `is_advisory=True`. Chain advance skips advisory levels; advisory reviewers notified but don't block. All-advisory guard raises `ApprovalRoutingError` at submit if no blocking approver exists. Advisory reject → 422. |
| Approval roles bulk-delete audit log | ✅ Working | `DELETE /api/approvals/roles` now writes `APPROVAL_ROLES_CLEARED` audit entry with `deleted_count` in the same transaction before commit. |
| Roles page — HoD auto-patch accuracy | ✅ Fixed | `Promise.allSettled` replaces `Promise.all` + `.catch(() => null)`. State only reflects roles whose PATCH succeeded. |
| Designation-based approval policy | ✅ Working | Migration `s1t2u3v4w5x6`. `approval_policies.ceiling_designation` + `finance_l[1-3]_designation`; `approval_role_thresholds.designation` replaces `approval_role_id`. Routing service reads designation strings for ceiling + threshold checks. |
| Finance chain reads FinanceReviewStep | ✅ Fixed | `approval_routing.py` `compute_chain()` now queries `FinanceReviewStep` records (ordered by level). Previously used dead `finance_l[1-3]_role_id` fields, silently ignoring all step-builder output. |
| Number formatting — shared utils | ✅ Done | `formatMoney`, `fmtCommaInput`, `stripCommas`, `formatNumber` in `utils.ts`. All local duplicates removed from 9 expense/approval pages + SplitLinePanel. |
| Financial Statements UI (P&L + Balance Sheet) | ✅ Done (2026-07-24) | Backend: `profit_and_loss()` + `balance_sheet()` in `gl_reporting.py`. Groups PL/BS accounts by `fs_head` → `fs_note`, ordered by min gl_number. `amount = total_credit − total_debit`. `GET /api/gl/financial-statements/pl` + `/bs`. Frontend: two-tab page at `/accounting/financial-statements` (P&L: date range + net income footer; BS: as_at_date + asset/L+E sign handling + footing-note banner). Full ERP mode only (`ModeNotAvailable` for Lite/Connected); sidebar "Financial statements" link visible only when `posting_mode = full_erp`. |
| Manual journal entry UI | ✅ Done (2026-07-24) | Backend: `GET /api/gl/journal-entries` (list, up to 200, date/status filter), `GET /api/gl/journal-entries/{id}` (line detail), `POST /api/gl/journal-entries` (wraps `post_journal()`; admin-only; Lite blocked with 400). Schemas: `ManualJournalCreate`, `ManualJournalLineCreate`, `JournalEntryOut`, `JournalEntryListItem` in `schemas/gl.py`. Frontend: list page at `/dashboard/business/accounting/journal-entries` (mode-gate, filter bar, inline row-expand); new entry form at `.../new` (GL picker, DR/CR inputs, balance indicator, Save as Draft / Post). Sidebar: "ACCOUNTING" section in `layout.tsx` (Journal Entries + Trial Balance links; hidden in Lite mode). |
| Three-Mode Architecture — Phase 1 backend | ✅ Committed `f24c2fe` | `posting_mode` on `tenant_org_config` (migration `h1i2j3k4l5m6`), `posting_batches` table (migration `i2j3k4l5m6n7`), `PostingResult` routing in `expense_posting.py`, `posting_batches` CRUD router, trial signup (`lifecycle_status='trial'`). Migrations committed; **pending `alembic upgrade head` locally**. |
| Three-Mode Architecture — Phase 2 SA Portal | ✅ Committed `803618e` | `GET/PATCH /api/platform/tenants/{id}/system-config`, frontend consultant config panel on `platform/tenants/[id]/page.tsx`. |
| Three-Mode Architecture — Phase 3 setup sequence | ✅ Committed `eac2584` (#51) | Mode-aware setup portal: `GET /api/setup/progress` returns sections list driven by `posting_mode`. Lite shows org+modules+employees+tax+roles+workflows+go-live. Connected adds CoA+AccountMapping. Full ERP adds Dimensions+Periods+BankAccounts. GL Group hierarchy tab in ExpenseItemPicker: committed `55028cc`. |
| Document Security Hardening | ✅ Done (5 commits) | Tasks #53–#55 + DOCX/XLSX + ORM index fix. 15-min signed URLs; magic bytes + ZIP structure validation; SHA-256 hash; Pillow image compression → WebP; pikepdf PDF compression; hash dedup (reuse blob within tenant); 15-year retention (SA-configurable via `document_retention_years`); `document_access_log` table (upload/view/delete events with IP); deletion guard (403 if within retention period or retain_until IS NULL); dedup-safe blob delete. DOCX/XLSX accepted: ZIP validated, `word/document.xml` or `xl/workbook.xml` required, `vbaProject.bin` → rejected. |
| Signup → trial flow | ✅ Done (`f24c2fe`) | `lifecycle_status='trial'` + `suppress_outbound_email=True` in `auth.py` signup |
| Consultant config panel in SA portal | ✅ Committed `803618e` | `GET/PATCH /api/platform/tenants/{id}/system-config` + frontend panel |
| "Trials & signups" SA portal page | ✅ Committed `8dc89be` | Lead management queue: list trial tenants, lead-status tabs, inline notes, one-click activation |
| Setup sequence: mode + module aware | ✅ Committed `eac2584` (#51) | `GET /api/setup/progress` now returns mode-aware section list; sidebar and step visibility driven by `posting_mode` |
| GL Group hierarchy path in ExpenseItemPicker | ✅ Committed `55028cc` (#52) | Second nav tab: GL Group → Subgroup → Sub-subgroup → GL accounts. Backend: `GET /api/config/gl/groups` + extended search. Frontend: new tab for coding levels 3 and 4 in ExpenseItemPicker. |
| SA Create Company (direct tenant creation) | ✅ Committed `336e7b4` | `POST /api/platform/tenants` — SA creates tenant directly with admin account. Password show/hide + generator in modal. `must_change_password=True` on created UserTenant. |
| Nuke tenant (hard delete) | ✅ Committed `946aa16`, `c6d05ee` | `DELETE /api/platform/tenants/{id}?confirm=true`. Deletes both test+live pair in one operation. Guards: SA-only, lifecycle≠'live', confirm param required. |
| ImpersonationSession nullable FKs | ✅ Committed `83ab8b2` | Migration `m1n2o3p4q5r6`: FK columns SET NULL on user/tenant delete. Unblocks hard-delete of users and tenants. |
| Employee cascade deactivation | ✅ Fixed `d7ddea6`, `db69e51` | Email fallback when `emp.user_id` is None; session revocation uses correct `Session.user_tenant_id`. |
| Signup 2-step (intent fields) | ✅ Committed `336e7b4` | Captures `company_size` + `interested_modules` at signup. Stored on `tenants` (migration `n2o3p4q5r6s7`). |
| Module SOT (`_ALL_MODULES`) | ✅ Committed `336e7b4` | `backend/app/constants/modules.py` is the single source of truth for module codes + labels. All consumers import from here. |
| Module activation guard | ✅ Committed `336e7b4`, corrected (pending push) | `PATCH /api/setup/modules` guard `_require_consultant` → `_require_admin`. Licensing (Add/Remove subscription) removed from tenant setup page — SA portal only. Delete confirm input blocks paste/drop. |
| Force-change-password on first login | ✅ Committed `7989709` | `must_change_password` on `user_tenants` (migration `o3p4q5r6s7t8`). Login returns flag; if true, redirects to `/auth/change-password`. Page cannot be skipped. |
| Posting mode guard | ✅ Committed `7989709` | `PATCH /system-config` returns 409 if tenant has posted journal entries and mode is being changed. |
| Collapsible tenant user list | ✅ Committed `7989709` | Users section on `/platform/tenants/[id]` collapsed by default; header shows count summary. |
| Mode-Aware Implementation Portal — sidebar + pages | ✅ Committed `63f61fe` | `layout.tsx`: `postingMode` state, sidebar hides CoA/Dimensions/Currencies/Tax/Account Mapping for Lite. New `ModeNotAvailable` component. 5 page-level guards. `expense-config`: amber banner + disabled coding cards in Lite. |
| `_org_to_response()` posting_mode fix | ✅ Committed `63f61fe` | `backend/app/routers/setup.py`: `_org_to_response()` was not passing `posting_mode=org.posting_mode` to `OrgConfigResponse` — always returned Pydantic default `"full_erp"`. One-line fix; all frontend mode guards now receive real value. |
| Mode-Aware Module Activation | ✅ Committed (pending push) | `setup/modules/page.tsx`: `MODULE_MODE_AVAILABILITY` map (14 keys). Lite shows only expense/ap/ar/tax_engine/reporting. Connected + Full ERP show all 14. Licensed-but-incompatible modules stay visible (deactivate-only path). Unlicensed incompatible modules hidden. |
| SA portal — is_internal flag + modal enhancements | ✅ Committed `4f9f9a9` | Migration `p4q5r6s7t8u9`: `tenants.is_internal BOOL DEFAULT FALSE`. `TenantListItem` schema + `create_tenant()` wired through. Create Company modal: purple is_internal toggle, mode-filtered module checkboxes (incompatible modules hidden; auto-deselected on mode switch). Tenant list shows purple "internal" badge. `MODULE_MODE_AVAILABILITY` centralised to `lib/modules.ts`. |
| Auto-generated admin temp password + credential-reveal | ✅ Ready to push | `CreateTenantRequest`: `admin_password` removed. `CreateTenantResponse`: `temp_password: str` added. Backend: `secrets.choice()` 14-char generation, `must_change_password=True`. Frontend Create Company modal: password input removed; replaced with static info note + post-creation credential-reveal screen (email + temp_password copy button + "Go to tenant →"). |
| `create_tenant` org name seeding | ✅ Committed (pending push) | `platform.py` `create_tenant()`: seeds `legal_name` + `country` into `TenantOrgConfig` at creation — Organisation tab no longer blank on first entry. |
| Tax & Compliance rename | ✅ Committed (pending push) | `tax_engine` label renamed "Tax Engine" → "Tax & Compliance" in `constants/modules.py`, `lib/modules.ts`, `modules/[module]/page.tsx`. |
| Self-service trial provisioning | ⚠️ Not started | Roadmap Phase 1 |
| OCR / AI engine | ✅ M10 (2026-07-25) | AI Engine foundation: `ai_predictions` + `ai_learning_overrides` tables. `POST /api/ai/ocr` (Anthropic claude-haiku-4-5-20251001, JPEG/PNG/WEBP/PDF). `POST /api/ai/override` (learning loop stub). OcrScanModal on expense lines. `ocr_enabled` toggle per tenant. Mode-agnostic. |
| Reporting (FS statements, export) | ⚠️ Not started | Trial balance API exists; no formatted output |
| Accounts Payable | ✅ M11 (2026-07-25) | Vendor master, invoice lifecycle (DRAFT→PAID), AP aging report, GL journals (Full ERP) + posting_batches (Connected). Approval routing via `module="payable"` policy. |
| Accounts Receivable | ✅ M14 (2026-07-28) | Customer master, invoice lifecycle (DRAFT→RECEIVED), AR aging, GL journals (Full ERP) + posting_batches (Connected). Approval routing via `module="receivable"` policy. 4 frontend pages. |
| Budget & Planning | ⏳ M16 — code written, pending CC commit | `budget_periods` + `budget_lines` tables (migration `e3f4g5h6i7j8`). Variance engine reads actuals from journal_lines (Full ERP) / posting_batches (Connected) / expense_lines (Lite). 3 frontend pages. |
| SA Billing & Subscriptions | ✅ SA-B — committed `9ffd9e0` | `pricing_plans` + `tenant_subscriptions` + `billing_events` tables (migration `f4g5h6i7j8k9`). SA-only `/api/sa/billing/*` endpoints. SA portal Billing tab. |
| Tax Engine (transaction-level) | ✅ M19 — committed `9ffd9e0` | `tax_returns` + `wht_certificates` tables (migration `g5h6i7j8k9l0`). VAT/WHT/PAYE compute service. Tax returns list + VAT summary + WHT certificates frontend pages. |
| Payroll & HR | ✅ M15 — committed `9ffd9e0` | 7 tables: salary_structures, payroll_runs, payroll_lines, payslips, leave_types, leave_requests, leave_balances (migration `h6i7j8k9l0m1`). Full ERP GL posting on run approval. 3 frontend pages. |
| Fixed Assets | ✅ M18 — committed `9ffd9e0` | 4 tables: asset_categories, assets, asset_depreciation_schedules, asset_disposals (migration `i7j8k9l0m1n2`). SL + RB depreciation. Full ERP GL posting on depreciation/disposal. 2 frontend pages. |
| Inventory & Warehouse | ✅ M17 committed `9ffd9e0`; M17b pending CC commit | 5 tables (migration `j8k9l0m1n2o3` + `l0m1n2o3p4q5`): inventory_categories, inventory_locations, inventory_items, inventory_cost_layers, stock_movements. FIFO/WACC/STANDARD costing; COGS GL posting on ISSUE; PPV journals on STANDARD RECEIPT. 5 frontend pages (includes cost-layers endpoint + GL picker UI). |
| AI Intelligence Layer | ✅ M20 — committed `9ffd9e0` | `ai_insights` table (migration `k9l0m1n2o3p4`). Anomaly detection (3σ + AP dup scan), spending pattern LLM narrative, AR/AP cash flow forecast, GL auto-classify (`gl_number`/`gl_name` fix applied 2026-07-29). AiIntelligenceError → HTTP 503 (internal names never exposed). 2 frontend pages. |
| AI Categorisation (ICE) | ⏳ pending CC commit | 6 tables (migration `p9q0r1s2t3u4`): ice_tenant_config, vendor_behavior_profiles, employee_behavior_profiles, ice_predictions, ice_feedback, ice_audit_log. Predict+feedback+config+analytics API. Tenant Admin config page. IceSuggestionBadge component. |

---

## 7. Environment & Config Facts

| Item | Value |
|---|---|
| Local DB name | `ziva_dev` (PostgreSQL on localhost:5432) |
| Backend URL (local dev) | `http://localhost:8000` |
| Frontend URL (local dev) | `http://localhost:3000` |
| API docs | `http://localhost:8000/api/docs` |
| CORS | Always includes localhost:3000, localhost:3001; additional from `ALLOWED_ORIGINS` env var |
| Email | Resend REST API via `services/email.py` (httpx). Production: set `RESEND_API_KEY` env var. Dev: no key → console simulation log (no real sends). Test tenants: `suppress_outbound_email=True` blocks all outbound. |
| Supabase Storage | Bucket: `documents` (private); config via SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_BUCKET. **Signed URL expiry: 15 minutes** ✅ (was 1 hour — reduced in task #53). |
| **Storage target (future)** | Cloudflare R2 — migrate when tenants > 5 or storage > 5 GB. Zero egress ($0.015/GB/month vs Supabase $0.021/GB + $0.09/GB egress). S3-compatible drop-in. |
| **DB infrastructure (target)** | Upgrade Render PostgreSQL to Standard ($50/month) before launch. Supabase Pro consolidates DB + storage (Supavisor pooler + 8 GB). Redis caching for tenant config at 10+ tenants. |
| **Local DB state** | **POPULATED with real data** — 2 tenants (live Red Bull + test shadow), 595 GL accounts each, employees on test shadow, 17+ org structure nodes each. DB was NOT wiped. It is fully active. |
| **Alembic version (local)** | **`d2e3f4g5h6i7`** — last CC-committed head (M14 AR). 7 new migrations written (`e3f4…→k9l0…`) in working tree, pending CC commit. Run `alembic upgrade head` after CC commits. |
| Total migrations in repo (working tree) | 80 files (73 committed + 7 pending CC commit). Working-tree head: `k9l0m1n2o3p4`. |

**Red Bull tenant pair:** *(retrofitted to the M9.0.1 test-first direction — confirmed via direct DB query, 2026-06-30. Live now points to test; test has no parent.)*
| Field | Test tenant | Live tenant |
|---|---|---|
| `tenant_id` | `e8a2fd8c-5466-4618-bb37-97681a8bfb05` | `bd2c8a25-7467-494a-96fa-30f40b5b5d19` |
| Name | Red Bull Nigeria Limited (Test) | Red Bull Nigeria Limited |
| `environment` | `test` | `live` |
| `parent_tenant_id` | `NULL` ✅ | `e8a2fd8c-5466-4618-bb37-97681a8bfb05` (points to test) ✅ |
| `lifecycle_status` | `in_implementation` | `live` |
| Employees | 40 (on test shadow only) | — |
| Accounting periods | 12 (test shadow); 36 (live) | |

**System roles (15):** super_admin, tenant_admin, employee, line_manager, approver, gm, finance_reviewer, finance_poster, finance_manager, internal_auditor, external_auditor, vendor_admin, customer_admin, hr_officer, payroll_officer

---

## 8. Known Issues Register

> **Maintenance rule:** This register is updated every working session. When an issue is resolved, DELETE it from this register (resolved items must not clutter the list). When a new issue is found, ADD it with severity + evidence. Issues that remain unresolved STAY. Each issue carries: ID, severity, short title, evidence (file/line), and date first identified.

---

**Register is clean as of 2026-06-28.** All issues #001–#009 from the 2026-06-23 audit were resolved in commit `cc881f4`.

**Fixed 2026-06-28, committed `b3e70e3` (confirmed pushed to `origin/main`, 2026-06-29):**
- Duplicate fiscal-year periods: app-level date-range-overlap check + DB-level UQ(tenant_id,start_date) via migration `k7l8m9n0o1p2`. Cleanup script `backend/scripts/cleanup_duplicate_periods.py` run successfully against local data first.
- Stub first-year (registration-truncated, e.g. Aug–Dec) had no Year-end close section and showed an incorrect "Jan–Dec" dropdown label. Root cause: 3 places hardcoded `period_no == 12` / December instead of deriving the FY's actual last period via MAX(period_no). Fixed in `backend/app/routers/setup.py` (`management_close`, `get_period_checklist`) and `apps/ziva-bi/src/app/dashboard/business/setup/periods/page.tsx` (`decPeriod`, `formatFY`).

---

**M9.0.1 — test-first environment flow, done 2026-06-29, committed `b3e70e3` (confirmed pushed to `origin/main`):**
Reconciled the tenant environment architecture per `docs/BRIEF_M9_0_1_test_first_environment_flow.md` — signup now creates ONLY a test tenant; live is born second via an explicit super-admin promotion (direction flip — see §3). No DB schema change (no migration; `live_environment` is a response-schema-only addition, not a column).
- Backend: `app/routers/auth.py` (`signup` — test-only tenant creation, no clone call), `app/routers/platform.py` (`_resolve_promotion_pair` bidirectional resolver, `_create_live_from_test`, `_copy_flat_config`, `UserTenant` mirroring on first promotion, `promote` deprecated to 410), `app/routers/tenant.py` (`promote` deprecated to 410), `app/routers/setup.py` (`mark_go_live` guarded — 400 if `tenant.environment != "live"`), `app/schemas/platform.py` (`live_environment` field on `TenantDetail`).
- Frontend: `app/platform/tenants/[id]/page.tsx` (environment toggle only renders once a live counterpart exists), `components/PromotionReviewDialog.tsx` (`isFirstPromotion` branching across title/subtitle/warning/empty-diff/success/footer), `app/dashboard/business/setup/go-live/page.tsx` (routes to platform promotion review instead of calling the old go-live endpoint directly).
- Verified: full-project `npx tsc --noEmit` clean (0 errors); all 5 touched backend files 0 null bytes + `py_compile` clean.
- Two unrelated pre-existing file-corruption incidents were found and fixed in passing during verification (NUL-byte padding on `platform/tenants/[id]/page.tsx`; stale bash-mount cache on `PromotionReviewDialog.tsx` after this session's own edits) — both confirmed clean after fix, no content lost.
- **Resolved 2026-06-30:** Red Bull retrofit (re-point the existing live+test pair, §7, to the new direction) — confirmed applied via direct DB query run by Claude Code: live tenant's `parent_tenant_id` now points to the test tenant's id; the test tenant's `parent_tenant_id` is `NULL`. No further action needed.

**Register is clean as of 2026-06-30.** No open issues.

---

### UNCONFIRMED SUSPICIONS (not yet verified — investigate before adding as confirmed findings)

1. **Residual schema drift (73 lines remaining — cosmetic only, dangerous items resolved)**: Fully investigated 2026-07-21. Critical DROP TABLE risk on `tenant_invitations` is fixed (added missing `app.models.tenant_management` import). 5 partial indexes excluded from autogenerate via `include_object()` hook in `alembic/env.py`. Migration `t2u3v4w5x6y7` drops stale `gl_account_suggestion` column. Remaining 73 `alembic check` lines are comment-only diffs, index-naming-convention mismatches, and known type widenings on `impersonation_sessions` — none are data-loss or DROP risks. Tracked as non-urgent follow-up after P1 deployment.

2. **Promotion engine may not promote org_structure changes**: `promotion_engine.py` handles 5 entity types (TenantDimension, ChartOfAccount, DimensionValue, GLDimensionRequirement, TenantAccountMapping). If org_structure nodes differ between test and live, the promotion diff/apply will silently miss them. Needs full read of `promotion_engine.py` to confirm.

3. **GET /reports visibility under zero-roles**: The role-based filter at `expenses.py` checks finance roles for non-admin users. If a user has no roles assigned, the query might default to showing only their own reports (correct) or all reports (wrong). Low-confidence; trace the query under zero-roles condition to confirm.

---

## 9. Totals

| Entity | Count |
|---|---|
| Application DB tables | ~100 (committed: 73 + M20 batch 18 = 91; pending: M17b +2 = 93, ICE +6 = 99 — see §2 for full list) |
| Alembic migrations in repo | 80 (73 committed, head `d2e3f4g5h6i7`; 7 pending CC commit, working-tree head `k9l0m1n2o3p4`) |
| Backend router files | 24 (adds budget.py, billing.py, tax_engine.py, payroll.py, fixed_assets.py, inventory.py, ai.py extended — pending CC commit) |
| Backend model files | 18 (adds budget.py, billing.py, tax_engine.py, payroll.py, fixed_assets.py, inventory.py, ai.py extended — pending CC commit) |
| Backend schema files | 17 (adds budget.py, billing.py, tax_engine.py, payroll.py, fixed_assets.py, inventory.py — pending CC commit) |
| Backend service files | 12 (adds budget_service.py, tax_compute_service.py, ai_intelligence.py — pending CC commit) |
| Backend scripts | 5 (seed_m7_categories, seed_demo_tenant, cleanup_orphan_employee_usertenant, purge_test_tenant_users, cleanup_duplicate_periods) |
| API endpoints | ~270+ (adds ~70 across 7 new routers) |
| Frontend pages | ~73+ (adds 14 for M20 batch + 1 ICE ai-config page — pending CC commit) |
