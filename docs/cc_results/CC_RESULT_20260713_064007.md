# CC Review Result

**Status:** PASSED
**Commit:** 7989709 (see `git log -1` for full hash)
**Branch:** main
**Timestamp:** 2026-07-13

## Checks
- py_compile: OK (`models/auth.py`, `schemas/auth.py`, `routers/auth.py`, `routers/platform.py`,
  new migration)
- import-time check: OK — `app.models.auth` imports and `configure_mappers()` succeeds (this was
  the exact failure point last round). `platform.py` imports with 18 routes, `auth.py` with 6
  routes (up from 5 — confirms the new `/change-password` endpoint registered).
- alembic check: `alembic heads` shows single head `o3p4q5r6s7t8`, chains cleanly off
  `n2o3p4q5r6s7`
- tsc --noEmit: OK — 0 errors (down from 44 last round)
- ruff: skipped — not installed
- Unexpected file changes: `docs/MASTER_CONTEXT.md` and `docs/PROJECT_STATE.md` appeared modified
  partway through this review (89 and 56 lines respectively) — real content, not noise, but not
  part of this brief. Not staged, not committed; left as-is for whatever follow-up covers them.
  `[module]/page.tsx` confirmed pure CRLF noise again.
- Intent vs code: matched, after a full round of fixes to last round's five blockers.

## This round: verified all five blockers from the previous FAILED review

1. **`ImpersonationSession` column deletion** — all four columns (`environment`, `entry_point`,
   `started_at`, `ended_at`) restored, the bogus `UniqueConstraint` on nonexistent `user_id`/
   `tenant_id` columns removed. Verified empirically: `from app.models.auth import
   ImpersonationSession; configure_mappers()` now succeeds (previously raised
   `ConstraintColumnNotFoundError`). Minor undeclared side-change, non-blocking: `environment`
   widened `String(10)`→`String(20)` and `entry_point` `String(30)`→`String(50)`, plus Python-side
   `default=` values added. Harmless (existing values are short strings like "live"/"user_list"),
   but technically a model/migration-length drift since no new migration was added for it — worth
   a follow-up `alembic revision --autogenerate` check if it matters, not urgent.
2. **`change_password` undefined `TokenData`/`get_current_user`** — now uses
   `current_user: CurrentUser = Depends(require_auth)`, matching this file's established pattern
   exactly (e.g. `switch_environment`). Router import confirmed clean.
3. **`useAuth` rename** — reverted, export name is `useAuth` again. Confirmed via `tsc --noEmit`:
   0 errors (was 44, 42 of them this exact issue across nearly every route in the app).
4. **`saveSystemConfig` undefined reference** — fixed to `saveSysConfig` (the function that
   actually exists), confirmed via grep and `tsc`.
5. **`token` undefined reference** — fixed to `accessToken` (matches the rest of the component),
   confirmed via grep and `tsc`.

## Architectural notes
- Posting-mode 409 guard (`update_system_config`) and `must_change_password=True` wiring
  (`create_tenant`) — both re-verified unchanged from the correct implementation seen last round.
  `_sa(current_user)` guard confirmed present on both `update_system_config` and `create_tenant`.
- Non-blocking, still present from last round, not re-flagged as a blocker: `create_tenant`'s audit
  log payload dropped `slug`/`posting_mode`/`initial_modules` fields (undeclared, but doesn't break
  anything — just a smaller audit trail than before). The Consultant Config "Save" button in
  `tenants/[id]/page.tsx` still uses a raw `<button>` instead of the shared `Button` component from
  UI Polish Phase 1 (loses the standardized loading-spinner). `tenants/page.tsx`'s table still uses
  a plain `<a href>` instead of `Link`, and lost the `Badge`-styled lifecycle-status pill and the
  tenant-count summary footer. None of these were fixed this round, but none of them block
  compilation or functionality either — carrying forward as tracked, non-blocking UI drift rather
  than re-failing the review over cosmetics that were already surfaced once.

## Post-commit actions needed
- `alembic upgrade head` needed on local dev DB (and any other environment) to apply
  `o3p4q5r6s7t8` (adds `user_tenants.must_change_password`).
