# CC Review Result

**Status:** FAILED
**Commit:** none — not committed
**Timestamp:** 2026-07-13

## Docs review (Step 1b)
Unchanged from last round's assessment — the M9.0.1 section already documents the test-first
principle this fix enforces, and this round doesn't touch anything that requires a new doc entry.
No new inaccuracies introduced this round beyond the already-noted, non-blocking prose staleness in
the `336e7b4` milestone entry (still describes a UI toggle that no longer exists) — unchanged from
last round, not re-blocking on it.

## Checks
- py_compile: OK (`backend/app/schemas/platform.py`, `backend/app/routers/platform.py`) — again,
  does not catch the bug below (runtime attribute access, not a syntax error).
- import-time check: OK — `platform.py` imports cleanly, 18 routes.
- alembic check: not applicable.
- tsc --noEmit: not re-run — `frontend/.../tenants/page.tsx` is confirmed byte-identical to the
  version already verified clean and complete last round (see below).
- ruff: skipped — not installed.
- Unexpected file changes: none — all 3 declared files match exactly.
- Intent vs code: **does not match** — the fix from last round's `CC_RESULT.md` was applied
  correctly at the primary call site, but a second, identical occurrence of the same bug in the
  same function was missed. See Issue 1.

## Issues Found

### 1. CRITICAL — second, unfixed occurrence of the same `data.environment` bug, in the same function

Good news first: the primary fix from last round landed exactly right. `create_tenant()`'s
`Tenant(...)` constructor now correctly reads `environment="test"` / `lifecycle_status=
"in_implementation"`, docstring updated to match, and `CreateTenantRequest.environment` is
confirmed absent (`hasattr(r, 'environment')` → `False`).

But a second reference to the same now-removed field survives, ~60 lines further down in the same
function — the audit-log metadata dict:
```python
await _log(
    "platform.tenant.created_by_sa",
    current_user.user_id,
    tenant.id,
    {
        "company_name": data.company_name,
        "admin_email": data.admin_email,
        "environment": data.environment,   # <-- still here, still crashes
    },
    db,
)
```
Confirmed empirically, same failure mode as last round:
```python
>>> {'company_name': r.company_name, 'admin_email': r.admin_email, 'environment': r.environment}
AttributeError: 'CreateTenantRequest' object has no attribute 'environment'
```
Practical effect: `create_tenant()` still crashes on every call, just a few statements later than
before — after `db.add(tenant)` / `db.add(TenantOrgConfig(...))` / `db.add(admin_user)` /
`db.add(UserTenant(...))` have all been queued, but before `db.commit()` is ever reached, so no
partial data lands in the DB (the session is discarded uncommitted when the exception propagates) —
but the SA still cannot create a tenant through this endpoint at all. Swept the rest of the file for
any other `data.environment` reference: the only other hit is line 167, inside the unrelated
`enter_tenant` endpoint, where `data` is a different schema (`EnterTenantRequest`) that legitimately
still has an `environment` field — confirmed not a bug, left alone correctly.

**Fix needed** — since `environment` is now always `"test"` for every SA-created tenant (no longer a
caller-supplied choice), either:
- Replace the line with a literal: `"environment": "test",` (keeps the audit trail field, now
  trivially constant), or
- Drop the key from the dict entirely (it no longer carries information once every row has the same
  value).
Either is fine — just needs one of the two, since the current line references a field that no
longer exists.

## What's confirmed correct
- `backend/app/schemas/platform.py` — unchanged from last round, still correctly has no
  `environment` field.
- `backend/app/routers/platform.py` — the `Tenant(...)` constructor and docstring fixes are both
  correct and exactly as specified. The TODO comment in `platform_promotion_apply` is unchanged
  from last round, still correct.
- `frontend/src/app/platform/tenants/page.tsx` — unchanged from last round (confirmed via diff
  against `git diff` output showing identical hunks to the prior review), still correctly has no
  `environment` state, no reference in the POST body, and the static test-first banner in place of
  the removed radio selector.

## Next step
Cowork: fix the one remaining line in the audit-log dict (`"environment": data.environment` →
either `"environment": "test"` or remove the key) inside `create_tenant()`'s `_log(...)` call in
`backend/app/routers/platform.py`. This is the last place in the file referencing the removed
schema field — confirmed via a full-file grep this round. Then trigger `/review-commit` again.
