# CC Review Result

**Status:** BLOCKED — PROJECT_STATE.md still stale (migration head + missing endpoints)
**Commit:** none — not committed
**Timestamp:** 2026-07-20

## Good news — all three prior blockers are resolved, verified in full

### 1. `selective_tree` routing mode — now implemented correctly
`backend/app/services/approval_routing.py`: new `elif policy.routing_mode == "selective_tree":`
branch (line 424). Read in full. Walks the org tree identically to `org_tree`, filters to managers
whose `ApprovalRole.designation` is in `policy.selected_designations`, raises `ApprovalRoutingError`
if no designations are selected, applies the same ceiling-role/threshold/vacant-seat logic as
`org_tree`. The pre-existing `if not chain: raise ApprovalRoutingError(...)` guard (line 600, not
part of this diff) still applies uniformly, so a misconfigured policy can't silently produce a
zero-approver chain.

One nuance, already honestly disclosed rather than hidden: the `"approve"` vs `"review"` sub-field
of `selected_designations` is not yet behaviorally distinguished — all included designations become
full blocking approval steps. This is called out explicitly both in a code comment ("Phase 1: all
included designations are treated as full approvers") and in `MASTER_CONTEXT.md`'s new entry ("All
included designations are treated as full approvers (phase 1; "review" distinction stored but
deferred)"). Non-blocking — correctly scoped and disclosed, not silently swept under the rug.

### 2. `_require_admin` gate — added
`clear_all_approval_roles()` in `approvals.py` now calls `_require_admin(current_user)` as the first
line, before `block_if_readonly_impersonation`. Matches the sibling pattern (`replace_all_coa`) I
compared it against last round.

### 3. `MASTER_CONTEXT.md` — two new, thorough §5 entries added
"Period management, HR, and org polish (~2026-07-20)" and "Approval matrix enhancements (~2026-07-20,
migration `q5r6s7t8u9v0`)" — both read in full. They accurately cover every item from both rounds:
all 4 period fixes, employee-upload warnings, the positions 0-occupant fix, the roles-page fetch fix,
the org single-save-button cleanup, the bulk-delete admin guard, `selective_tree`, open step types,
and `function_code`. No inaccuracies found against the actual code.

## Docs review (Step 1b) — blocking finding

`docs/PROJECT_STATE.md` was not touched in this round's fix (`PENDING_COMMIT.md`'s "Changed files"
list only names `MASTER_CONTEXT.md`) and is stale relative to everything shipped across both rounds:

- **Header still reads `Alembic migration head: o3p4q5r6s7t8`** (line 11) — factually wrong once
  this commits; the real head is now `q5r6s7t8u9v0`. Per this project's own standing rule
  ("PROJECT_STATE.md wins all conflicts on volatile facts... regenerated at the end of each working
  session"), a wrong migration head is exactly the kind of fact this doc exists to get right — a
  future session reading it would not know a migration needs to be run.
- §4.4 (Approvals endpoint table) has no row for `DELETE /api/approvals/roles`.
- No mention anywhere of `selective_tree`, `selected_designations`, or `function_code` on
  `finance_review_steps` — the same three terms I checked for last round, still absent.
- §6 (Feature/Milestone Status) has no rows for the employee-upload warnings change or the
  Positions 0-occupant fix (these were also missing last round and weren't added this round either,
  since the fix was scoped to `MASTER_CONTEXT.md` only).

This is narrow and mechanical to close — the content already exists almost verbatim in
`MASTER_CONTEXT.md`'s new entries; it just needs the volatile-fact version added to
`PROJECT_STATE.md`'s header, §4.4, and §6.

## Checks
- `py_compile` (`approval_routing.py`, `approvals.py`): OK
- `tsc --noEmit`: OK — 0 errors, full frontend
- Unexpected file changes: none — this round's diff is exactly the 3 files
  `PENDING_COMMIT.md` names (`approval_routing.py`, `approvals.py`, `MASTER_CONTEXT.md`)
- No new migration in this specific round's diff (the migration itself, `q5r6s7t8u9v0`, was already
  reviewed and verified clean in the prior round)

## Architectural notes (non-blocking)
- Nothing new to add beyond what was already noted in the prior two rounds' results — this round's
  diff is a clean, targeted fix with no scope creep.

## Next step
Cowork: update `docs/PROJECT_STATE.md` —
1. Header line 11: `q5r6s7t8u9v0` instead of `o3p4q5r6s7t8`, and update the "Git commit" line's
   description to mention this batch.
2. §4.4 Approvals table: add `DELETE /api/approvals/roles` row (admin-gated bulk-delete).
3. §6 Feature Status: add rows (or extend existing ones) for `selective_tree` routing mode,
   employee-upload warnings, and the Positions 0-occupant fix — content can mirror what's already
   written in `MASTER_CONTEXT.md`'s two new entries.

Then trigger `/review-commit` again. Everything else — all backend/frontend code across both
rounds — is verified correct and ready to commit as soon as this one file catches up.
