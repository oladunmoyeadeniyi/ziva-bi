# CC Review Result

**Status:** FAILED — Round 2's fix edited the wrong model class; one field swap needed
**Commit:** none — not committed
**Timestamp:** 2026-07-21

## What's confirmed correct this round
- The 5th partial index fix (`uq_chart_of_accounts_gl_number`) is fully verified working — re-ran
  `alembic check` and grepped the entire output for `chart_of_accounts`/`gl_number`: zero matches.
  Both the `Index(..., postgresql_where=...)` declaration on `ChartOfAccount` and the matching entry
  in `alembic/env.py`'s `_PARTIAL_INDEXES` set are correct.
- `docs/PROJECT_STATE.md` updates verified accurate: migration head now correctly shows
  `t2u3v4w5x6y7`, and the schema-drift "unconfirmed suspicion" note has been replaced with an
  honest, accurate status (critical risk resolved, 73 cosmetic items explicitly tracked as
  non-urgent follow-up — matches exactly what I recommended last round).
- `py_compile` passes on both changed files.
- `.bak` file removal: already executed by me in the previous round, still correctly staged
  (`D  frontend/.../go-live/page.tsx.bak`).

## Blocking finding: the `change_type` length fix was applied to the wrong class
I re-ran `alembic check` after this round's changes and it now reports a **different, new**
`change_type` mismatch on `employee_code_history` — a table that had **zero** drift in every
previous round's check. I read `master_data.py` directly to find out why:

```python
# line 499-525 — EmployeeCodeHistory (a completely different table)
class EmployeeCodeHistory(Base):
    ...
    change_type: Mapped[Optional[str]] = mapped_column(String(50), nullable=True)  # 'retrospective' or 'progressive' — VARCHAR(50) matches DB column
```
```python
# line 538-582 — EmployeeTransfer (the actual table I flagged last round)
class EmployeeTransfer(Base):
    ...
    change_type: Mapped[Optional[str]] = mapped_column(
        String(20), nullable=True,   # ← still unchanged, still wrong
        comment="'retrospective' | 'progressive' — whether this transfer applies backwards or forwards"
    )
```

Both classes happen to have a field named `change_type` — the fix was applied to
**`EmployeeCodeHistory`** (line 525) instead of **`EmployeeTransfer`** (line 579-582), which is the
one I actually flagged last round (its `ExpenseApproval`-adjacent context, `is_retrospective`
sibling field, and the "M8.1... accidentally stripped from the model during the positions merge"
comment all confirm `EmployeeTransfer` was the intended target).

I confirmed the real column lengths directly against the DB, not just the model comments:
```
employee_code_history.change_type  → VARCHAR(20)   (was already correct before this round's edit)
employee_transfers.change_type     → VARCHAR(50)   (still wrong in the model — unfixed)
```

Net effect of this round's edit: a previously-clean table (`employee_code_history`) now has new
drift, and the originally-flagged table (`employee_transfers`) still has its original drift. This
needs a straight swap, not a re-guess:

- **Revert** `EmployeeCodeHistory.change_type` (line 525) back to `String(20)` — it was correct
  before this round; remove the incorrect "VARCHAR(50) matches DB column" comment along with it.
- **Fix** `EmployeeTransfer.change_type` (line 579-582) to `String(50)` — this is the one that
  actually needs the length change.

## Checks
- `py_compile`: OK (both changed files)
- `alembic check`: still fails — the `chart_of_accounts` item from last round is resolved; the
  `change_type` item is not resolved, it moved to the wrong table.
- Unexpected file changes: none new since last round. `CLAUDE.md` remains modified in the working
  tree (the strategic-review follow-up, still correctly outside this PR's scope — not touched here).

## Next step
Cowork: swap the two `String()` lengths as described above — `EmployeeCodeHistory.change_type` back
to `String(20)`, `EmployeeTransfer.change_type` to `String(50)`. This is the only remaining item.
Everything else in this batch (tenant_management import, the 5 partial indexes + naming-mismatch
hook, the drop-column migration, render.yaml/.env.example, the `.bak` removal, and both
`PROJECT_STATE.md` updates) is verified correct and ready. Trigger `/review-commit` again once this
one swap is made — I'd expect this to be the last round needed for the critical/blocking items.
