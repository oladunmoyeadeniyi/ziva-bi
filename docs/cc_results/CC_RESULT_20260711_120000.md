# CC Review Result

**Status:** PASSED
**Commit:** eac25846a738d24ec5db62399a2e97480eacf02f
**Branch:** main
**Timestamp:** 2026-07-11

## Checks
- py_compile: OK (routers/setup.py, schemas/setup.py)
- tsc --noEmit: OK, zero errors
- ruff: skipped — not installed in this environment (not on PATH or in backend/.venv)
- Intent vs code: matched

## Architectural notes

- Traced every mode rule from the brief against the code and confirmed correct: `lite` hides CoA/Dimensions/AccountMapping/Periods/BankAccounts and unlocks Employees after Organisation; `connected` shows CoA + Account Mapping as blocking, hides Dimensions/Periods/BankAccounts, unlocks Employees after CoA; `full_erp` adds Dimensions to the blocking set and shows Periods/BankAccounts as optional (non-blocking).
- The critical fix from the brief — `account_mapping_complete = total_roles > 0 and am_count >= total_roles` — is correctly implemented as an "all roles mapped" check, not "at least one." The `total_roles > 0` guard correctly prevents a false-positive completion if the `PostingRole` catalogue is ever empty (stays `False`, doesn't silently pass).
- All new imports (`TenantAccountMapping`, `PostingRole` from `app.models.account_mapping`; `BankAccount` from `app.models.bank_account`) verified present at the top of `routers/setup.py`, and all referenced model fields (`PostingRole.role_key` as PK, `TenantAccountMapping.id`/`tenant_id`, `BankAccount.id`/`tenant_id`/`is_active`) confirmed to exist against the actual model definitions — this closes out the NameError-class bug from the previous failed review pass.
- `dims_locked` is computed unconditionally (used only by `coa_locked` in the `full_erp` branch) — confirmed this doesn't leak into `connected` mode's `coa_locked`, which correctly falls back to `not org_complete` only.
- Known deferred issues (accepted, not blocking, tracked here per the brief): (1) `get_progress` runs ~12+ sequential DB queries — fine for MVP, needs batching before scale; (2) no warning when a consultant changes `posting_mode` mid-setup — new required sections just silently appear; (3) `dims_not_applicable` remains an escape hatch even in `full_erp` mode, which could let a tenant satisfy `dims_complete` without real dimension setup — consultant's responsibility per the brief.
- `posting_mode` is additive on `ProgressResponse` — no breaking change for existing callers.

## Post-commit actions needed
None — no migration required (reads the existing `posting_mode` column from migration `h1i2j3k4l5m6`, already applied locally).
