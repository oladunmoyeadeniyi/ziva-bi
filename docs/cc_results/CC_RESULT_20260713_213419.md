# CC Review Result

**Status:** BLOCKED — docs not updated
**Commit:** none — not committed
**Timestamp:** 2026-07-13

## Docs review (Step 1b)
`docs/MASTER_CONTEXT.md` and `docs/PROJECT_STATE.md` are both unmodified (`git status --short`
shows zero output for either). This round introduces genuinely new, undocumented functionality —
confirmed by grep: `is_internal` appears nowhere in either doc, and the existing
`MODULE_MODE_AVAILABILITY` references only describe the old inline-const location in
`setup/modules/page.tsx`, not the new centralized `lib/modules.ts` export or the Create Company
modal's new mode-filtering behavior. This is a new-feature commit (new DB column, new UI toggle,
new cross-module refactor), not a bugfix to already-documented behavior — unlike the last several
rounds, there's no basis for treating this as exempt from Step 1b. Blocks per the skill's own rule.

**Non-blocking finding worth flagging while I'm in here:** the new migration
(`p4q5r6s7t8u9_tenant_is_internal.py`) docstring and revision header claim it "merges the two
divergent heads: `k2l3m4n5o6p7` + `o3p4q5r6s7t8`." I traced the actual ancestry to check this before
trusting it: `k2l3m4n5o6p7` was already fully absorbed into the mainline via the `m1n2o3p4q5r6`
merge migration from earlier this session (`alembic history` confirms `k2l3m4n5o6p7` → ... →
`o3p4q5r6s7t8` is a single unbroken chain — no second file anywhere in `alembic/versions/` declares
`k2l3m4n5o6p7` as a `down_revision` except that one prior merge and this new one). So there was only
**one** real head (`o3p4q5r6s7t8`) before this migration, not two — the "divergent heads" premise in
the docstring is factually wrong. This is harmless in practice (declaring an already-merged ancestor
as a redundant co-parent doesn't break the graph — `alembic heads` still correctly resolves to a
single head, `p4q5r6s7t8u9`), so I'm not blocking on it, but the docstring/reasoning should be
corrected next time this file is touched, since "merges two divergent heads" is a claim someone
might rely on later when reasoning about the migration graph.

## Checks (run anyway, so this doesn't cost an extra round-trip once docs land)
- py_compile: OK — `backend/app/models/auth.py`, `backend/app/schemas/platform.py`,
  `backend/app/routers/platform.py`, and the new migration file all pass.
- import-time check: OK — `platform.py` imports cleanly, 18 routes (unchanged count — no new
  endpoint added this round).
- alembic check: reports "Target database is not up to date" — expected and routine, the new
  migration simply hasn't been applied to local dev yet (same as every prior new-migration round).
  `alembic heads` resolves to a single head (`p4q5r6s7t8u9`), confirming the graph itself is
  consistent despite the docstring inaccuracy noted above.
- tsc --noEmit: OK — 0 errors.
- ruff: skipped — not installed.
- Unexpected file changes: none — all 7 declared files match exactly.
- Intent vs code: matched, in full — every one of the three bundled changes reads exactly as
  described.

## Code read in full — no functional bugs found
- **Migration** — `is_internal BOOLEAN NOT NULL DEFAULT FALSE`, correct `downgrade()`. Column
  definition itself is fine; only the docstring's stated reasoning for the merge is wrong (see above).
- **`models/auth.py`** — `Tenant.is_internal: Mapped[bool]`, matches the migration exactly
  (`nullable=False, default=False, server_default="false"`).
- **`schemas/platform.py`** — `TenantListItem.is_internal: bool` (required, correctly populated in
  every construction site — see below), `CreateTenantRequest.is_internal: bool = False`.
- **`routers/platform.py`** — `list_tenants()` populates `is_internal=t.is_internal`; `create_tenant()`
  passes `is_internal=data.is_internal` to the `Tenant(...)` constructor. Both wiring points correct.
- **`lib/modules.ts`** — `MODULE_MODE_AVAILABILITY` added, values byte-identical to what was
  previously inline in `setup/modules/page.tsx` (14 keys, same Lite allowlist) — confirmed no drift
  during the move.
- **`setup/modules/page.tsx`** — inline const removed, now imports from `@/lib/modules`. Rest of the
  file (the `isAvailableForMode` helper, `visibleModules` filter, deactivate-only safety net from
  two rounds ago) unchanged and still correct.
- **`platform/tenants/page.tsx`** — `isInternal` state, correctly reset and included in the POST
  body; `handlePostingModeChange()` correctly prunes `selModules` to drop any module incompatible
  with the newly-selected mode (called synchronously from the radio's `onChange`, so no stale-state
  race); module checkbox list correctly filtered by `MODULE_MODE_AVAILABILITY[m.key]?.includes(postingMode)`;
  `autoComplete="off"` / `"new-password"` correctly applied to admin email/password fields; purple
  "internal" badge correctly rendered next to the tenant name when `t.is_internal` is true.

## Architectural notes (non-blocking)
- Minor UX nit in the internal-sandbox toggle: the `onClick` handler lives only on the inner switch
  `<div>`, not the surrounding `<label>` — clicking the descriptive text next to the switch doesn't
  toggle it, only clicking the small switch itself does. Cosmetic, not a bug, easy to leave as-is or
  polish later.
- `TenantDetail` (the single-tenant detail-page schema, separate from `TenantListItem`) was not
  given an `is_internal` field this round — consistent with the brief's stated scope ("Shown as a
  purple 'internal' badge in the SA **tenant list**"), not an oversight.

## Next step
Cowork: add the `docs/MASTER_CONTEXT.md` (§5, new milestone entry) and `docs/PROJECT_STATE.md`
(§2 schema for `tenants.is_internal`, §4 endpoint updates for `list_tenants`/`create_tenant`, §5 or
§6 feature status) entries covering all three bundled changes. Optionally fix the migration
docstring's "two divergent heads" claim while in there. No code changes needed — everything written
this round is correct and ready to ship once the docs land. Then trigger `/review-commit` again.
