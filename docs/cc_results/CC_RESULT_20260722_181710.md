# CC Review Result

**Status:** PASSED
**Commit:** 64021ad
**Branch:** main
**Timestamp:** 2026-07-22

## Checks
- py_compile: OK (`backend/app/routers/invitations.py`, `backend/app/routers/tenant.py`)
- Unexpected file changes: none — exactly the two files in the brief, exactly one line changed
  in each (`invitations.py:159`, `tenant.py:465`), matching the brief precisely
- Intent vs code: matched — both diffs are the identical `scalar_one_or_none()` →
  `.scalars().first()` change applied to the previous round's `auth.py` fix, on the same
  `Role.name == X, Role.tenant_id.is_(None)` query shape

## Verification detail
Re-grepped the full backend for every remaining instance of this exact query pattern
(`Role.tenant_id.is_(None)`) to confirm this closes out the issue completely rather than leaving
a third or fourth site undiscovered:
- `main.py:104` — already used `.scalars().first()` (was never affected)
- `auth.py:361` — fixed last round
- `invitations.py:157` — fixed this round
- `tenant.py:463` — fixed this round

All four call sites of this query shape are now accounted for and safe. No remaining instances
of the crash pattern.

## Docs review
No `MASTER_CONTEXT.md`/`PROJECT_STATE.md` update required — this is a follow-up bug fix closing
out a finding from the previous review round, not a new feature/schema/endpoint change.
Still-outstanding, unrelated: stale Alembic head in `PROJECT_STATE.md` (lines 680-681/739).

## Architectural notes
This closes the loop from last round's review — Cowork acted directly on the CC_RESULT.md finding
rather than waiting for it to surface as a separate incident, which is the workflow working as
intended. Combined with the post-deploy dedup SQL from the prior commit, once that SQL runs the
underlying duplicate-role data problem and all three code-level symptoms are fully resolved.

## Post-commit actions needed
None new from this commit. The post-deploy dedup SQL from the previous round (`29826f0`) is still
the operative action if it hasn't been run yet — reminder carried forward, not duplicated here.
