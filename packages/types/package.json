{
  "name": "@ziva/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types mirroring backend Pydantic schemas — zero runtime code",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'stub — no build step yet'",
    "lint": "echo 'stub'",
    "type-check": "echo 'stub'"
  }
}
