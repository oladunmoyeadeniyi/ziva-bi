{
  "name": "@ziva/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Shared API client, auth context, WebAuthn hooks, and push subscription hooks for all Ziva apps",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'stub — no build step yet'",
    "lint": "echo 'stub'",
    "type-check": "echo 'stub'"
  }
}
