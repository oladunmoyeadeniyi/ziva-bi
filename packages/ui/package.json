{
  "name": "@ziva/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared React component library for all Ziva apps (Button, PageContainer, BiometricLoginButton, etc.)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'stub — no build step yet'",
    "lint": "echo 'stub'",
    "type-check": "echo 'stub'"
  }
}
